# Deferred content — the authoritative stop list

Every matrix entry NOT yet authored in `cosmic-pack`, with its blocking
dependency. An entry leaves this table only when its file lands. Batch verifiers
treat a missing file as unauthorized unless it has a row here.

| Entry | Batch | Blocked on | Unblocks at |
| --- | --- | --- | --- |
| ~~`enchants/blood-lust`~~ | 01 | ~~doc-04 bleed-stack publisher + tagged-effect PROXIMITY_EVENT~~ **AUTHORED in wave 3b-3 (R-QC46).** Both halves landed, and the first turned out to be a DIRECTION problem rather than a missing store: the stacks were already published (`%victim.var.bleedstacks%`, which `deep-wounds` and `devour` both read), but the jar's Blood Lust block lives INSIDE `Bleed.addBleedStack` and a chestplate cannot observe a sword hit landing on somebody else. So the coupling inverts — `bleed.yml` and `deep-bleed.yml` ANNOUNCE each stack they land on a player and this file subscribes, which is the honest dependency (Bleed works with nobody wearing this; this does nothing without somebody's Bleed axe). The second half is `PROXIMITY_ANNOUNCE` plus a `%proximityevent%` fact. The TAG is the load-bearing part and the reason it is a fact rather than nothing: one trigger now carries two unrelated observations, and without it Avenging Angel would proc on every bleed stack and Blood Lust on every nearby death — so `avenging-angel.yml` gained `%proximityevent% == "death"` in the same sitting. It is its own effect rather than a flag on `SET_VAR` because the announcement is a separate radius-bearing decision, and hanging it there would give every counter write in the pack a nearby scan it never uses. Authored at the measured envelope: chestplates, the 25→50 % ladder, `max(2.0, i * 0.05 * L)` over the subject's halved stacks, the 7-block ally test, DRIP_LAVA and the eat cue. The `sets/phantom` and `sets/koth` roster lines are unblocked in the same commit | closed |
| ~~`enchants/ghost`~~ | 01 | ~~matrix-UNRESOLVED external metadata consumer — owner decision pending~~ **DROPPED BY OWNER RULING (R-QC38)** — no consumer of the marker exists anywhere in the corpus, so the entry is cut from the port rather than shipped inert. The Supreme boots' roster line that named it is permanently omitted, noted in `content/sets/supreme.yml` | closed — dropped |
| ~~`enchants/enchant-reflect`~~ | 02 | ~~PROC_REBOUND (incoming-direction family; #276 STOP analysis is the design input)~~ **AUTHORED in batch 7** — the whole entry is the one effect, on the PASSIVE walk, at the measured 2/2/3/3/3/4/4/4/5/5 ladder. ONE authoring fact worth carrying forward: the band params are compared against the incoming enchant's rarity-tier **WEIGHT** (`BootCore.tierWeightsFor` reads `TierRegistry.Tier.weight()`), NOT the matrix's rung number, so "tier ≤ 5" is `tier-max: 50` on this pack's ladder and a literal `tier-max: 5` would answer for nothing at all. `tier-min` stays at the default 0, which makes this the lowest-priority grade — the heroic outranks it wherever both bands reach (see its row). The jar's non-player-victim quirk needed nothing: the marker arms for PLAYERS only, so a mob defender never claims | closed |
| ~~`enchants/phoenix`~~ | 02 | ~~ESCALATING_SOUL_COST (everything else expressible today — drop-in)~~ **AUTHORED in batch 5** — the ladder is the shipped envelope (`soul-cost: 500` + `soul-cost-growth: 2` + `soul-cost-cap: 8000` + `soul-cost-decay-period: 12000`), charged against carried gems (`soul-cost-carried: true`: matrix 07's Soul Mode section does not list Phoenix among the mode's consumers, and a last-stand save that only fired in soul mode would never fire). The recorded "one enchant exempt from defensive-proc suppression" does NOT ship: batch 7 applied the END-SUPPRESS contract rule (row 76) to it — all tier > 5 enchants must stay reachable by suppression, and `suppress-immune` exempts an enchant from ALL of it — so the flag was struck here exactly as it was from `alien-implants.yml` and `godly-overload.yml`, and a live Silence/Tombstone window now parks the save. The matrix's own decomposition, a second PASSIVE ability carrying `SUPPRESS_IMMUNE`, was doubly out: that effect immunises the WEARER, so it would unsilence every enchant they own. `silence.yml` and `tombstone.yml`, which both name the exemption from the writing end, carry the corrected reading | closed |
| ~~`enchants/self-destruct`~~ | 02 | ~~SUMMON_PAYLOAD (phase=detonate + scatter + payload effects)~~ **AUTHORED in batch 5** — the blast is a second ability on the `SUMMON_PAYLOAD` trigger (DAMAGE 16 / IGNITE 40 t / `VELOCITY mode=away strength=1.7 anchor=activator`, which on a payload activation anchors at the charge), the charges ride `scatter: 3`; two clauses pending below | closed |
| ~~`enchants/spirits`~~ | 02 | ~~SUMMON_PAYLOAD (periodic phase: pulsing ally heal on the summon)~~ **AUTHORED in batch 5** — the pulse is a second ability on the SUMMON_PAYLOAD trigger, configured by the blaze's `payload-*` box; one clause pending below | closed |
| ~~`enchants/undead-ruse`~~ | 02 | ~~SUMMON_PAYLOAD (named ring minions with per-level buff amplifiers); VIEWER_HIDE half already shipped~~ **AUTHORED in batch 5** — SPAWN_SWARM's `name`/`effects` (with the `NAME*LEVEL` amplifier suffix) carried the whole minion half; one clause pending below | closed |
| ~~`enchants/plague-carrier`~~ | 02 | ~~SUMMON_PAYLOAD (detonate burst, terrain=none); legacy note: CREEPER_HISS alias unverified~~ **AUTHORED in batch 5** — the creeper burst is a second ability on the `SUMMON_PAYLOAD` trigger, configured by the charged creeper's `payload-*` box (`ALL`, the jar's measured no-ally-filter asymmetry); the CREEPER_HISS alias has since LANDED in `Aliases.SOUND` (`CREEPER_HISS→ENTITY_CREEPER_PRIMED`, verified against both era constant lists), so the legacy lane plays the burst; one clause pending below | closed |
| ~~`enchants/nutrition`~~ | 02 | ~~MODIFY_FOOD `mode=absolute`~~ **SHIPPED**; ~~EAT is a held-only trigger (`uses held`, no equipment scan), so a worn-leggings enchant never walks it~~ **AUTHORED in wave 3b-3 (R-QC42).** The premise was FALSE and worth stating plainly for every future row that reads a trigger's flags: `usesHeld`/`scansEquipment` are carried on `TriggerKind` and consumed by NOTHING in the walk — `WornFlattener` routes by trigger mask and combat DIRECTION alone, and armour and main hand merge into one candidate list before it ever runs. A worn EAT ability already walked. What shipped is therefore the honest declaration (`Trigger.neutral("EAT")`, the same correction ADR-0049 already made to `ITEM_DAMAGE`) plus the #286 safety test that pins the routing for all ten worn-reachable triggers, so a future 'make the flag mean something' cleanup fails loudly instead of quietly emptying Nutrition, the mask INTERACT bonuses and Repair Guard. The file itself is a drop-in: `MODIFY_FOOD mode: absolute` IS the jar's multiply-the-resulting-level reading (quirk and all), the gains-only guard already lives in the read-back listener, and the 3-tick window is the hand-off from the consume event to `FoodLevelChangeEvent`. The `nutritionEnchantment` metadata is not ported under R-QC38 — the codex confirms no reader exists in the corpus | closed |
| ~~`enchants/repair-guard`~~ | 02 | ~~`%item.durabilitypercent%` fact (the EQUIP_CHANGE gap's unshipped half); authoring without it reproduces the jar's fatal proc-on-every-unequip~~ **AUTHORED in batch 7, re-decomposed onto `ITEM_DAMAGE`** (deviation **D-02-15**). The fact shipped, but it is populated on ONE trigger — the durability read is taken at the ITEM_DAMAGE site off the exact damaged stack, and an `EQUIP_CHANGE` context carries no item at all, so the matrix's unequip framing stays dead: the gate would read the cleared 0 and pass on every unequip, which is D-02-6 by another route. On ITEM_DAMAGE the recorded `<= 15/20/25 %` gate is real, and `%itemdamage.armor%` confines it to worn pieces (the trigger also fires for the held item, whose durability the fact would otherwise be describing) and incidentally closes the no-bar NaN→0 hole. Recorded looseness: the event names ONE piece while the enchant is resolved from the whole worn set, so a worn-through boot can arm a guard living on the helmet — the jar was loose from the other end (any damaged piece leaving any slot) | closed |
| ~~`enchants/metaphysical`~~ | 02 | ~~SHIPPED as a roster marker (effect-less PASSIVE, boots); its consumers (Trap/Snare/Pummel) need a victim worn-enchant-level fact for the reduction clauses~~ **CLOSED by the wave-1f enchlevel facts** — `trap.yml` and `snare.yml` author the rebate as a chance expression over `%victim.enchlevel.metaphysical%` (the key is the file STEM; the `enchants/metaphysical` stable-key spelling reads 0 in silence). The marker file stays effect-less for good. `pummel.yml` (batch 06) and `titan-trap.yml` (batch 06, `max(1, <base> - 1.25 × …)`) author theirs the same way, so all four consumers now read the marker; only the blocked-proc line they share is still owed, and Pummel's per-target SUBJECT — both on their partial rows below | closed |
| ~~`enchants/sticky`~~ | 02 | ~~shipped as a roster marker (same victim worn-level fact for its consumers)~~ **CLOSED by the wave-1f enchlevel facts** — `disarmor.yml` authors `-0.25 pp × %victim.enchlevel.sticky%` on every rung, with no floor: the expression chance's [0,100] clamp IS the recorded absolute immunity at equal-or-higher Sticky | closed |
| ~~`enchants/kill-aura`~~ | 03 | ~~two independent blockers: the activation gate needs the masks family's monster-summon state on the victim (doc-11 interaction fact, nothing on the frozen surface expresses it), and the `killaura` var it writes is matrix-UNRESOLVED — no consumer exists anywhere in the jar corpus. Authored without the gate it would burst EXPLOSION_LARGE on ordinary hits to write a var nobody reads.~~ **DROPPED BY OWNER RULING (R-QC38)** — the `killaura` var has no consumer anywhere in the corpus, so neither blocker is worth clearing; the entry is cut from the port | closed — dropped |
| ~~`enchants/hero-killer`~~ | 04 | ~~the victim worn-gear fact, and the wave-1f enchlevel facts do NOT supply it. The gate is "victim wearing ≥ 1 Heroic Armor piece" — a COUNT of worn pieces carrying an item-model classification (matrix 06: a worn LEATHER heroic-armor piece; `HeroicStat`, not an enchant), whereas `%victim.enchlevel.<key>%` returns the LEVEL of ONE custom enchant keyed by its file stem. There is no enchant key naming heroic armour (cosmic-pack ships no heroic items at all yet), and no fact family counts worn pieces, so no spelling of the new fact expresses the gate and a level must not be substituted for a count.~~ Everything else IS expressible today, so this stays a one-clause drop-in: `soul-cost: 4` carries the whole soul half (a soul-cost ability never fires outside soul mode, and the charge is the availability gate — the as-intended reading of D-04-11's over-draw), `%heldticks% > 5` is the held-swap gate, `%soultrap% != 1` the actor-side Soul Trap read (authorable whether or not Soul Trap itself ships). Authored WITHOUT the heroic gate it would hand a flat +10/20/30 % against every player, which is a different enchant. **UNBLOCKED in wave 2c:** `%victim.heroicpieces%` ships — a COUNT of the victim's four worn armour pieces carrying a heroic upgrade (`!HeroicStat.isZero()`, the same classification `HeroicService` guards on), counted in `WornResolver` where slot provenance still exists and read on the hit path off the flattened `WornState` by UUID, so it is Folia-safe on a cross-region victim. Armour ONLY — a heroic sword in the hand is not a piece the defender is wearing — and it is a count rather than the heroic stat SUM, which cannot tell four 10 % pieces from one 40 % piece. The gate authors as `%victim.heroicpieces% >= 1`. **The fact reads a REAL count as of wave 2e**: a set member declaring `heroic: true` mints already heroic through the upgrade gesture's own stats write, so the count moves off 0 without a player consuming four upgrades first. **AUTHORED in the wave-2c drain** — the whole entry is one condition over the shipped envelope: `%victim.type% == "PLAYER" && (%heldticks% > 5 \|\| %heldticks% == 0) && %soultrap% != 1 && %victim.heroicpieces% >= 1`, `soul-cost: 4` and a flat `DAMAGE_MOD(side=attack, mode=add)` at 10/20/30, no chance roll. The `%heldticks% == 0` arm is the unset-means-allowed default `boss-slayer.yml` documents — one fact, one threshold, now three files — and `%soultrap%` is the bare actor-scope read of the mark `soul-trap.yml` writes victim-side, so that var name is load-bearing across the pair. LIVE as of wave 2e (a rebase-time correction: the drain authored this as inert-on-purpose, and wave 2e's minted-heroic set pieces made the count real in flight) — the gate bites against any M-Kit wearer today, and the file's own liveness comment is corrected in the same rebase | closed |
| ~~`enchants/solitude`~~ | 03 | ~~SHIPPED as a roster marker (effect-less HELD, sword). Its whole payload is the `%solitude%` marker §Silence reads, and the matrix's "cleared on un-hold" half has no expression: `SET_VAR` has no lifecycle-teardown, so the recorded `ttl=0` marker written on take-hold never drops and one take-hold would boost every later Silence off any weapon, forever. Needs either a SET_VAR HELD/PASSIVE stop half or an actor-side co-held worn-enchant-level fact (which would drop the marker entirely).~~ **CLOSED by the wave-1f enchlevel facts** — the second route landed and the marker is dropped entirely: `silence.yml`'s S = 0..3 ladder now reads `%actor.enchlevel.solitude%`, whose map is built from the wielder's resolved equipment (armour **plus** the held hands — a non-armour held item stays in it), so a held Solitude sword reads while held and stops reading when it leaves the hand. No SET_VAR, so no teardown to miss. One recorded delta on `silence.yml`: the read is per-WIELDER, not per-item, so an off-hand Solitude sword also boosts (and can boost a Silence BOW), where the jar demanded the same item | closed |
| `enchants/target-tracking` | 05 | a victim-name token on `RUN_COMMAND`. The entry's ENTIRE payload is one command, `f focus <the struck player>`, and `RUN_COMMAND` substitutes ACTOR tokens only (`{PLAYER}`/`{UUID}`/`{WORLD}`); `{VICTIM}` exists on `MESSAGE` but not here, and the effect takes no `who` target, so nothing can name the target of the hit. Authored as-is it would run the literal string `f focus {VICTIM}` on every hit. Second, softer question for the same file: SE has no factions of its own and the matrix itself says the pack omits or retargets this enchant on a stack without a factions plugin — the command string is server-configurable, so the pack-side ruling (ship the bridge, or ship nothing) is owed alongside the token | wave 1f (token) + owner ruling (factions bridge) |
| ~~`enchants/soul-trap`~~ | 04 | ~~`SOUL_MODE_DISABLE` — an effect forcing a target player out of soul/god mode; the wave-2 soul family.~~ **AUTHORED in batch 7** — `SOUL_MODE_DISABLE` shipped and everything else was already expressible, so the file is the decomposition line for line: two mutually exclusive abilities per level split on `%victim.souls%` (the drain and the jar's dry-victim fallback hit), each carrying the whole tail the matrix applies to both paths. `soul-cost: 5` IS the attacker-side "in soul mode with souls > 0" gate (D-04-14's as-intended charge), so no separate condition. ONE clause read differently from the matrix and is now RULED: the drain is `SOUL_TRANSFER{cap: 10 x level, ratio: 0}`, not `REMOVE_SOULS(who=@Victim)`. That verb debits a victim who is THEMSELF in soul mode and is a no-op otherwise, so a victim carrying gems out of soul mode kept every one of them (`SOUL_MODE_DISABLE` is likewise a no-op there) and the trap landed only its mark, window and line; the felt behaviour is a theft from carried gems, which `SOUL_TRANSFER` expresses — it reads the gems themselves and is gated on neither end's mode. ~~`ratio: 0` destroys the take, because the actor-credit question is MATRIX-SILENT (the jar's console steal log is not ported and nothing else records where the souls went) — **the ratio flips to 1 at the polish review if that steal log proves credit**, which is the one thing left open on this entry.~~ **RULED: `ratio: 1` (R-QC36)** — the attacker banks the stolen souls. No codex check was required; the ruling is the authority, and `overflow: mint` (the default) now matters, since a gemless attacker has a credit to place. One engine clause travelled with the ruling: `SoulService.transferSouls` bailed on `ratio <= 0` BEFORE the drain, which would have made the whole effect a no-op at the authored value and refunded the victim the trap is supposed to empty — the guard is now `cap <= 0` alone, with the boundary pinned in `SoulTheftTest`. Era debt: the `WITCH_MAGIC`/`SPELL` particle rows are already in `Aliases.PARTICLE`; `ENDERMAN_SCREAM→ENTITY_ENDERMAN_SCREAM` is NOT, and is now owed on the legacy-sweep list below | closed |
| ~~`enchants/heroic-enchant-reflect`~~ | 06 | ~~`PROC_REFLECT` — the same incoming-direction primitive its base `enchants/enchant-reflect` (row above, batch 02) is stopped on: on an incoming hit, re-execute the ATTACKING item's triggered abilities with the roles swapped, gated by attacking-enchant tier ≤ `tier-max` AND reflect level ≥ that enchant's level, and then NOT applying the reflected enchant normally for that hit. Nothing on the surface does it — `REFLECT` returns a damage percentage, `ECHO_STRIKE` re-runs the actor's OWN attack, and neither re-runs the attacker's procs against him. There is no second half to ship: the entry's whole decomposition is that one row, and everything else recorded is its parameters (max 10, ladder `0.02 + 0.01×(level/3)` = 2/3/4/5 % over four integer-division steps, victim-must-be-a-player, 20-armor set, base 25.0 interval 10.0, table weight 10). The heroic grade differs from the base ONLY in `tier-max` (7 vs 5), so it reflects other heroics and soul enchants; authored without the primitive it would be an empty PASSIVE marker with no consumer anywhere. Lands WITH its base, which the contract's REPLACE pairs it to, and brings the reflect-priority chain with it (mastery `tier==8` else heroic `≤7` else normal `≤5`, exclusive — the highest-priority branch runs even when a lower one sits at a higher level)~~ **AUTHORED in batch 7 with its base**, as base + delta: one number differs, the tier band. Two authoring facts, both non-obvious. (1) The bands are rarity-tier **WEIGHTS**, not the matrix's rungs, so `<= 7` is `tier-max: 70` on this pack's ladder — and deliberately not 80, which is the mastery grade's own rung. (2) `tier-min` is **10**, the LOWEST weight on the ladder, not the soul rung: precedence is "the armed band with the greatest tier-min that still reaches this tier", so 10 outranks the base's 0 across their whole shared range and this grade answers for everything up to 70 — the matrix's exclusive chain exactly. Authored as `60..70` it would instead CEDE tiers 1-5 to a base that `removes-required` has just deleted, i.e. an upgrade that silently loses all low-tier rebound. A future mastery takes `80..80` and outranks this one on tier 8 alone, with neither file changing | closed |
| ~~`enchants/skilling`~~ | 06 | ~~`EXTERNAL_SKILL_XP_MULTIPLIER` — multiply the XP a registered third-party skill system awards, filtered to skill categories (`factor`, `categories`). The entry's ENTIRE decomposition is that one row (`1.0 + 0.04 × L`, +4 %…+40 % on the five gathering skills), so there is no second half to ship. Nothing on the surface can observe another plugin's XP events: `EXP_GAIN` / `EXP_MULTIPLY` cover VANILLA player XP only, and authoring it there would multiply the wrong economy entirely. The matrix additionally hands the scope call to the spec owner (drop the entry, or take the soft-depend), and the jar's hook is a RAW listener that bypasses every dispatch gate — it works in The End and through outpost tier suppression — so routing it through normal dispatch (END-SUPPRESS then applies) is a second, felt ruling owed at the same time. Acquisition is fully recorded for whenever it lands: max 10, table weight 2, the 21-tool item set, base 15.0, interval 6.0, and tier 2 → `uncommon` from the registration appendix~~ **DROPPED BY OWNER RULING (R-QC44)** — the scope call the matrix hands the spec owner is answered: no soft-depend on a third-party skill plugin, so `EXTERNAL_SKILL_XP_MULTIPLIER` is never built and the entry is cut | closed — dropped |
| ~~`enchants/atomic-detonate`~~ | 06 | ~~`BLOCK_MATERIAL_FILTER`, plus the asymmetric half of `FACE_ORIENTED_BOX_SELECTOR`. The volume gap is HALF closed — `@Bore{half-width, half-height, depth}` IS the matrix's face-oriented box and spells L2 (5×5×5) and L4 (7×7×7) exactly — but BORE's cross-section is symmetric and the jar's L1 (4×4, extents 1,2,1,2) and L3 (6×6, extents 2,3,2,3) are not, so two of the four rungs have no spelling. The filter gap is not closed at all, and it is the real stop: `BREAK_BLOCK` carries NO material guard anywhere (`DispatchSinkBase.breakBlock` breaks whatever the selector hands it), and BORE's `materials` is an ALLOW list whose complement — the jar's 21-entry deny list (OBSIDIAN, BEDROCK, fluids, doors, hopper, anvil, comparators…) — is unbounded and unwritable. Shipped as-is, one swing would delete bedrock, obsidian and spawners inside a 343-block cube, with neither the pickaxe/spade tool-class sublists nor the 7-material void-drops list (`BREAK_BLOCK.drops` is all-or-nothing) expressible either. Both gaps are shared with the base `enchants/detonate`, whose 3×3×depth slab BORE DOES express — the base is blocked on the deny list alone. Nothing else on the entry blocks: D-06-10's selling component is out by ruling R9, and D-06-11 / D-06-2 (origin double-drop, cached-face NPE) are fixed structurally by the engine's own break path. Its heroic metadata is a drop-in the moment the filter lands — `requires: [enchants/detonate]` + `removes-required: true`, tier 7, the 21-tool `applies-to`, max 4, weight 6, always 100 %, `PARTICLE(EXPLOSION_LARGE)` per block and one `DURABILITY(damage 1)` per explosion. **The filter half SHIPPED in wave 2c** (`exclude-materials` on all four block-volume selectors), so this entry is down to ONE stop: `@Bore`'s cross-section is still symmetric, and the jar's L1 (extents 1,2,1,2) and L3 (2,3,2,3) are not, so two of the four rungs remain unspellable. L2 and L4 already spell exactly. **AUTHORED (PARTIAL) in the wave-2c drain** — everything expressible ships: the heroic metadata (`requires: [enchants/detonate]` + `removes-required: true`, tier 7, the 21-tool set, max 4, no chance roll), the origin deny, the 21-entry `exclude-materials` list authored exactly as `detonate.yml`'s (era fallback chains and all), the `PARTICLE(EXPLOSION_LARGE)` and `DURABILITY(damage 1)` rows, and the two rungs that spell — L2 `@Bore{half-width=2, half-height=2, depth=5}` = 125 blocks and L4 `{3, 3, 7}` = 343, block for block. L1 `(1,2,1,2)` and L3 `(2,3,2,3)` ship with NO effects rather than a rounded cube: the nearest symmetric cross-sections are 3x3 = 9 and 5x5 = 25 against L1's recorded 16, and 5x5 = 25 and 7x7 = 49 against L3's recorded 36, so the enchant is INERT at those two rungs and an upgrade from II to III is a downgrade until per-axis extents land. Two riders travel with the file rather than blocking it: the VOID-DROPS list (7 bulk materials broken dropless — `BREAK_BLOCK.drops` is all-or-nothing, so a 343-block blast now buries the miner in the cobble the jar voided, and R9 rules out paying for it by selling), and the Explosives Expert clause, which is moot while neither grade authors the tool-class sublists~~ **CLOSED WHOLE (R-QC5 + R-QC7).** Both remaining stops shipped and are authored. `@Bore`'s per-side `left`/`right`/`up`/`down` extents (wave 2d) are the asymmetric half: L1 authors `{left=1, right=2, up=1, down=2, depth=4}` = 4x4x4 = 64 and L3 `{2, 3, 2, 3, depth=6}` = 6x6x6 = 216, the recorded volumes block for block, so no rung is inert and II→III is an upgrade again. `BREAK_BLOCK.void-materials` (wave 2d) is the void-drops rider: the 7 bulk materials are authored on BOTH grades by R-QC7 (base-grade extension recorded as D-06-19). Only the Explosives Expert clause is still moot, and it stays moot for as long as neither grade authors the tool-class sublists — a rider on `enchants/explosives-expert`, not a stop here | closed |
| ~~`enchants/hijack`~~ | 05 | ~~target-scoped summon conversion~~ **AUTHORED WHOLE in wave 3b-3 (R-QC45).** The ledger offered two shapes and the smaller one won: an ownership-stealing flag on `SUMMON_REBIND`, not a `who`-targeted `CONVERT_SUMMON`. The effect already addressed the right entity — the arrow's victim, through `who: @Victim` — and already carried the entire upgrade envelope; the ONLY thing standing in the way was one equality test that read `the actor's own`. `steal: true` widens that test to `somebody's`, and deliberately does NOT touch the other precondition: the target must still be a TRACKED summon. That second test is the jar's `hasMetadata("guardianSummoner")` in the engine's own vocabulary, and keeping it is what stops the `DESPAWN` + `GUARD` fake's failure mode — a farmed wild golem minting a free tier-8 guardian. The broadcast rides the effect (`steal-message` / `steal-radius`, tokens `{FROM}`/`{OWNER}`) because the sink is the only point where both names exist at once: the robbed owner is a registry entry the effect layer never sees, and it is gone from the registry one line later. Everything else was as the row predicted — the `IRON_GOLEM` type gate (not `%victim.mobtype%`), the 8/16/24/32 % ladder, the guardian-level-`2 x L` health/buff ladder, the restarted 600 t, `rise: 2`, and the `IRONGOLEM_DEATH` era row shared with Guardians. One cue coarsening the row did not name: the jar plays that sound TWICE (per-listener at 0.8/1.2 and world-audible at 1.0/0.55) and ADR-0066 emits an id at most once per event sink, so the per-listener copy is dropped and the spawner's kept | closed |
| ~~`enchants/ghostly-ghost`~~ | 06 | ~~the SAME matrix-UNRESOLVED external metadata consumer that stopped `enchants/ghost` in batch 01, one rung up: matrix 06 records that no reader of `heroicGhostEnchantment` (nor of the non-heroic `ghostEnchantment`) exists anywhere in the decompiled tree, so the whole payload is a marker nobody consults. Its own entry says "ships as an inert marker", and that reading is NOT taken, for two reasons. First, one family, one call: the base is deferred on this exact blocker and shipping the heroic while the base waits would split the pair. Second, the heroic APPLY-GATE makes the file unreachable anyway — `requires: ["enchants/ghost"]` cannot be satisfied on a pack that ships no Ghost, so the book would roll (weight 0: heroic-book only) and never apply to anything. It is NOT a roster marker in the metaphysical.yml / sticky.yml sense either: those ship because consumers inside this pack read their worn level, and this one has no reader at all. Everything else is a drop-in the moment the owner rules — PASSIVE, 20 armour, max 3, `SET_VAR(name=ghostly-ghost, value=L, ttl=0)` with the engine's WornState teardown replacing the jar's unconditional unequip clear (the engine keeps the highest remaining level, which is the better half of that quirk)~~ **DROPPED BY OWNER RULING (R-QC38)** — the one decision covered both grades, as this row anticipated: the base `enchants/ghost` is cut, so the heroic goes with it | closed — dropped |
| ~~`enchants/detonate`~~ | 06 | ~~`BLOCK_MATERIAL_FILTER` — the per-block DENY list.~~ The other half of the matrix's gap pair is CLOSED: `@Bore{half-width=1, half-height=1, depth=D}` is exactly the recorded 3x3 slab marched D layers into the mined face, and its `depth` counting the activation block's own layer reproduces the jar's 9xD volume block-for-block (the recorded 10/19/28 "blocks touched" totals are 9xD plus the D-06-3 origin re-process, which the engine never does). What is left has no expression: `@Bore`'s `materials` is an ALLOW list ("keep only these"), and the entry's filter is a 22-material DENY list plus tool-class-conditional sublists plus a comparator/diode-above guard. Inverting it would mean enumerating every mineable block on every supported version, and the omission is not cosmetic — authored without the filter the volume breaks BEDROCK, obsidian, fluids, doors, hoppers and anvils on a 100 %-at-L3 proc, which is a server-grief tool, not a mining enchant. Everything else is a drop-in when the filter lands: the depth roll is per-level chance on two ordered abilities (hi-depth rule then fallback), `PARTICLE(EXPLOSION_LARGE)` and `DURABILITY(amount=1, mode=damage, target=item)` ride the same list, and D-06-2's "orient from the mined face" is what `@Bore` already does. Its era debt travels with it (`SMOOTH_BRICK`/`MYCEL`/`*_SPADE` aliases, `LARGE_EXPLODE`→`EXPLOSION_LARGE`). Sibling `heroic/atomic-detonate` needs the same filter AND the asymmetric extents `@Bore` does not carry. **The DENY HALF SHIPPED in wave 2c:** all four block-volume selectors gained `exclude-materials`, a second `D.materials()` list on the same intern pass, so the 22-material deny list is now writable verbatim (`@Bore{half-width=1, half-height=1, depth=D, exclude-materials=[BEDROCK,OBSIDIAN,…]}`) and a type named on both lists is dropped. That is THE stop this row named, so the base entry is now authorable. What did NOT ship, and is not needed for the base entry: the tool-class-conditional sublists and the 7-material void-drops list (`BREAK_BLOCK.drops` is still all-or-nothing) — both were recorded as riders on this row rather than blockers, and the comparator/diode-above guard is expressible only as a flat deny of those materials. **AUTHORED in the wave-2c drain** — the depth ladder is the matrix's own shape: levels 1-2 roll the whole explosion (33 %/66 %), 3/6/9 are deterministic, and 4/5/7/8 are TWO ORDERED abilities — the deeper `@Bore` first at the recorded chance, then the floor depth unconditionally. That composes into "deep on a hit, shallow otherwise" with no var to carry the roll, because the floor volume is already air by then (and AIR is itself on the deny list), and it is what lets the cues ride the unconditional rule so one swing costs exactly one durability point — the jar's per-explosion charge, not a per-block one. The 22-entry deny list authors as 17 `exclude-materials` entries: five of the jar's are dropped for cause (the duplicated BEDROCK the codex itself calls dead, and the 1.8 ITEM ids SIGN / WOOD_DOOR / REDSTONE_COMPARATOR, which no block ever reports), and every era-split entry is a `1.8\|modern` FALLBACK CHAIN (`STATIONARY_WATER\|WATER`, `SIGN_POST\|OAK_SIGN`, `IRON_DOOR_BLOCK\|IRON_DOOR`, `WOODEN_DOOR\|OAK_DOOR`, `REDSTONE_COMPARATOR_ON\|COMPARATOR`) so ONE list serves both lanes — the compiler takes the first name the running version has, which is what makes a verbatim 1.8 list authorable at all against a floor-strict gate. Two deltas recorded in-file rather than approximated: the durability charge loses its pickaxe-only and no-dropless-block gates (a Detonate spade now takes the same 1 point), and PARTICLE is ONE burst at the mined block rather than the jar's burst per broken block — the effect's `who` slot resolves entities and a block-volume selector hands back locations, so a per-block spray has no spelling. The tool-class sublists and the comparator-above guard stay owed as riders | closed |
| ~~`enchants/experience`~~ | 06 | ~~block-provenance on `EXP_GAIN`.~~ The entry is one `EXP_MULTIPLY(1.0+0.25L)` behind the gate `%isblock% == true`, and that gate cannot be satisfied: `%isblock%`/`%block.type%` are populated ONLY from an activation's block, and the only `EXP_GAIN` fire path (`TriggerListeners.onExpChange` → `fireExp`) builds a blockless self-context, so the fact is false on every XP gain. Authored WITH the condition the enchant is silently dead forever; authored WITHOUT it, a held tool multiplies XP from mob kills, bottles, furnaces, trading and fishing by up to 2.25x — an economy-wide change, not the recorded block-XP boost. `EXP_MULTIPLY` on the MINE trigger is not a way around it either: `fireMine` never reads the sink's XP multiplier (only `fireExp` does), so it would compile and do nothing. Everything else about the entry is trivial (5 flat levels, no chance, no cooldown). **UNBLOCKED in wave 2c by the second option, and it is now the only honest one:** `PlayerExpChangeEvent` carries no source AT ALL — not a block, not a cause — so no hook on that path can distinguish block XP, and a `BlockExpEvent` feed would have had to invent provenance the event does not have. `fireMine` now reads the sink's `expMultiplier()` and scales `event.setExpToDrop(...)` (truncated: the recorded 7 → 8/10/12/14/15 ladder), so the entry authors as **`trigger: MINE` + `EXP_MULTIPLY(1.0+0.25L)` with NO `%isblock%` condition** — MINE *is* the block-sourced path, and its context already carries the block, so the recorded gate is structural rather than authored. The scaling lands before the drop read-backs, which is the matrix's Telepathy/Auto Smelt ordering quirk for free. **AUTHORED in the wave-2c drain** — authored exactly that way: `trigger: MINE`, five flat `EXP_MULTIPLY` rungs at 1.25/1.50/1.75/2.00/2.25, no condition, no chance, no cooldown, the 21-tool set and the codex tier table's `experience` 1. The truncation ladder is the engine's, not the file's (a 7-XP block gives 8/10/12/14/15), and the in-file comment states why the recorded `%isblock%` gate is absent so nobody re-adds it | closed |
| ~~`enchants/obsidian-destroyer`~~ | 06 | ~~block facts on `INTERACT_LEFT`.~~ The entry is `BREAK_BLOCK(drops=true)` at the clicked block under `%block.type% == "OBSIDIAN"` at `20xL` %, and the trigger carries no block: `TriggerListeners.onInteract` builds `ActivationContext(player, null, null, player.getLocation())`, so `%block.type%` is empty on every left click even though `PlayerInteractEvent#getClickedBlock` has it. Neither half survives that. The gate authored as recorded never matches (dead enchant); dropped, the ability instant-breaks WHATEVER `@Block{distance=5}` ray-traces onto — at L5 that is a 100 %-per-left-click block eraser for the whole world, the worst possible failure. The volume selectors are no substitute: they carry a `materials` allow list but centre on the activation block, which on this trigger is the player's own feet. MINE is not the trigger either — the enchant exists to skip the 9-second obsidian break, so a hook that fires only once the break finished is not the enchant. **UNBLOCKED in wave 2c:** `onInteract` now carries `PlayerInteractEvent#getClickedBlock` on all three interact triggers, and anchors the context's LOCATION at that block too — so `%block.type%` is the face the player hit AND `@Here` resolves there, which is what makes the ability safe to author: `BREAK_BLOCK:@Here` under `%block.type% == "OBSIDIAN"` can only ever break the block that was clicked. (Before the anchor moved, `@Here` on a click was the player's own feet — the ability would have mined the ground under the clicker.) A click at open air still carries no block, so the gate is false and nothing fires. **AUTHORED in the wave-2c drain** — `trigger: INTERACT_LEFT` under `%block.type% == "OBSIDIAN"`, one `BREAK_BLOCK(drops: true)` on the default `@Here`, chance `20 x L`, the 5-pickaxe set and the codex tier table's `obsidiandestroyer` 2. Both halves of the wave-2c change are load-bearing and the file says so: the fact makes the gate match, and the moved anchor is what keeps `@Here` on the clicked face instead of the clicker's feet | closed |
| ~~`enchants/demonic-gateway`~~ | 07 | `TURRET_RING` — N invulnerable stationary summons placed on open, PvP-enabled ground in a ring around the actor for T ticks, each acquiring the nearest visible enemy inside an acquire range and firing a slow configured projectile on a jittered period after an initial arming delay, a projectile hit running the actor's `IMPACT` abilities on whoever it struck. The summon surface does not reach it: `SPAWN_ENTITY` places `count` entities at ONE point (`scatter` is a bounded random offset, not a ground-scanned ring), `invincible: true` covers the damage immunity but nothing cancels an ENDER_CRYSTAL's own explosion or its block damage, and the payload phases are `periodic`/`death`/`detonate` — a proximity PULSE around the summon, with no acquired target, no projectile, no travel time, no line of sight and no refire jitter. Authored on `payload-phase: periodic` the enchant becomes a 2–5 crystal damage AURA with none of the artillery it is named for. The two halves that WOULD ship alone exist only to be fired by a skull: the 24-block `@Aoe{filter=ENEMIES}` broadcast announces turrets that were never placed, and the whole `IMPACT` ability (`IGNORE_ARMOR` + 5 %-of-max `DAMAGE`, then the `6+3·L %` `FREEZE` trap with its verbatim line, then `%distance% < 4` → `VELOCITY away 1.1` + `ENTITY_WITHER_SHOOT`) has no source to fire it. Everything else is a drop-in the moment the primitive lands: `DEFENSE` under `%victim.type% == "PLAYER"` (the attacker), chance `1.5+0.5·L` = 2.0/2.5/3.0/3.5/4.0/4.5 %, a 200 t ability cooldown armed only on placement success (CAGE-style), crystals `min(2+⌊L/2⌋, 5)` = 2/3/3/4/4/5, ring radius 7/7/7/8/8/8, ttl `min(2.5·L, 20)` s = 50–300 t, acquire range `5+L` = 6–11, skull speed 0.0525–0.0650, refire 8–13 t, trap chance 9–24 %, and D-07-7's as-intended trap duration `min(5, 1+0.125·L)` s against the measured flat 1 s. Era debt travelled with it: ENDER_CRYSTAL and WITHER_SKULL both exist on 1.8.9, and the two sound rows (`GHAST_FIREBALL` → `ENTITY_GHAST_SHOOT`, `WITHER_SHOOT` → `ENTITY_WITHER_SHOOT`) LANDED in wave 2c; crystal invulnerability on 1.8.9 was recorded as NMS-flag work (legacy-overlay hazard). **UNBLOCKED in wave 2d.2:** `TURRET_RING` shipped whole — ring placement is a per-site ground scan gated by `ProtectionProvider.allows` (owner ruling R-iv), the volley is a self-re-arming chain on each turret's own entity scheduler (a jittered period has no fixed-period spelling, and a region task at the caster cannot reach a turret across a Folia boundary), and a skull hit fires the actor's `IMPACT`. The recorded era hazard did NOT materialise: both eras treat a zeroed non-living damage event as cancelled, so the existing summon guard keeps an ender crystal alive on 1.8.9 with no NMS work. `ENDER_CRYSTAL → END_CRYSTAL` joined the alias table (1.20.5 renamed it). One divergence travels with the file: a fireball needs `setDirection` as well as a velocity, and the 1.21 line rescaled that call, so the shot reads faster there than the authored speed. **AUTHORED in QC batch 2b (R-QC1).** Six rungs of `TURRET_RING` at the recorded ring/ttl/acquire/speed ladder, the 24-block `filter=ENEMIES` broadcast, and the skull payload as THREE `IMPACT` abilities — the surface has no per-effect chance or condition, so the recorded "then roll the trap, then check the distance" tail is three independently-gated abilities against one strike. The `MOBSPAWNER_FLAMES` world effect authors as `FLAME` at a port-chosen count (a world effect carries none — the anti-gank.yml convention), and D-07-7's as-intended trap window rounds to whole ticks (23/25/28/30/33/35) with the `{seconds}` inlined at what the FREEZE really holds, the bidirectional-teleportation.yml precedent. TWO divergences recorded in-file rather than approximated: the 200 t lockout arms on the PROC, not on placement success (gate 6 reserves before the effect runs and nothing reports how many ring sites took), so a proc whose every site was blocked now costs a lockout where the jar's cost nothing; and both `%distance%` and `VELOCITY(mode: away)` anchor on the ACTIVATOR, which on an IMPACT walk is the wearer, so the 4-block gate and the 1.1 shove read the wearer instead of the crystal that fired | closed |
| ~~`enchants/feign-death`~~ | 07 | `VANISH`'s two irreducible halves — the DECOY and the early BREAK. The hide half is already shipped and the gap block is stale for it: `VIEWER_HIDE{duration, viewer: all}` is a packet-level hide of the wearer from every online player, worn armour vanishing with the body, which is the gap contract's first clause verbatim. So is the entry's `VAR_SCALED_PARAM` — an expression chance spells the recorded ladder directly (`1 + 5 * (1 - %actor.healthpercent% / 100)` for L1, base `L` pp plus up to +5 pp at 0 health), and the survivable-hit gate (`%damage% < %actor.health%`), the two damage causes, the 200 t lockout, the title/subtitle pair, the `ENTITY_WITHER_SHOOT` cue, both chat lines (`wait:`-delayed on the unvanish) and the ghost-hit companion (an ATTACK ability behind `%feign.vanished% == 1` off a SET_VAR window) all express today. What does NOT: (a) the unequipped DECOY corpse that plays the death animation and despawns at 30 t — the illusion the enchant is NAMED for, and nothing on the surface makes a player-shaped body, let alone plays a death animation on it (`SPAWN_ENTITY`/`SPAWN_SWARM`/`GUARD` take an EntityType and a nameplate); (b) the early break after `break-hits` landed outgoing hits — `VIEWER_HIDE`'s window is fixed at arm time, a counter var can COUNT the hits and no effect can end the hide on the Nth; (c) the mid-vanish join re-sync, which the shipped effect rules out in the other direction ("a relog on either side ends it early"). Authored on the hide alone it is a different enchant: a flat invisibility button granting up to six unbreakable seconds of untargetable attacking at L4, where the jar's window ends on the wearer's own landed hit (measured `L−1`, D-07-8's as-intended `L`), and with no corpse the victim never sees the death that IS the read. Everything else is a drop-in the moment the primitive lands — the 30/60/90/120 t ladder, the 30 t decoy, D-07-8's `L` free hits, the four verbatim strings, and the interactions the entry records (damage taken while vanished never consumes a hit; a re-proc refreshes rather than the jar's stale-timer early end; the ghost-hit line takes plain `§f` in place of the faction-relation prefix). Era debt travels with it: decoy and hide are packet work, so the 1.8.9 overlay needs era spawn/destroy/status packets and the pre-1.8 title protocol, and the file rides the `WITHER_SHOOT→ENTITY_WITHER_SHOOT` alias row, which LANDED in wave 2c. **MOSTLY UNBLOCKED in wave 2d.2 (owner ruling R-i):** `VANISH` shipped HIDE-ONLY — the duration ladder, the early break after `L` landed OUTGOING hits (D-07-8's as-intended count), the named var maintained while active, the re-proc refresh and the mid-vanish join re-sync all ship. The window is store-backed with one shared restore closure, because four separate paths end a vanish and "un-hide exactly once, never on half the server" has to be structural; a quit RESTORES rather than forgets, since the hidden set lives on the WATCHERS' connections and dropping the record would strand the subject invisible. Only the attack side counts a hit, so damage taken while vanished cannot spend one — the measured rule, held by the shape. What is left is the DECOY alone, deferred to the polish decision below (packet seam vs roster-marker); the effect omits `decoy`/`decoy-ttl` rather than accepting and ignoring them, so an authored file needs no edit when that decision lands. Authored without the corpse it is the enchant minus its illusion — the victim never sees the death that IS the read — **AUTHORED in QC batch 2b (R-QC30), decoy excepted.** The ruling picked the R9 shape — everything expressible ships and the corpse stays a roster marker until the `PlayerRender` era seam is a project. Shipped: the health-scaled expression chance, the survivable-hit gate and both damage causes, `VANISH{duration: 30 x L, break-hits: L, var: mastery.feign.vanished}` at D-07-8's as-intended count, all four wearer strings and the ghost-hit companion off the vanish var. The 10 s lockout is authored as the SHARED `feignwindow` var rather than an ability cooldown, because `pets/feign-death.yml` already names that contract binding: a pet click and a natural proc consume each other's window. ONE divergence: the UNVANISHED line is a `wait:`-delayed MESSAGE at the nominal window, so a vanish that breaks early on the L-th outgoing hit still prints it late — R-QC30's approved end-of-window feedback hook is where it moves when built (recorded in the partials table below) | closed except the decoy — R-QC30 roster-marker |
| ~~`enchants/horrify`~~ | 07 | `FACING_SET` and `FALL_SHIELD`, steps 3 and 4 of a six-step proc. Everything around them expresses today: the 2/4/6/8 % DEFENSE roll behind `%victim.type% == "PLAYER"`, `FREEZE{duration: 40/60/60/80, dot: 0, slow: 100}` over `@AOE{r=32, filter=ENEMIES}` (symmetric sphere for the jar's cube, per the D-07-1 geometry note), the per-target `§c§l* HORRIFIED …` line with its `ENTITY_GHAST_SCREAM` cue (alias row already landed), the wearer's summary, and the `horrify.nofall` marker as a plain `SET_VAR{ttl: 200}`. Neither gap has any spelling: `FACING_SET` writes a target's yaw to face away from the wearer and nothing on the surface aims a body (`TELEPORT`/`TELEPORT_BEHIND` move one and never turn it), and `FALL_SHIELD` grants ANY player — not an enchant wearer — a one-shot cancel of their next fall damage, where the only fall knob is the `FALL` trigger fired off the falling player's OWN gear, which a horrified stranger need not carry. A THIRD row is out on the era ruling rather than the surface: step 2's `POTION(JUMP, level=129)` no-jump is the amplifier-128 integer overflow, which `titan-trap.yml` settled pack-wide as not-shipped on either lane — on the modern lane Jump Boost 129 is a real and enormous jump that would fling the target the proc exists to pin, and then kill them on landing. So a shipped file would lose three of six rows, and losing the jump lock is what makes the missing fall shield visible: the jar's 10 s no-fall window exists to refund the landing its own root and jump lock cause, and without it a target rooted mid-air eats the fall the jar cancelled. What ships whole when both land: the numbers (chance `2·L`, `i = 2+⌊L/2⌋` = 2/3/3/4 s with levels 2 and 3 measured identical, radius 32 fixed, the one-shot 200 t window), the three verbatim strings, and the D-07-9 correction that one `immune_freeze` target skips itself instead of aborting the whole proc — that immunity and its `§8§l* DRAGON SLAYER [§7Horrify blocked!§8§l] *` line are set-side and travel with the armour-set batch (doc 10), keyed to the mastery GROUP as the family rules record. The jar's ally-protection cancel/un-cancel listener pair nets to a no-op and is not ported. **UNBLOCKED in wave 2d:** both primitives shipped — `FACING_SET` takes an `anchor` enum rather than the matrix's `of=` selector slot (the DSL carries exactly one target slot per effect), and the recorded `of=@Self` is `anchor: activator`; `FALL_SHIELD` is spent AHEAD of the FALL walk, so the landing is erased rather than reduced. The third row stays out on the era ruling, not the surface: step 2's amplifier-128 no-jump is the integer overflow `titan-trap.yml` settled pack-wide as not-shipped. **AUTHORED in QC batch 2b (R-QC1).** Five of the six steps: the `2 x L` roll, `FREEZE{40/60/60/80, dot: 0, slow: 100}`, `FACING_SET{mode: away, anchor: activator}`, the `horrify.nofall` marker beside `FALL_SHIELD{window: 200}`, and the per-target line with its ghast scream — all over `@Aoe{r=32, filter=ENEMIES}`. Step 2's amplifier-128 no-jump stays out on the pack-wide `titan-trap.yml` era ruling, not on the surface. D-07-9 needs nothing authored: the immunity is `sets/dragon-slayer.yml`'s defender-keyed `SUPPRESS_INCOMING`, which by construction skips only its own holder where the jar's early-return aborted the whole proc. The WEARER'S SUMMARY now ships too, on `%selected%` (R-QC66, ADR-0076) | closed |
| ~~`enchants/mark-of-the-beast`~~ | 07 | `VULNERABILITY` — a universal incoming-damage amplifier, and there is no second half to ship without it. The mark makes the struck attacker take DOUBLE damage from EVERY source (fall, fire and void included — measured and KEPT, because that totality is the point of a mark). Nothing on the surface amplifies what a target RECEIVES: `MARK` boosts only the marking actor's own hits (its own gap block says so), `DAMAGE_MOD side=defense` folds one activation on the wearer's own defence walk while the mark rides somebody else's body with no walk to fold into, `WEAKEN`/`OUTGOING_DEBUFF` move the opposite direction (what the target DEALS), and `MAX_HEALTH_DRAIN` shrinks the pool instead of doubling the blow. Authored without it the file would broadcast `§e§l* MARK OF THE BEAST [§7{attacker}: {seconds}s§e§l] *` to every player inside 22–32 blocks and mark nobody. Everything ELSE is expressible today and drops in the moment the primitive lands: `chance` `1.5 × L` (1.5…9 %), the DEFENSE-side `%victim.type% == "PLAYER"` gate (on a defence pass the "victim" IS the attacker — `guardians.yml`'s idiom since batch 01), the `@AOE{r=20+2·L, filter=PLAYERS}` broadcast with no ally or vanish filter (measured, kept), `PARTICLE(SPELL_WITCH, count=20, who=@Attacker)` at D-07-11's corrected location, and — new on the wave-2b surface — `cooldown-per-victim: true` at the mark duration, which IS D-07-10's expiry-based re-mark guard replacing the jar's presence-only test that wedges forever on a lost removal task. The four verbatim strings, the `d0 = ⌊L/3⌋+2` = 2/2/3/3/3/4 s ladder and the fixed ×2.0 wait with it; the per-hit line reports the APPLIED damage where the jar printed the pre-doubling half (display-value note, no ledger row), and the ally gate folds TRUCE into allies pending the spec's faction-mapping ruling. Era debt: `SPELL_WITCH` is 1.8.9's `witchMagic`, which the resolver already carries. **UNBLOCKED in wave 2d:** `VULNERABILITY` shipped, consulted on BOTH damage paths so "every source" really is every source, and folded as a NEGATED reduction rather than an outgoing bonus — the outgoing bucket is scaled by `combat.attack-scale`, so a +100 % mark authored there would land as +500 % on this pack. Non-stacking, tie-keeps-the-incumbent, with the lapse notice scheduled at the arm and matched on the expiry tick (a mark that ends quietly ends while nobody is being hit). **AUTHORED in QC batch 2b (R-QC1).** Six rungs of `VULNERABILITY{percent: 100}` at the 40/40/60/60/60/80 t ladder carrying its own `hit-message` and `expiry-message` (the lapse notice has no MESSAGE spelling — a mark that ends quietly ends while nobody is being hit), the `SPELL_WITCH` burst at D-07-11's corrected location, the unfiltered `22..32` broadcast and the marked player's own line. D-07-10 is `cooldown-per-victim: true` with the per-rung `cooldown:` set to the mark duration, so the guard expires instead of wedging. Every target slot is `@Victim`: on a DEFENSE walk the engine's victim IS the party who hit the wearer, and `{VICTIM}` is therefore the name the jar's broadcast printed | closed |
| ~~`enchants/mortal-coil`~~ | 07 | `POTION_AMP_REDUCE` — a PARTIAL amplifier cut on a named potion effect (−(L+1) for the window, re-applications capped at source−N, buff restored at expiry). `POTION_LOCK` is not a stand-in, even though the matrix calls it "the exact decomposition whenever the source amplifier is ≤ L+1": this pack ships three HEALTH_BOOST sources ABOVE that line — `godly-overload.yml` IV–VI, `protection.yml` V–VII and `overload.yml` I–III — so a Mortal Coil I proc (−2) against a Godly Overload III wearer would strip all 24 bonus HP where the matrix takes 8 (each amplifier step is 4 HP), at the exact moment that player is being hit. `overload.yml` and `godly-overload.yml` have BOTH recorded this drain as "reduces the expressed HEALTH_BOOST amplifier by its own level + 1 and skips the effect at ≤ 0 — an interaction rule, not authored here" since batch 02, so a full strip would also contradict the two files waiting for it. Nothing else on the entry blocks: chance 4/5.5/7/8.5/10 %, the ATTACK-side `%victim.type% == "PLAYER"` gate, `cooldown-per-victim: true` at the 48/56/64/72/80 t window (the wave-2b knob, and D-07-12's exact bound over the jar's "until the next potion refresh" minimum), and the one verbatim line `§c§l* MORTAL COIL {level-roman} (§7{attacker} [{seconds}s]§c§l) *` — `{level-roman}` has no MESSAGE token (`tokens` binds numbers) and authors as literal per-rung text, `{attacker}` is MESSAGE's own `{ATTACKER}`. No expiry cue, sound or particle exists (measured, kept), and the health clamp stays downward-only: the buff returns at expiry and vanilla restores max health, never the current health the clamp took. Its interaction rows travel with it — the LOVER mask's silent total immunity and the Santa mask's read of the affected state (doc 11). Era: HEALTH_BOOST exists on 1.8.9, and the recorded interception hazard dissolved on inspection — `EntityPotionEffectEvent` is cancel-only (`javap` on paper-api 1.21.11: `setCancelled`/`setOverride`, no setter for the applied effect), so the modern lane cannot rewrite an amplifier down either and the per-tick re-assert is the sole enforcer on BOTH lanes — no era seam. **AUTHORED in QC batch 2b (R-QC1).** Five rungs of `POTION_AMP_REDUCE{effect: HEALTH_BOOST, amount: L+1}` over the 48-80 t window with `cooldown-per-victim: true` at the same figure (D-07-12's exact bound), and the one verbatim line with its numeral and seconds inlined as literal per-rung text. `masks/lover.yml`, `crystals/anti-m-kit-necromancer.yml`, `overload.yml` and `godly-overload.yml` were all authored ahead of it against this key and all four are now live | closed |
| ~~`enchants/rot-and-decay`~~ | 07 | FOUR primitives, and they are the entry — `STACKING_DOT`, `PHANTOM_BLOCKS`, `SUMMON_STRIKE_PAYLOAD` and `SUMMON_PURGE`. Only two of the seven decomposition rows express today: `SPAWN_ENTITY` closed the matrix's `SUMMON_STYLE` gap (`name` + `speed` now sit on one primitive, so `§2§l{attacker}'s Rotting Corpse` at Speed I/II/III on `k = 1+⌊L/3⌋` zombies is a drop-in, riding the shared `{owner}`-token stop the guardians/spirits/undead-ruse row already carries), and `DURABILITY`'s wave-2b percent modes close the payload's decay branch (`percent-damage` 2.5 on `slot:boots`, the measured first-worn order). Everything that makes the enchant a FIELD is missing. `STACKING_DOT` is the enchant: per-victim ramping real-pipeline damage (`2 x stacks` every 10 t, +1 a tick to `min(6, L)`, 60 t stack window, 20 t lead-in, per-tick victim line) for as long as they stand in the wearer's ground, and nothing on the surface has a ground to stand in — `PERIODIC_DAMAGE` is a fixed per-target window armed at proc time, with no field, no re-entry, no ramp and no shared per-victim counter (the measured quirk that every attacker's stacks pool on one victim). `PHANTOM_BLOCKS` is the field's only tell: a per-viewer CLIENT-ONLY overlay (glowstone to self and truce-or-better, end stone to everyone else) over qualifying surface blocks in `3+⌊L/3⌋`, and `TEMP_BLOCK`/`WALKER` place REAL blocks every viewer sees identically — authored there the proc would rewrite up to a 13x13 patch of the world under the fight, per-viewer colouring lost. `SUMMON_STRIKE_PAYLOAD` is the courier contract that connects the zombies to the payload at all: cancel the summon's vanilla melee hit, consume the summon, and run the OWNER's `IMPACT` abilities on whoever it struck. `payload-phase` has no melee rung (`periodic` pulses on proximity with no hit, no cancel and no consume), so authored on it the zombies become a walking aura and the entry's own 50/50 branch — `SET_VAR(rot.branch)` + `IGNORE_ARMOR` + 10 %-of-max `DAMAGE` against the durability branch — fires on nothing. `SUMMON_PURGE` removes FOREIGN summons in 15 blocks whose owner is not the actor (or is offline / an ally) with per-summon despawn particles; `CONVERT_SUMMON` is the near-miss and inverts the reading — it CONVERTS every mob in radius to the wearer's side, permanently, so shipping it would hand the proc an army where the entry clears one. Ship the two expressible rows alone and Rot and Decay is a zombie-summon enchant with no rot and no decay: no field, no DoT, no visible ground, and a payload with no way to fire. Everything else is recorded for whenever they land — chance `5+L` = 6…15 %, field radius `3+⌊L/3⌋` = 3/3/4/4/4/5/5/5/6/6 and duration `20 x L` t, zombie speed multipliers 1.2/1.4/1.6, the 50 % health branch (10 % of max, armour-bypassing) against the durability branch (`ceil(2.5 % max)` off the first worn piece, level-independent) with `ENTITY_ZOMBIE_ATTACK_IRON_DOOR` 3.0/1.5, all four verbatim strings (`§2§l{attacker}'s Rotting Corpse`, `§c§l* DECAYING [§7-{damage}HP ({stacks} stacks)§c§l] *`, `§c§l* ROTTED [§7-{damage}HP ({attacker})§c§l] *`, `§c§l* DECAYED [§7- {durability} Durability§c§l] *`), the wearer's measured silence, the zombies dropping nothing, and the Mother of Yijki immunity to both halves (set-side, doc 10). Era debt travels with it: `END_STONE` is `ENDER_STONE` on 1.8.9 (no alias row yet), `ZOMBIE_METAL` → `ENTITY_ZOMBIE_ATTACK_IRON_DOOR` and `ZOMBIE_REMEDY`-family cues need rows, `SPELL_WITCH` → `WITCH` already has one, and a per-viewer block overlay is packet work against the 1.8 block-change format — a legacy-overlay hazard the primitive carries, not the file. **UNBLOCKED in wave 2d.2: all four primitives shipped.** `STACKING_DOT` re-asks whose ground is under each victim every pulse and its ladder is per-VICTIM, shared across attackers — which is the measured pooling quirk this row records, now structural rather than incidental. `OWNED_GROUND` reads the owner the `TempBlockLedger` stamps per LAYER. `PHANTOM_BLOCKS` went through a new `BlockVisibility` era seam, material-token shaped because 1.8 has no `BlockData`; the legacy hazard this row anticipated is real but ordinary — the seam captures the true appearance rather than a material, because reverting granite by material would repaint it as plain stone. `ENDER_STONE → END_STONE` joined the alias table with it. `SUMMON_STRIKE_PAYLOAD` is the 5th payload rung, at HIGHEST so it can cancel the blow, firing the OWNER's `IMPACT`. `SUMMON_PURGE` removes rather than converts. **AUTHORED in QC batch 2b (R-QC1), with one half inert.** Ten rungs of `SUMMON_PURGE` + `SPAWN_ENTITY` (`owner: activator`, `payload-phase: strike`, `{OWNER}`-filled nameplate) + `PHANTOM_BLOCKS` + `STACKING_DOT`, and the payload's 50/50 as two mutually exclusive `IMPACT` abilities split on a 1-tick `rot.branch` marker, so one coin flip decides the branch rather than two independent rolls. THE FIELD'S DAMAGE HALF IS INERT AS SHIPPED and the file says so: `STACKING_DOT` asks `TempBlockLedger` who PLACED the block under each victim's feet, and `PHANTOM_BLOCKS` places nothing and stamps no owner — the two primitives shipped in the same wave against different readings of what "the field" is. Laying real ground to satisfy the test would be the 13x13 world rewrite the overlay exists to avoid, so the seam is recorded (partials table below) instead of worked around. Also out: the DECAYED durability line, whose `{durability}` is the points actually taken and binds to nothing (R-QC2's no-unbound-token rule) | closed except the field DoT's ground read and the DECAYED line |
| `crystals/anti-m-kit-architect` | 08 | an owner ruling the matrix records as codex-UNRESOLVED (`matrix/10` § Architect). Architect is a line-for-line copy of Death Knight — same four mastery-pool enchants, same items, same numbers — so its Anti-M-Kit Crystal only exists as a distinct thing if the pool is TAGGED per set. In the jar the pool almost certainly tags as Death Knight's, which would make the Architect crystal inert; on this surface a crystal cancels by ENCHANT KEY, and the two sets share every key, so an Architect file would be a byte-identical duplicate of `crystals/anti-m-kit-death-knight` — shipping it would silently PICK the "both crystals live" branch of the very question the matrix leaves open (and hand players two crystals that do one job). Nothing engine-side blocks it: the moment the ruling lands, the file is a copy or a no-op | owner ruling |
| `items/mastery-shard` | 08 | a support-item TYPE that does not exist. `matrix/10` records the shard as the key to the ability sets' book restriction (§C.9: an M-Kit piece refuses ordinary enchant books unless the book carries the matching set's Mastery Shard; the shard applies at 100 %, one of each set per book, and the jar's whole-cursor-stack-loss bug is not replicated — ledger D-13-4), and `ItemsLoader` reads a closed list of likenesses — soul-gem, crystal, heroic, slot-orb, the six scrolls, the four traks, dust, books, pet, pet-food, mask, reforge. There is no shard among them, and the RESTRICTION it lifts has no surface either: no gear state says "this piece refuses ordinary books", so an authored shard would unlock a lock nobody can set — the ONLY blocker that matters. The likeness is NOT one: `matrix/13-support-items.md` § Mastery Shard decomposes §C.9 in full — the `INK_SACK` material, the per-set dye data, all four display names and the three-line lore, verbatim — so nothing here would be invention | `BOOK_RESTRICTION_PAIR` (`matrix/13` § Gap declarations): a gear-side `refuses-books: <family>` flag plus a book-side `grants-book-key: <family>` set field that lifts it, the shard applying at 100 % into that set with duplicates refused without consuming, and the refusal message authored gear-side. Neither half exists |

## Partial entries (file shipped, one clause pending)

These files ARE authored and compile — the pack is complete without them
changing. Each carries one recorded clause the frozen surface cannot express
yet, noted in the file itself and tracked here so a later wave knows where to
come back.

| Entry | Pending clause | Blocked on | Unblocks at |
| --- | --- | --- | --- |
| ~~`enchants/deep-wounds`~~ | ~~the bleed-stack fold `DAMAGE_MOD amount = "%victim.var.bleedstacks% * 0.5"` (+0.5% a live stack, ceiling ×1.11/×1.12/×1.13 at 20 stacks)~~ | **CLOSED in batch 4** — `enchants/bleed` shipped the publisher and the fold row is authored on all three levels; the file is whole | closed |
| `enchants/devour` + `enchants/rage` | the Devour→Rage exclusion is COARSER than the matrix's `devour-suppresses-rage-multiplier`: the rule parks Rage's damage fold and its `raged` victim mark but keeps the combo increment, and devour.yml ships `SUPPRESS(scope=ENCHANT, key=enchants/rage, duration=4, mode=timed, who=@Self)`, which parks the whole enchant — so the increment skips those 4 t too and a suppressed hit never banks | a suppression key finer than the enchant: Rage's fold, mark and increment are three abilities under ONE enchant key, and `scope=KIND` on DAMAGE_MOD would park every damage fold the holder owns. Closes with per-ability (or per-effect-line) suppression scope. Currently unreachable in play — Rage's `%actor.helditem% contains "_SWORD"` gate cannot pass on the axe Devour rides — so it is recorded, not a stop | follow-up candidate |
| ~~`enchants/divine-immolation`~~ | ~~the pulse's SECOND burst, LAVA x15, has no home — the effect carries a single particle slot and the flame is the one the enchant is named for~~ |**CLOSED in wave 3b-3 (R-QC57).** `PERIODIC_DAMAGE` gained `tick-particle-2` / `tick-particle-2-count`. Worth stating why this is not the duplicated-cue case ADR-0066 refuses: that rule de-dupes ONE id per event sink, and this is a second particle TYPE — the pulse's cue was always FLAME plus LAVA, not FLAME twice. The whole entry is now authored | closed |
| `enchants/trap` + `enchants/disarmor` + `enchants/snare` + `enchants/titan-trap` | ~~the Metaphysical / Sticky chance rebates and Trap's 1 % floor~~ **AUTHORED off the wave-1f enchlevel facts** (`max(1, <base> - 2.5 × %victim.enchlevel.metaphysical%)` on Trap, `<base> - 0.25 × %victim.enchlevel.sticky%` on Disarmor, `<base> - 7 × %victim.enchlevel.metaphysical%` on Snare — no authored floor where the matrix records none, since the expression chance clamps to [0,100] and that 0 bound IS the absolute immunity). Each rung has since gained the heroic grade's own additive term at the same coefficient — see the `enchants/polymorphic-metaphysical` row. What remains on all three is ONLY the victim-shown blocked-proc line (`§8§l** METAPHYSICAL (§8Trap blocked!§l) **` / `(§8Snare blocked!§l)`, and Sticky's Disarmor equivalent, which the matrix never quotes) | not a fact these files can read: the matrix fires each line "on a would-be proc", and its own decomposition is a PRE-roll chance subtraction it calls distribution-identical — so the engine rolls once, at the already-reduced chance, and nothing reports a roll that failed only because of the rebate. Re-gating on the FULL-immunity regime was considered and rejected: it would print only when the victim is already immune, and Trap's D-03-11 floor deletes that regime, so the consumers would diverge — and Titan Trap's own 0.01 floor means it is NEVER fully blocked, so a full-immunity gating would print its line exactly never. Closes with a post-gate "rebate blocked this proc" hook (or the matrix's interaction-rule layer, which the surface has no analogue for) | follow-up candidate |
| `enchants/pummel` | ~~the Metaphysical rebate~~ **AUTHORED as a batch-06 drop-in** (`<base> - 4 × %victim.enchlevel.metaphysical%` on every rung, no floor — the expression chance's own [0,100] clamp is the bound the matrix records; the heroic grade's additive term at the same coefficient landed with the `enchants/polymorphic-metaphysical` ruling). What remains is the SUBJECT and the blocked line `§8§l** METAPHYSICAL (§8Pummel blocked!§l) **`. **BOTH CLOSED in the per-target wave (R-QC66, ADR-0076).** The rebate is now `each-chance: "100 - 4 x M - 4 x PM"` on the splash row over `%target.enchlevel.*%`, and the ability's `chance:` is the plain `6 x L` ladder again — so D-04-13's ruling lands as written: the veto is per BODY, decided against the bystander under test, where the shipped drop-in could only read the STRUCK victim (who is `exclude=victim` and never in their own splash) and so vetoed the whole crowd. The blocked line ships as the exact COMPLEMENT of that row over the same single draw (`%target.roll% >= …`), which is the "would-be proc" moment a pre-roll subtraction could not produce. R-QC69a confirms the balance delta — one Metaphysical wearer now shields only themselves, strictly a nerf — and D-04-16 records it | nothing | closed |
| ~~`enchants/corrupt`~~ | ~~the Inversion carve-out: a victim HOLDING an Inversion item takes the flag and the portal burst but NONE of the DoT rows (jar order kept)~~ **AUTHORED as a batch-06 drop-in** off `%victim.enchlevel.inversion%` (the worn map spans armour plus the held hands, so a held Inversion sword reports its level; `%victim.helditem%` was only ever a material, which is why the row existed). Shipped as TWO MUTUALLY EXCLUSIVE branches per level (`… && %victim.enchlevel.inversion% == 0` for the rot, `… > 0` for flag+burst only) rather than one ability with a gated tail: the surface has no per-effect condition, and two independently-rolled abilities would let the flag land without the rot — disjoint conditions keep it to one roll at the recorded chance. The enchant's root `condition:` is gone with them, since an ability condition REPLACES it rather than anding. The other direction was already CLOSED in batch 4 (swords/Inversion reads the flag actor-side as a bare `%corrupt%`) | nothing — the fact landed in wave 1f and the clause is authored | closed |
| ~~`enchants/bleed`~~ | ~~the PLAYER-side derived slow — `MOVEMENT_SPEED(speed = "0.2 - 0.005 * i")` held for as long as stacks live — and the death half of D-04-6 (stacks clear on death)~~ |**BOTH CLOSED in wave 3b-3 (R-QC57).** (a) `MOVEMENT_SPEED` gained `hold:`, which is the unbounded form the row asked for spelled as a flag rather than a negative span (`D.TICKS` floors at 0, so the sink's own `-1` sentinel had no surface). A held grant schedules no revert, because what ends it is a stack count and not a clock; the ONE thing it may not opt out of is the quit drain, so a logout still restores 0.2 rather than persisting an altered walk speed to disk (F08). Three things end it and they are the jar's own three — `blessed.yml`'s proc (which already restores 0.2 beside its counter reset), the next stack re-granting at the new depth, and a logout. Death does NOT, which is measured and recorded in-file. (b) `SET_VAR` gained `clear-on-death:` and the sweep is per ENTRY rather than a registry of names, so a reload cannot leave a stale global opinion behind. A MOB still loses everything; a PLAYER loses only what asked to go, because their vars survive death on purpose — a blanket clear would end every mark and window somebody else armed on them. `deep-bleed.yml` writes the same counter and takes the same flag | closed |
| `enchants/cleave` | the recorded PER-SPLASH-VICTIM 20 t (1000 ms) stamp, shipped as the ability's own 30 t per-player bucket. **NOT closed by `cooldown-per-victim`, which shipped and drained `enchants/thundering-blow` below.** That knob keys gate 6 on the ACTIVATION's victim — the struck target — and Cleave's stamp is per SPLASH MEMBER, who are selector targets of one activation and never the cooldown key. Taking it here would silently re-price the enchant (one bucket per primary target instead of one per attacker) without expressing the recorded rule. **CLOSED (R-QC66, ADR-0076):** `each-cooldown: 20` on the DAMAGE row stamps the SELECTOR TARGET in `CooldownStore`'s existing per-victim dimension, under a target bucket gate 6 never produces — so the 20 t splash-member stamp and the ability's own 30 t attacker throttle are two windows that cannot collide | nothing | closed |
| ~~`enchants/thundering-blow`~~ | ~~the recorded PER-VICTIM 50 t cooldown, shipped as the engine's per-player bucket~~ **AUTHORED in batch 7** — `cooldown-per-victim: true` on the enchant, which keys the 50 t bucket on the struck entity instead of the coarse player bucket. Exact here, because this enchant's activation victim IS the entity the matrix stamps; the coarsening note is retired | ~~per-victim cooldown scope~~ **SHIPPED** (wave 2b) as the ability/enchant-level `cooldown-per-victim` | closed |
| `enchants/mighty-cleave` (batch 06) | the recorded PER-SPLASH-VICTIM 20 t stamp, shipped as the ability's own 30 t per-player bucket — the counterpart's row above, one grade up. The SHARED half IS authored here (`SUPPRESS(scope=ENCHANT, key=enchants/cleave, duration=20, who=@Self)`, the mutual lockout `cleave.yml` assigns to the heroic side), coarsened from the victim to the holder. **CLOSED (R-QC66, ADR-0076)** exactly as the counterpart above: `each-cooldown: 20` on the DAMAGE row keys the selector member, leaving `cooldown-per-victim` to key the ability's own 30 t throttle on the struck player | nothing | closed |
| `enchants/polymorphic-metaphysical` (batch 06) | ~~TWO clauses, and together they leave the file INERT in play.~~ ONE clause now. ~~(a) The downstream rebate feed: `%victim.enchlevel.<key>%` keys on the FILE STEM (`WornResolver.stemOf`), so this grade publishes `polymorphic-metaphysical` while `trap.yml` (−2.5 pp), `titan-trap.yml` (−1.25 pp) and `snare.yml` (−7 pp) all read `metaphysical` alone and see 0 for a wearer carrying only the heroic — the three files' "the heroic writes the SAME worn state" comments do not hold on the shipped fact, and Pummel's −4 pp will inherit the same trap.~~ **(a) CLOSED by owner ruling — the feed is ADDITIVE, not highest-of-the-two.** All four consumers now carry a SECOND term at their own coefficient, `<base> − <coef> × %victim.enchlevel.metaphysical% − <coef> × %victim.enchlevel.polymorphic-metaphysical%`: `trap.yml` −2.5 pp and `titan-trap.yml` −1.25 pp keep their `max(1, …)` floors (D-03-11 and the entry's own 0.01), `snare.yml` −7 pp and `pummel.yml` −4 pp keep none, the expression chance's [0,100] clamp being the bound. The heroic's coefficient repeats the base's because matrix 06 § Polymorphic Metaphysical records the heroic's stored level feeding the SAME reductions. `max()` was the shape this row previously proposed and is not what shipped: additive and max() are indistinguishable in play (both grades are BOOTS-only and the contract's REPLACE keeps them off one item, so the two terms are never non-zero together), and additive is the safe reading if a pack ever separates the grades. The four files' stale "the heroic writes the SAME worn state" comments are rewritten to the stem-keyed truth. (b) The Ice Aspect veto (`0.2 × level` = 20/40/60/80 % per proc) and its wearer-shown line `§d§l** POLYMORPHIC METAPHYSICAL (§7Ice Aspect blocked!§d§l) **`. The roster marker itself ships (applies, renders, occupies its rung) | (a) ~~NOT an engine gap — a one-term edit per consumer, `max(%victim.enchlevel.metaphysical%, %victim.enchlevel.polymorphic-metaphysical%)`, which spells the highest-only rule out; four files across three batches, so it is recorded rather than reached from the heroic's own file.~~ **AUTHORED** — never an engine gap, and the four consumer edits landed in one pass on the owner's additive shape. (b) the interaction-rule layer the Metaphysical blocked-proc lines already wait on: a chance-per-INCOMING-proc veto. SUPPRESS cannot stand in — the attacker's ATTACK walk runs BEFORE the defender's DEFENSE walk in one event, so a defensively armed window parks Ice Aspect a hit late, for a fixed span, against every other victim too; `mode: next-hit` is consumed on the target's own incoming hits, not their next swing | (a) ~~drop-in~~ closed + (b) follow-up candidate |
| ~~`enchants/spirits`~~ | ~~the ally heal pulse never reaches its effects. The payload ability is authored and configured (period/radius/ALLIES/max-targets all per level), but every block of an enchant shares ONE cooldown bucket, and the 200 t re-arm the DEFENSE spawn takes covers the blaze's whole 200 t life — gate 6 denies every pulse it could ever fire.~~ **AUTHORED in batch 7** — `cooldown-scope: none` on the SUMMON_PAYLOAD block at every level, so the pulse neither blocks on nor arms the bucket the spawn proc owns; the spawn keeps its 200 t re-arm and the heal now pulses for the blaze's whole life. `enchants/rocket-escape`'s FALL companion, named in the same pool bullet and starved the same way by its own 600 t launch, took the identical one-line fix in the same pass (it was cancelling nothing, so the escape ended in the fall damage it had just saved you from) | ~~per-ability cooldown-scope opt-out~~ **SHIPPED** as `cooldown-scope: none` | closed |
| ~~`enchants/guardians` + `enchants/spirits` + `enchants/undead-ruse`~~ | ~~the summon NAME's owner token. All three author the matrix's verbatim `{owner}` string as `{player}`, but nothing substitutes it~~ **FLIPPED in batch 7**: the token landed as `{OWNER}` (`SummonNames.fill`, substituted before colour translation) and all three files now carry `§b§l{OWNER}'s Guardian` / `§c§l{OWNER}'s Spirit` / `§d§l{OWNER}'s Undead Minion`. Guardians (GUARD auto-binds the activator) and Spirits (`owner: activator`) both fill with the wearer's name. **Undead Ruse fills EMPTY for now** — `SPAWN_SWARM` passes an owner ONLY when `cloud:` is set, and an ownerless summon substitutes the empty string rather than showing a player the authoring syntax — so its nameplate reads `'s Undead Minion` until the `owner` param that file's OTHER row already waits on lands. Both halves then close together; nothing further is owed on Guardians or Spirits | ~~an actor-name token on the summon-name param~~ **SHIPPED** as `{OWNER}`; Undead Ruse additionally needs the `owner` param its own row below already waits on, since a swarm passes no owner to fill it with | closed (Undead Ruse's fill rides its `owner` row) |
| `enchants/undead-ruse` | the minion OWNERSHIP half: the jar's minions never target or hurt their summoner, and these will — vanilla zombie AI takes the wearer standing inside their own ring. Count, buff amplifiers, names, permanence, the vanish window and all three particle bursts ship | an `owner` param on SPAWN_SWARM. `owner: activator` on SPAWN_ENTITY/GUARD binds `GuardianCasts`, which `SummonTargetGuardListener` reads to cancel a summon acquiring its owner; the swarm spawner binds only `SwarmSpawns` (disable-teardown), so there is no ownership to read | wave 1f |
| ~~`enchants/self-destruct`~~ | ~~the per-level FUSE ladder (100/80/60 t = 5/4/3 s): all three levels ship on vanilla's own 80 t fuse~~ |**CLOSED in wave 3b-3 (R-QC57)** — `SPAWN_ENTITY` gained `fuse`, the shape `powered` already takes. It is deliberately a SEPARATE knob from `ttl` rather than a reinterpretation of it, because `ttl` despawns: an authored 60 t ttl would have removed the charge before it could explode and left L3 spawning duds, which is precisely why the row existed. `ttl` stays omitted — a primed TNT always leaves by exploding | closed |
| ~~`enchants/plague-carrier` + `enchants/self-destruct`~~ | ~~the payload only lands while the OWNER still wears the piece — both entries are death-triggered, `SummonPayloadService` runs the owner's abilities out of their LIVE `WornState`, and the blast most likely resolves for an owner who no longer carries the enchant, leaving a DUD~~ |**CLOSED in wave 3b-3 (R-QC57)** — the ledger's first option, the abilities snapshotted onto the SUMMON at spawn. `PetSummons` carries the pinned ids beside the flags it already holds, so the pin's lifetime is the summon's and its cleanup is free; the capture rides the existing `SummonPayloads` seam (`payloadCandidates`), because reading worn state is the feature layer's business and no engine class may. The payload then runs through `runDetached`, which exists for exactly this shape — a candidate list that has already left the worn state, so a lookup would reject it and a post-death fact mask would read every authored fact as its default. Stale pins are dropped on the same generation stamp R-QC58 put on the flags: dense ids do not survive a reload, and a summon that outlives one falls back to the live read rather than firing ids that now mean something else. The shared-bucket `cooldown:` readings both files carry are untouched | closed |
| ~~`enchants/hellfire` + `enchants/infernal`~~ | ~~the BOW_FIRE flaming-arrow dressing — permanent (`2147483647` t) fire on Hellfire's shot, `<level>*60` t on Infernal's~~ |**CLOSED in wave 3b-3 (R-QC57).** `PROJECTILE_DRESSING` gained `fire-ticks` AND its `type` became optional, which together make the effect with no rider exactly a dressing: it lights the arrow itself. That is the only surface that can — `IGNITE` takes its targets from a selector and no selector names a shot in flight, which is the premise the row recorded. Both files author a BOW_FIRE ability at the measured values | closed |
| `enchants/dimension-rift` — **STOPPED in wave 3c, not built** | the REVERT hook: as each rifted block restores, players within 2 blocks lose Jump Boost and are popped upward (`REMOVE_POTION(JUMP_BOOST)` + `VELOCITY(mode=add, y=0.5)`). The soul-sand floor, the scattered web layer (`fill-chance` closed the matrix's `TEMP_BLOCK_FILL_CHANCE` gap), both particle aggregates, the level*15+40 timers and the player gate all ship | `TEMP_BLOCK_REVERT_HOOK` — an effect list executed at TempBlockLedger restore time, re-selecting by proximity to the restored blocks. `wait:` fires on a fixed offset from the activation, not on the ledger's own revert, and it cannot re-target off blocks it did not place. **WHY IT STOPPED (wave 3c):** `TempBlockLedger.revert` returns `void` and `Pending` carries `(layerId, seq, delayTicks)` — no per-stamp payload channel exists but `owner`. Worse, restore has FOUR entry points (`revert`, `reclaim`'s TRAP_BREAK early-restore, `healExpired`'s leak sweep, and `rearmChunk`'s reload replay), and a hook wired to one of them is a lie in play: the pop-up would fire on a natural expiry and not on a shattered trap. Wiring all four means an effect list that outlives its activation — the actor may be offline, the region may have unloaded and re-armed, the snapshot may have rolled — which is the payload-pin problem R-QC57 solved for summons, one layer down and with a chunk unload in the middle. That is its own wave, not a leftover | its own wave |
| `enchants/explosive` + `enchants/cowification` | two rider clauses. (a) The jar spawned Explosive's wither skull with `yield=0`/`incendiary=false` — pure scenery that never detonates — and `PROJECTILE_DRESSING` carries neither knob, while its `invulnerable`/`no-pickup` guards only apply to a LivingEntity rider (a WITHER_SKULL is not one), so the rider keeps vanilla skull behaviour. (b) The single-rider PRIORITY: the jar suppresses Cowification's cow when the bow also carries Explosive, and Explosive makes no reciprocal check; on the frozen surface the last `PROJECTILE_DRESSING` of the shot simply wins. Cowification's `ENTITY_COW_HURT` 1.0/0.7 cue to whoever strikes the cow is the same engine-owned lifecycle and is likewise unauthored | (a) yield/incendiary params on PROJECTILE_DRESSING (the same effect the `enchants/hellfire` + `enchants/infernal` row wants fire ticks on); (b) ~~no longer blocked~~ **AUTHORED as a batch-06 drop-in** — `cowification.yml`'s BOW_FIRE ability carries `condition: "%actor.enchlevel.explosive% == 0"` (the shooter's worn map spans the held hands, so their own bow reports its Explosive level) and `explosive.yml` stays check-free, exactly as measured. `venom.yml` stood its rider down for the same jar reason and could take the same gate, but its file records the collision as engine-owned instead — the two readings want reconciling in one pass. Clause (a) and the `ENTITY_COW_HURT` striker cue remain, both cosmetic (a dressing rider is scenery and is removed the moment the arrow lands), so this is recorded, not a stop | (a) wave 1f/2 |
| `enchants/teleblock` — **STOPPED in wave 3c, not built** | the LAUNCH-time soul charge. The jar bills at the shot and stamps the arrow "this shot was funded", so a missed shot still costs souls; the port bills at IMPACT (`soul-cost` on the BOW ability) — same cost per LANDED shot, same soul-mode + all-or-nothing gate, same silent refusal, but a miss is now free. Everything else ships: the funded launch cues, the D-05-6 as-intended teleblock window, the pearl strip and the verbatim message | a per-projectile payload stamp — a BOW ability resolves off the bow, not off the arrow a BOW_FIRE ability paid for, so no cross-ability "this shot was funded" link exists. Billing at launch instead would leave the impact ability ungated (a free teleblock for a shooter with an empty gem), which is why the charge MOVED rather than being split; a var-armed window leaks the same way on a miss. **WHY IT STOPPED (wave 3c):** the STORE is not the gap — `engine.stores.ProjectileStore` is built, TTL-evicting and unit-tested, and is referenced by nothing in production, so a per-shot stamp already has a home. What is missing is BOTH ends of the wire: an effect that stamps the shot at launch (the projectile handle exists only inside `TriggerDispatch.fireBow`, which is why `PROJECTILE_DRESSING` is a read-back and not an effect that acts) and a fact the landing ability can gate on, plus a ruling on the refund a funded shot that never lands is owed. Three surfaces for a change whose only play delta is that a MISS costs souls — and the shipped direction is the safe one, since no shot can land unfunded | follow-up candidate |
| ~~`enchants/auto-smelt`~~ | ~~two clauses. (a) The Detonate stand-down… (b) The ore ids…~~ |**BOTH CLOSED in wave 3b-3 (R-QC10 + R-QC22b).** (a) was never really a suppression problem: the stand-down and the in-volume smelt are ONE rule seen from two ends, and the end that had no expression was the volume's. `BREAK_BLOCK.smelt` (option (a) of the F2 transform surface) closes it, so the tool that stands down here goes on smelting — through the blast. The refusal is authored as a clause on each ability condition (`%actor.enchlevel.detonate% == 0 && %actor.enchlevel.atomic-detonate% == 0`) rather than `blacklist:`, which is an APPLY-time refusal and would forbid the very Auto Smelt + Fuse + Detonate tool the rule is built on. One looseness recorded in-file: `%actor.enchlevel.*%` spans armour plus BOTH hands, so a Detonate pickaxe in the off hand stands the mined block's smelt down too — reachable only with a deliberate two-pickaxe loadout. (b) closed under the standing era rule R-QC22b: the two gates widen to `DEEPSLATE_IRON_ORE` / `DEEPSLATE_GOLD_ORE` / `NETHER_GOLD_ORE`. They live in STRING comparisons, so a name absent on 1.8 simply never matches and no fallback chain is needed. Copper stays out — a new ore is not a flattening variant of one the jar knew | closed |
| ~~`enchants/fuse`~~ | ~~nothing on the file — its ONLY meaning is the in-volume smelt rule, and both consumers landed WITHOUT it: an in-volume smelt would have to reach blocks the sink breaks itself~~ |**LIVE in wave 3b-3 (R-QC10)** — the transform landed as `BREAK_BLOCK.smelt`, and because the param is a NUMBER rather than a flag the jar's whole co-enchant test collapses into one authored product on both Detonate grades: `min(1, %actor.enchlevel.fuse%) * %actor.enchlevel.auto-smelt%` is 0 without this marker (max 1, so the `min` is belt-and-braces) and the Auto Smelt level with it — `flag && flag1` AND `setAmount(autoSmeltLevel)` in a single expression, with no second ability and no boolean-from-a-condition shape. The file itself still authors nothing; the marker's job is to be worn so that product is non-zero | closed |
| `enchants/haste` | nothing pending on the numbers — the entry ships whole (Haste I/II/III, flat 40 t, no roll, no cooldown). Recorded only so the two accepted widenings are traceable: `INTERACT_LEFT` also fires on a swing at open air (the jar's block-damage hook did not), and the jar's refresh path additionally demanded an iron or diamond pickaxe in hand, which matrix-06 explicitly declines to replicate across the 21-tool set | nothing — accepted per the matrix | closed |
| `enchants/guided-rocket-escape` — **(b) CLOSED in wave 3c (R-QC57); (a) STOPPED** | three clauses, all on the SABOTAGE and world edges. (a) The veto ships as a pre-roll chance subtraction (`chance: "100 - 10 * %sabotage%"`, reading sabotage.yml's own victim-side mark actor-side) — distribution-identical, the shape Trap/Snare use for Metaphysical — so the two halves that shape cannot carry are lost: the jar BURNS the 15 s cooldown on a vetoed escape (a failed roll arms nothing here), and the victim line `§c§l ** §7Guided Rocket Escape:§c§l SABOTAGED **` (leading space verbatim) fires "on a would-be proc" that one already-reduced roll cannot report. (b) The cooldown bucket the matrix records as SHARED with the non-heroic at asymmetric thresholds (15 s read here, 30 s there) is per-enchant in the engine, so each grade paces itself — only observable while both are worn on different pieces. (c) The duels-lowered launch (`y = 2+L` in `world_duels`/`world_duels2`) and the dungeon-parkour region veto are not ported: the first needs the whole ladder duplicated behind another deployment's world names, the second a region-flag surface SE has no analogue for. The `world_koth` refusal DOES ship, as `disabled-worlds:` — inert anywhere that world does not exist. Everything else is authored, including the FLY window on the closed `FLY_SPEED_PARAM` gap | (a) the post-gate "a rebate blocked this proc" hook the three Metaphysical consumers already want, plus a cooldown that arms on a vetoed activation. **WHY IT STOPPED (wave 3c):** both halves need an event the shipped expression cannot produce — a roll that failed BECAUSE of the rebate. A pre-roll subtraction yields ONE roll and no such moment, by construction. R-QC41's consume-time feedback surface would supply the hook, but it attaches to SUPPRESS windows and one-shot charges, so reaching it means re-expressing the veto as a defender-armed `SUPPRESS_INCOMING` — which changes both the distribution and the timing (the attacker's ATTACK walk runs BEFORE the defender's DEFENSE walk, so a defensively armed window parks the veto a hit late, for a fixed span, against every other victim too). That is the exact objection `polymorphic-metaphysical.yml` already records against SUPPRESS as a Metaphysical stand-in, and it is the same stop; (b) ~~a cooldown scope shared ACROSS two enchant keys~~ **CLOSED (R-QC57)** — `cooldown-scope:` takes a NAME as well as `none`, so both grades' launch abilities point at `rocket-escape` and pace each other while keeping their own 300 t / 600 t thresholds (a shared bucket paces, it does not align); (c) deployment topology, an operator edit rather than an engine gap | (a) with the Metaphysical feedback hook; (b) closed |
| ~~`enchants/infinite-luck`~~ **CLOSED (R-QC9)** | ~~the CONSUMER, which is the whole point of the entry. This file is authored exactly as the matrix decomposes it — a PASSIVE roster marker with zero local logic, effect-less rather than the recorded `SET_VAR(..., ttl=0)` for the Solitude reason (a ttl-0 marker written on a PASSIVE walk never drops), publishing its level as worn state for `%actor.enchlevel.infinite-luck%`. What has no home yet is matrix 10's threshold check: satisfied when the stored level ≥ N, over an accumulator each worn LEATHER heroic-armor piece raises by +12.5 (four pieces = 50.0; iron/diamond heroic pieces contribute 0), which the codex leaves UNRESOLVED past that point. Consequence worth stating plainly: `removes-required` strips Lucky as this lands, so until the consumer ships an upgraded item trades a working `level/400` death save for an inert marker — the jar's own arrangement, recorded rather than softened | ONE thing now, not two: the matrix-10 semantics (owner/matrix ruling on the UNRESOLVED accumulator). **The piece half is UNBLOCKED** — `%victim.heroicpieces%` shipped in wave 2c, the identical fact `enchants/hero-killer` was stopped on. Note the accumulator this entry records is WEIGHTED (each LEATHER heroic piece +12.5, iron/diamond 0) while the fact is an unweighted count, so the two only coincide for an all-leather set; whether the material weighting survives the port is part of the same owner ruling. **NARROWED in the wave-2c drain, and the piece half did NOT in fact close:** `%victim.heroicpieces%` is VICTIM-scope only, while this consumer counts the WEARER'S OWN pieces — on a defence pass that name reads the ATTACKER, so there is no scope on which it answered at drain time. The ACTOR-SIDE twin it called for has SINCE shipped (`%actor.heroicpieces%`, wave 2d, the same flattened-WornState no-entity-read rule), and as of wave 2e the count reads REAL minted-heroic pieces — so the fact half is closed and the three M-Kit sets whose pieces mint heroic are all LEATHER, which is precisely the case where weighted and unweighted agree. Nothing on THIS file moves either way (the marker publishes the level; the arithmetic is the set's), so it stays authored as-is and both remaining items are the consumer's, now recorded in-file so whoever writes it does not re-derive them~~ **BUILT in QC wave 3c (R-QC9).** The file is no longer inert: it publishes its level for the sets to threshold against AND carries its own death save, `chance: 12.5 x %actor.heroicpieces% - 12.5 x %victim.heroicpieces%` on the family's `%posthit.health% <= 0` gate with `CANCEL`. The UNRESOLVED accumulator is READ, and the reading is recorded in-file: the codex carries two independent numbers on this enchant and the port had conflated them — `stored >= N` is what the SETS check (each names its own N over the published level) and `+12.5 per heroic piece` is the SAVE's chance, reaching exactly 50 % at a full heroic set. D-10-1's correction rides the same expression as its second term, so the negation is one roll and not two. The leather weighting is ledgered as D-06-20, not built (R-QC9 rules the count unweighted), and it is unreachable on shipped content — every heroic-minting M-Kit piece is leather | ~~batch 10 ruling (the UNRESOLVED accumulator)~~ **closed by R-QC9** |
| ~~`enchants/arrow-deflect` + `enchants/lethal-sniper`~~ | ~~the Lethal Sniper bypass: a Lethal Sniper arrow beats the victim's Arrow Deflect at `0.1 × its level` (10–50 %)~~ **AUTHORED in batch 7** on `arrow-deflect.yml`, where the matrix puts it: `chance: "100 - 10 * %victim.enchlevel.lethal-sniper%"` on the deflect block at every level — the pre-roll subtraction Trap/Snare use, distribution-identical to a separate bypass roll, and the [0,100] clamp is the bound. The subject checks out: a projectile hit resolves the damager to the SHOOTER, so the DEFENSE pass's "victim" is the archer and the worn map spans their held bow; an ordinary archer reads 0 and the block stays at 100. A bypassed arrow does not consume the arming flag, so the guard is still up for the next one inside its window | nothing — the wave-1f enchlevel facts were the whole dependency, and the row was already marked a drop-in | closed |
| every heroic file (batch 06) + its non-heroic base | two halves of the shared heroic APPLICATION CONTRACT (matrix 06 § Heroic application rules). Each heroic ships `requires: ["enchants/<base>"]` + `removes-required: true`, which carries the APPLY-GATE, the REPLACE (the base's line is deleted, so the two never both run) and the SLOT-EXEMPT rule (`freedBy` makes the upgrade a net zero slots). What is NOT carried: (a) the gate's "at the partner's MAX level" gradient — `ItemEnchanter.checkRelationships` tests the prereq at `>= the level being applied`, so Deep Bleed II wants Bleed II rather than Bleed VI, and a partly-levelled base can be upgraded early; (b) REVERSE-BLOCK ("a non-heroic can never be applied over its heroic"), which `blacklist:` cannot carry from EITHER side. `ItemEnchanter.checkRelationships` tests it BIDIRECTIONALLY (`def.blacklist().contains(other) \|\| otherDef.blacklist().contains(def.key())`), so a `blacklist` on the base file fires just as surely on the heroic's own apply — where the base is the line `requires` demands be present — and refuses it. The two clauses are mutually exclusive on one pair of keys, and `requires` wins because it carries three contract rules to REVERSE-BLOCK's one. Consequence, and the reason not to "fix" it casually: re-applying the base over its heroic succeeds and both lines run — but that is also the ONLY way to climb a heroic's own ladder, since `removes-required` eats the prerequisite on every apply, so a hard REVERSE-BLOCK would freeze every heroic at the level it was first applied at. THIRD contract rule, settled by owner ruling rather than pending: **no heroic may carry `suppress-immune`.** END-SUPPRESS ("all tier > 5 enchants are inert while the holder is in The End", a SUPPRESS-scope world rule) has to be able to reach every tier-7 file, and `suppress-immune` exempts an ability from ALL suppression outright (`SuppressionStore.suppressesAny` returns false before it reads a single scope) — so the flag and the contract cannot both hold. It outranks the maintained-passive convention batches 1–2 apply to worn windows: `alien-implants.yml` (hunger lock) and `godly-overload.yml` (permanent HEALTH_BOOST) both shipped the flag and both had it removed, accepting that a silence window now lapses the lock and drops the buff where their non-heroic bases keep theirs. Any future heroic authored off a base that carries `suppress-immune` drops it at the grade seam | (a) a `requires-max` (or `requires-level: max`) flag on the prereq, since the reader has no notion of "the prereq's own ceiling"; (b) an apply-time rule that exempts the pair it names from the bidirectional blacklist test (or an upgrade-aware `requires` that accepts "the prereq OR this enchant already present") — an engine change, not a base-file sweep | (a) + (b) wave 1f/2 |
| ~~`enchants/vengeful-diminish` + `enchants/diminish`~~ CLOSED (R-QC19, ADR-0075) | The cap BASIS was one hit stale: `DAMAGE_CAP` fixed its value at ARM time from `DamageCapStore.lastTaken`, which `CombatDispatch` wrote only AFTER the defence walk folded and committed — so a cap armed on hit N priced off committed(N−1). The consume-order half shipped earlier (`consumeArmed` hoisted above BOTH walks, so a window survives its arming hit and `duration:` is live code); the basis half ships now. The arm records a PENDING price and `DamageCapStore.price(...)` resolves it at the fold commit against THIS event's committed value, so "half the hit that armed it" is literally true and Vengeful Diminish's measured double halving is exact rather than approximate. The `feedback` line moved to the commit with it — the first point where `{damage}` has a true figure — and `lastTaken` is gone, leaving one basis instead of two. The wave-2c review's objections (2)/(3) do not apply to this shape: the price is read BELOW every walk, off the committed figure the event already carries, so nothing needs an in-flight partial fold and no new §3.5 boundary surface. The recorded ARMED-STATE SHARING still needs nothing: `DamageCapStore` is keyed per PLAYER with one armed cap and re-arm replaces it, reproducing the jar's shared key exactly (matrix line still owed — it predicted per-enchant state and called that a structural change) | nothing — the pinned test flipped to `theArmedCapIsPricedOffTheArmingHit` | closed |
| ~~`enchants/titan-trap` + the Dragon Slayer set~~ | the heroic's MEASURED exemptions are copied, not fixed: unlike its counterpart it does not fire the proc-veto event and ignores the set's `immune_freeze`. So the set-side rule (`dragon-slayer-blocks-freeze`, with `§8§l* DRAGON SLAYER [§7Trap blocked!§8§l] *`) had to be keyed per ENCHANT — keying it to the freeze family would silently align the pair and lose the recorded posture. **HONOURED by the armour-set batch:** `sets/dragon-slayer.yml` keys the rule to `enchants/trap`, `enchants/ice-aspect` and `enchants/horrify`, and deliberately never `enchants/titan-trap`; both grades' files name the constraint. **R-QC29/32/33 CONFIRMS the asymmetry stays** — the owner declined the offered alignment | nothing — the constraint was a keying rule, and the set that had to honour it does | closed |
| `enchants/discombobulate` (batch 07) | nothing on the numbers — the file is authored exactly as the matrix decomposes it: an effect-less PASSIVE roster marker on ARMOR, max 1, publishing its level as worn state for `%actor.enchlevel.discombobulate%` instead of the recorded `SET_VAR(name=mastery.discombobulate, value=1, ttl=0)`, for the Solitude reason a ttl-0 marker written on a PASSIVE walk has no teardown. What is pending is the CONSUMER, and it is not in the port: the codex leaves it UNRESOLVED and places it in an external plugin, so no reader exists in this pack or in any batch still to come — the entry itself says the flag "is ported for parity only and gates nothing". Recorded as a PARTIAL rather than a stop for lava-strider's reason: the matrix decomposes a publisher here, where `enchants/ghost` (stopped above on the same class of question) is decomposed as literally "none (inert in corpus)" | nothing engine-side — an external consumer the port does not own | n/a — external consumer |
| ~~`enchants/explosives-expert`~~ | ~~nothing on the file — its ONLY meaning is one clause on `heroic/atomic-detonate`, and that entry landed with no tool-class gating at all, so the flag had nothing left to unlock~~ |**LIVE in wave 3b-3 (R-QC10)** — `atomic-detonate.yml` now authors the sublists (the jar's 15 pickaxe materials and 6 spade materials, ore spellings widened under R-QC22b) as FOUR mutually exclusive arms per rung, because a selector body is a literal and the deny list a rung needs depends on what the miner holds: expert / pickaxe / spade / neither. This marker's arm is the one that denies neither sublist. Both authoring facts the row carried held: the gate reads `%actor.enchlevel.explosives-expert%` and not the matrix's `%mastery.explosives-expert%` var spelling, and plain Detonate keeps ignoring the flag — it does not author the sublists either (nine rungs would take 36 arms for a rider the deny list already bounds), which is recorded on both files rather than approximated | closed |
| ~~`enchants/lava-strider` (batch 07)~~ | ~~nothing on the numbers — the file is authored exactly as the matrix decomposes it: an effect-less PASSIVE roster marker on BOOTS (the mastery family's only slot restriction), max 1, publishing its level as worn state for `%actor.enchlevel.lava-strider%` instead of the recorded `SET_VAR(name=mastery.lava-strider, value=1, ttl=0)`, for the Solitude reason a ttl-0 marker written on a PASSIVE walk has no teardown. What is pending is the CONSUMER, and it is not in the port: the codex leaves it UNRESOLVED and places it in an external plugin, presumably lava traversal, so no reader exists in this pack or in any batch still to come. The matrix's own `WALKER(material=…, replace=…)` suggestion is design rather than translation and the entry says as much — "pending an owner ruling; the matrix records the measured flag only" — so it is deliberately NOT authored~~ **CLOSED BY OWNER RULING (R-QC38)** — the consumer is never coming, so the entry ships the behaviour the matrix's own note names instead of a marker for it: a REPEATING `WALKER` lava-walk (OBSIDIAN, 60 t, radius 2, `replace: REPLACEABLE`) behind a `%actor.groundblock% contains "LAVA"` gate. Every number in it is a port choice and the file says so; the worn-state publication stays, so a future reader still finds the level | ~~an owner ruling: realize the strider as a `WALKER` lava field, keep the inert marker for parity/economy, or drop the entry. Recorded as a PARTIAL rather than a stop because the matrix decomposes a publisher here, where `enchants/ghost` (stopped above on the same class of ruling) is decomposed as literally "none (inert in corpus)" — the two are one owner call apart, and if it lands the other way this is one file deletion and a moved row~~ | closed |
| ~~`enchants/permafrost` (batch 07)~~ | ~~the standing-on-your-own-frost half: the matrix's SECOND `DEFENSE` ability, `DAMAGE_MOD(side=defense, mode=add, amount=14+L)` (15–20 % off the hit, ×0.85…×0.80) plus `POTION(SLOW_DIGGING, level=2, duration=50, who=@Attacker)` re-applied per hit, behind `%actor.groundblock% == "SNOW_BLOCK" && %mastery.ownedground% == 1` and the ENTITY_ATTACK/PROJECTILE cause filter. The freeze field itself ships WHOLE: two `TEMP_BLOCK` FOOTPRINT stamps at `dy` −1 and −2 with `airOnly: false` (replace-mode solid-only, which IS the codex's freeze filter), radius `min(2+L, 5)` = 3/4/5/5/5/5, ticks 100/100/100/120/120/120, the `2 + L/3` % true-double chance ladder, the `BLOCK_GLASS_BREAK` 3.0/1.1 cue and the verbatim `§c§l* PERMAFROST [§7{seconds}s§c§l] *`. The entry's OTHER recorded gap, `TEMP_BLOCK_EXTENT`, is CLOSED — FOOTPRINT's radius cap is 5 today, so L3+'s 11×11 field authors exactly. Two accepted widenings ride the shipped half, both recorded in-file: the jar froze only inside Factions warzone claims (the engine has no zone-claim concept, so this freezes anywhere — the spec's faction/zone ruling) and the cues fire on every proc where the jar played them only when ≥ 1 block actually froze (no fact reports a stamp's block count)~~ **CLOSED (R-QC6)** — `%actor.ownedground%` shipped, so the second DEFENSE ability is authored at every rung: `DAMAGE_MOD(side=defense, mode=add, amount=14+L)` = 15…20 plus `POTION(SLOW_DIGGING, level=2, duration=50, who=@Attacker)`, gated on `%actor.ownedground% && %actor.groundblock% == "SNOW_BLOCK"` and the ENTITY_ATTACK/PROJECTILE cause filter. Both gate halves are load-bearing — ownership alone would rebate any temp block the wearer laid, and material alone would rebate a natural snowy biome or the attacker standing on the wearer's own field. The one residual is the amount's BASIS: the jar keyed it to the level stored on the block, the pack keys it to the worn level, and the two differ only for a wearer who changes grade inside one 5-6 s field | ~~`OWNED_GROUND` — "the block the actor stands on is a temp block THIS actor placed" (a TempBlockLedger owner lookup), shared with `enchants/rot-and-decay` in the same batch. `%actor.groundblock%` alone is not a substitute and shipping it alone would INVERT the feature: keyed on the material, the rebate and the attacker's Mining Fatigue would land for anyone standing on ANY snow — a snowy biome, a teammate's field, or the attacker standing on the very frost this proc just laid under the wearer, where the matrix keys the reduction to the block's OWNER~~ | closed |
| `enchants/chain-lifesteal` (batch 07) | three recorded clauses, none of which stops the file — the chain, the ladder (chance 5·L %, radius `1+⌊L/2⌋`, D-07-3's as-intended cap `1+⌈L/2⌉` = 2/2/3/3/4) and the per-target block-crack cue all ship. (a) The per-target RE-ROLL: `rand(0.5, 0.5+L)` is the recorded roll and the range half of `RANDOM_AMOUNT` is closed by the wave-1 expression params, but an effect's numeric params are evaluated ONCE per activation, so every target in one chain takes the SAME drain where the jar rolled per target. (b) The drain is ATTRIBUTED damage, not the jar's raw health write: the engine has one damage path (ADR-0054), so armour and protection price it, and the primary victim's slot rides the triggering hit's own fold rather than a second hurt. `IGNORE_ARMOR` is deliberately not authored — it flags the whole triggering swing, not the drain. (c) `LETHAL_CANCEL` is DROPPED by this batch, which proposed-primitives.md § Provisional explicitly hands to doc 07: the measured "a chain kill cancels the source hit" has nothing left to cancel in the case that matters (the victim's drain IS the source hit under the fold), and voiding a landed melee because a bystander died to the chain reads as a bug in play rather than a felt rule | (a) per-target evaluation of a numeric param on a multi-target effect — an expression-engine change, not a new kind; (b) + (c) nothing: both are rulings, and (c)'s deviation-ledger row is MINTED as **D-07-16** (felt-unit review), which is what proposed-primitives requires of a drop | (a) a later expression wave; (b) + (c) closed by ruling |
| `enchants/auto-sell` (batch 07) | the WHOLE behaviour, by owner ruling R9 (economy note-only): the file ships INERT — verbatim identity, the verified `MINE` hook, the mastery presentation, an empty level body and the authored R9 note. The jar's mechanics are recorded in the file rather than authored (`BREAK_BLOCK(drops=false)` at the mined block, then credit of the FIRST drop's unit price out of the shop's price table), and D-07-2 (payout ignores stack size and further drops; as-intended Σ(price × amount)) is annotated, not implemented. The tool-side ordering rules are moot while it is inert — auto-smelt.yml already records the same stand-down from its side, and both Detonate entries have since landed (wave-2c drain) with their own R9 note in place of the payout | nothing — `SHOP_SELL` is WITHDRAWN (proposed-primitives.md § Excluded by owner ruling R9), and `MODIFY_MONEY` cannot substitute: the price table IS the behaviour. It reopens only if the owner reverses R9 and the engine takes an economy dependency | closed by ruling |
| `enchants/soul-siphon` (batch 07) — **STOPPED in wave 3c, not built** | the zero-souls DURABILITY branch's three unexpressible sub-clauses. The branch itself ships (`percent-damage` 2.5 on a `random-piece` of the victim, `percent-restore` 0.8333 — the recorded 1/3 transfer ratio on the same basis — on the attacker), and so does everything else on the entry: the single roll split across two complementary `%victim.souls%` gates, the unconditional `2+2L` heal on both, `SOUL_TRANSFER{cap: ceil(25·L), ratio: 0.5, overflow: mint}` and both verbatim soul lines with their `{souls}` expressions. Missing: (a) the SLOT COUPLING — the jar repairs the attacker's piece matching the slot it drained, and nothing carries a pick from one `DURABILITY` row to another, so the repair takes its own `random-piece`; D-07-14's as-intended "repair the attacker's FIRST worn slot" has no `select` rung either (the enum offers named slots, most/least-damaged and random). (b) The `max(1, …)` floor under the repair and the `+0.02 x L` flat term under the drain — a percent mode reads `percent` alone; the flat term is a measured no-op at every vanilla max, the floor bites only where the rounded product falls under half a point (a leather helmet, max 55, repairs 0). (c) The two durability STRINGS, `§c§l* SOUL SIPHON [§7-{durability} Durability§c§l]` and `§a§l* SOUL SIPHON [§7+{durability} Durability§a§l]`, whose entire content is a point count that depends on which piece the random pick took — no fact reports it, and a wrong number reads worse than silence, so the branch ships without feedback. Separately recorded, not a stop: the engine ROUNDS the percent product where the jar took a ceil, so drain and repair can each sit one point low (9/13/12/11 vs 10/14/13/11 on diamond; 3/4/4/4 vs 3/4/4/3) | a slot-carrying transfer on `DURABILITY` (`transfer-ratio` / `transfer-min` + a `matching` and a `first-worn` rung on `select`), and a moved-durability fact for the strings. **WHY IT STOPPED (wave 3c):** two engine surfaces, neither a leftover. The `matching` rung needs a slot carried from ONE effect row to another inside one activation, and the effect surface has no scratch between rows at all — `ArmorSelect.pick` already returns the index it chose, so the information exists and simply has nowhere to live. The strings need the four sink durability writes (all `void`) to report the points they moved AND a fact slot filled AFTER an effect ran, where every fact today is filled once when the activation is built — the ordering `%victim.souls%` depends on, and which this file already records | follow-up candidate |
| ~~`enchants/demonic-gateway` + `enchants/rot-and-decay` (batch 07)~~ | ~~both payload bites land ARMOUR-REDUCED where the jar's were raw health writes — Demonic Gateway's 5 %-of-max skull hit and Rot and Decay's 10 %-of-max corpse strike~~ | **CLOSED in wave 3b-1** — the flag now rides the hurt. A payload walk has no folded combat event to hand `IGNORE_ARMOR` to, so the sink stamps its `armorIgnored` read-back onto the attributed hurt `DAMAGE` issues (`EngineDamage.hurt(…, ignoreArmor)`) and `CombatDispatch` spends it on THAT event — the same two ARMOR/MAGIC modifiers the folded path zeroes, because the reduction is the server's either way. The flag is read at EXECUTION, not emit, so authoring order cannot drop it. Neither file needed an edit; both notes now read LIVE. Two honest scopes: an ownerless engine hurt is priced normally (no `EntityDamageByEntityEvent`, and no author behind it), and a hit PARKED by the combo ledger (ADR-0069) rejoins its victim's next real swing through the fold, where it is armour-priced — the bank carries no per-entry flag | closed |
| `enchants/rot-and-decay` (batch 07) | ~~the field's DAMAGE half — `STACKING_DOT`'s ladder never starts — and~~ the durability line `§c§l* DECAYED [§7- {durability} Durability§c§l] *`. Everything else in the entry ships: the 15-block `SUMMON_PURGE`, the `{OWNER}`-named corpses at the recorded speed multipliers, `payload-phase: strike` and both halves of the 50/50 (the armour-bypassing 10 %-of-max bite with its ROTTED line, and the boots-first `percent-damage: 2.5` with its zombie-metal cue), and the per-viewer glowstone / end-stone overlay at `3+⌊L/3⌋` for `20·L` t | ~~TWO~~ ONE remaining thing. (a) **CLOSED in wave 3b-1** — `STACKING_DOT` tested ground ownership through `TempBlockLedger.ownerAt` alone, i.e. a block the ACTOR placed, and `PHANTOM_BLOCKS` is packets only, so the pulse re-read the ground, found no owner, and neither stacked nor damaged: two primitives shipped in one wave against different readings of what a field is. The overlay now CLAIMS its patch in a packet-layer registry (`PhantomFields`: positions + owner + deadline, released with the window and swept if its region task is dropped), and BOTH consumers — `%actor.ownedground%` and the DoT's per-pulse test — read one composed lookup over the real-block ledger and that registry, so a fact and a DoT can never disagree about standing in a field again. Still nothing is written to the world. (b) `{durability}` is the number of points the hit actually took, no fact reports a piece's max durability, and this pack ships no unbound `{token}` lines (R-QC2) — it wants a taken-amount token on `DURABILITY`, the same feedback family R-QC41 built for one-shot charges | follow-up candidate |
| `enchants/horrify` (batch 07) | the wearer's summary `§a§l* HORRIFIED {count} player(s) [§7{seconds}s§a§l] *`. **CLOSED (R-QC66, ADR-0076)** on `%selected%`, the count the effect above it bound after every per-target filter — this 32-block radius and this ENEMIES filter, where `%nearbyenemies%`'s fixed 8-block living-entity scan would have printed a wrong number at every level. `{seconds}` is inlined per rung like the target line's. `enchants/tombstone` closes on the same fact | nothing | closed |
| `enchants/feign-death` (batch 07) | the DECOY corpse, and the UNVANISHED line's timing | the corpse is R-QC30's roster marker — a packet-seam decision (`PlayerRender`), deliberately not part of this pass; `VANISH` omits `decoy`/`decoy-ttl` rather than accepting and ignoring them, so this file needs no edit when it lands. The line is authored as a `wait:`-delayed MESSAGE at the nominal window, which prints late when the vanish broke early on the L-th outgoing hit; R-QC30 also approves an end-of-window feedback hook on `VANISH`, and the line moves onto it when that is built | R-QC30 (marker) / the VANISH end hook |
| `enchants/tombstone` (batch 07) | ~~the wearer's summary line `§c§l* TOMBSTONE [§7{count} players§c§l] *`~~ **AUTHORED (R-QC66, ADR-0076)** on `%selected%`, the post-filter count of the row above it — this radius (`6+L` = 7–16), this ENEMIES filter and this nearest-first cap (`4+⌊L/2⌋`), where `%nearbyenemies%`'s fixed 8-block living-entity scan would have printed a wrong number at every level. It is still sent unconditionally, `[0 players]` included (measured), and the wearer keeps the recorded 4.0-volume anvil clang as the proc cue. Everything else ships: the `0.02·(L/3)` true-double chance ladder, the AoE freeze + the amp-128 SLOW rung + `SUPPRESS(scope=TYPE, key=DEFENSE, 80 t)` on the SAME channel `silence.yml` arms, the `ENCHANTMENT_TABLE`/`PORTAL` bursts with their `wait: 80` expiry repeat, the radius-0 `FALLING_BLOCK(ANVIL)` and its `IMPACT` payload (`percent-damage` 10 on the `whole-set`, both zombie-door cues, the verbatim anvil line), and the per-target line with its always-`4` `{seconds}` inlined. D-07-15 needs nothing: the engine's `Math.round` on the percent product already delivers the intended minimum of 1 for every max at or above 5. What does NOT ship is the amp-128 JUMP rung, on either lane, by the pack-wide `titan-trap.yml` ruling: the jar's Jump-amplifier-128 no-jump is a 1.8-only integer overflow, and on the modern lane that amplifier is a real and enormous jump that would fling the very targets the proc exists to pin (the same ruling already keeps `enchants/horrify` from shipping its own copy). FREEZE is the root that replaces it — the matrix decomposes freeze and the potion pair as one step — so the entry loses only the jump half of the hold, and a jump knob on FREEZE stays owed engine-side. The SLOW rung is kept: amplifier 128 is a full movement stop on BOTH eras (the modern multiplier floors at 0 rather than wrapping), so it carries no overflow hazard and it is what the jar's freeze-restore stripped while leaving JUMP running. Two rows carried elsewhere: the `§c§l* SILENCE BLOCKED [§7{attacker}§c§l] *` line belongs to the immunity side (`SUPPRESS_IMMUNE` has no feedback hook, and `silence.yml` records the same string), and the `scope=TYPE` engine seam that `silence.yml` reported is CLOSED — R-QC3 (ADR-0075) stamps every ability with the TYPE its trigger's combat direction implies, so these windows silence each caught target's whole defender side. Era: `ZOMBIE_WOODBREAK` → `ENTITY_ZOMBIE_BREAK_WOODEN_DOOR` and `ZOMBIE_WOOD` → `ENTITY_ZOMBIE_ATTACK_WOODEN_DOOR` have no alias rows yet, so both cues warn-skip on the legacy fork (parked on the legacy-sweep list); the falling ANVIL's block-state data is the usual 1.8 overlay hazard | the two zombie-door sound aliases for the legacy lane | era row only |
| `enchants/nature-wrath` (batch 02, re-opened in batch 7) | the SUBJECT of the Poltergeist freeze immunity. The branch itself is now authored here by owner ruling — a rebate belongs to the roll it rebates, so `poltergeist.yml` publishes the level and the consumer rolls it — as two mutually exclusive abilities per rung whose chance expressions sum to the recorded figure (`0.4 - 0.05 x PG` rooting, `0.05 x PG` immune, and the same 12.5 %-a-level split at every level), which is D-07-13's uniform roll replacing D-02-2's always-true test and leaves the advertised proc rate untouched. **THE PER-TARGET READING IS NOW AUTHORED (R-QC66, ADR-0076)**, and with it both consequences the two-ability split forced. One ability per rung carries the package at its plain advertised chance; the FREEZE row and the immune line are the two arms of ONE draw per bystander (`each-if` over `%target.roll%` and `%target.enchlevel.poltergeist%`), so they partition per BODY where the split could only adjudicate the DEFENSE pass's `%victim%` and spared or rooted the whole scan together. The DoT consequence closes too: the codex records the jar's task running on an immune player for the full window, so the immune arm authors the same activator-attributed burn on `PERIODIC_DAMAGE` at the same rate and window. Residue, in-file: `PERIODIC_DAMAGE` stacks where `FREEZE` refreshes, so two procs on one immune body inside a window burn twice; and ENDER_DRAGON_GROWL, recorded per FROZEN victim, plays at the activation point so it still sounds on the rare proc where every bystander is immune. The blocked line ships verbatim with `Nature's Wrath` inlined, `{source}` having no token binding | nothing | closed |
| ~~the two doc-10 Poltergeist consumers (`armor-sets/dimensional-traveler`, `armor-sets/mother-of-yijki`)~~ | ~~the same immunity branch `nature-wrath.yml` now carries, owed on THEIR entries by the same ruling and at the same D-07-13 uniform `12.5 x L` % (measured 12/25/37 % and 11.5/24/36.5 % respectively, both bugs), each printing the verbatim line with its own name inlined. Recorded here because the enforcement lives in a batch that has not been authored yet — this pack ships no armour sets at all today~~ **SETTLED in batch 08 — the claim that this pack ships no armour sets is STALE: all twelve `content/sets/` files are authored.** The two consumers split, and the split is the interesting half. `sets/dimensional-traveler.yml` AUTHORS the branch on the `nature-wrath.yml` idiom — two mutually exclusive abilities per rung whose chance expressions sum to the recorded 1 %, the immune share a uniform `0.125` pp per Poltergeist level (D-07-13), the blocked line `§4§l* POLTERGEIST [§7Immune: Dimensional Traveler§4§l] *` with its own name inlined because `{source}` has no token binding. A victim wearing no Poltergeist reads a real 0, so the immune rung collapses to `chance 0` and the ordinary case pays nothing for the split. `sets/mother-of-yijki.yml` CANNOT take that shape and records the rule instead: its rebate sits PER STRIKE POINT inside `DELAYED_STRIKE_FIELD`, where an ability-level chance split has nothing to divide — the field is one activation that resolves sixteen independent detonations, and the immunity is a property of each one. **DIMENSIONAL TRAVELER'S SUBJECT GAP CLOSES in the per-target wave (R-QC66, ADR-0076):** the shift keeps its plain 1 % roll and the FREEZE row and the immune line become the two arms of one draw per body, so an immune body is dropped from the root alone instead of sparing the whole scan. Its crystal twin takes the same pair in the same sitting — the rebate could not travel while it was an ability-level chance split (0.025 pp of a 0.2 % roll, and a second message rung that double-printed for anyone wearing both), and per-target it is neither. The jar's 8 000 ms per-victim immunity window still has no expression; an immune body is simply dropped from this activation's root. MOTHER OF YIJKI is unchanged — its rebate sits per STRIKE POINT inside `DELAYED_STRIKE_FIELD`, which the wave explicitly re-points at the deferred-payload fact surface | ~~nothing engine-side beyond the subject gap the row above names~~ Dimensional Traveler is authored and needs nothing further; Mother of Yijki needs a PER-TARGET (per-strike-point) chance inside `DELAYED_STRIKE_FIELD` — the same per-target subject gap, one layer deeper | ~~with the armour-set batch~~ **DT closed (batch 08)**; Yijki waits on the per-target subject fact |
| `crystals/mother-of-yijki`, `crystals/dimensional-traveler` (batch 08) | the PROC-ROLL half of each crystal — the Revenge of Yijki roll at `0.010000000000000002` per crystal-bearing piece (the exact FP literal the jar computes as `0.05 x 0.2`, advertised to players as "20%") and the Dimensional Shift roll at `0.002 x pieces` (0.2 % a piece measured, also advertised as "20%"). Both rolls are authorable today and so is each payload: `DELAYED_STRIKE_FIELD` and the extended `FALLING_BLOCK` profile both shipped in wave 2, and the matrix parameterises both to the last cue. What is missing is the OTHER copy: each payload belongs to an armour set that has not been authored yet, and a crystal file cannot include one — it would carry its own 16 strike points, its own warning line, its own layer/density/re-hit profile, written from a second reading of the same matrix rows. Two copies written weeks apart drift; two copies written together do not. The damage halves (-5 % defence a piece; +7.5 % attack a piece) ship now and are unaffected. The crystal lore lines advertising "20%" are held back with the rolls they describe — a string never ships ahead of the behaviour it names | ~~nothing engine-side — a cross-batch authoring constraint: author each rider in the same sitting as its set~~ **CLOSED in 2e.2**: both sets carry their payloads, so both riders were copied from them in one sitting, one reading. Each ships at its ONE-piece rate (`chance: 0.010000000000000002` and `chance: 0.2`) because a crystal ability fires once per crystal-bearing piece, which is the same scaling the damage halves already use, and the two "20%" lore lines ship with the rolls they name. TWO CLAUSES OF THE DT SET'S READING DELIBERATELY DID NOT TRAVEL, recorded on the crystal: the Poltergeist rebate (0.025 pp of a 0.2 % roll, and its immune MESSAGE rung would double-print for anyone wearing the set AND a crystal), and the `IMPACT` landing payload — a landing block fires the ACTOR's `IMPACT` abilities and the SET owns the only one, so a crystal-only wearer rains blocks that land for nothing, while a second rung here would double-bill every landing for a wearer who has both. That second one closes with the group-scoped `IMPACT` identity in the row below | **CLOSED — wave 2e.2** |
| `crystals/ranger`, `crystals/dragon-slayer` (batch 08) | PER-PIECE SCALING of the two chance-valued crystal riders: Ranger's 20 % teleblock immunity and Dragon Slayer's 10 % cancel of an incoming Silence or Ice Aspect. Both shipped at their ONE-piece value — right for one crystal, short for two to four (Dragon Slayer's four-piece 40 % read 10 %). The damage halves always scaled exactly, that being what the additive fold does with one `DAMAGE_MOD` per worn crystal; the suppression halves could not, a window being keyed per (holder, scope, key) so a second crystal RE-ARMED the first instead of compounding it, with no fact for a `chance:` expression to read | ~~a worn-crystal-count fact (`%actor.crystals.<key>%` or equivalent) for a scaled `chance:`, or additive stacking on a suppression window~~ **CLOSED by R-QC52 (wave 4a).** `%actor.crystals.<key>%` / `%victim.crystals.<key>%` ship (key = the crystal's file stem), and `SuppressionStore.strongerDefender` resolves two arms of one (holder, scope, key) to the STRONGER chance over the later expiry. Both files author a four-rung ladder of REPEATING abilities gated `>= 1..4`; at n rungs all n arm and the highest governs, which is linear — retiring this row's own "four staggered abilities would be four windows on one key and still not linear" objection. Ranger authors 20/40/60/80 % (codex 05 § Teleblock, `Math.random() < 0.2 * pieces`) with its verbatim block line on every rung; Dragon Slayer 10/20/30/40 % (codex 10 § Dragon Slayer crystal hooks). Recorded with it: D-10-4's premise was wrong — the Ranger roll IS implemented, in the Enchants plugin's `TeleblockListener`, which the armour-sets codex does not read | **CLOSED — wave 4a (R-QC52)** |
| `crystals/anti-m-kit-ghost`, `crystals/anti-m-kit-necromancer` (batch 08) | six of their eight pool keys point at enchants this pack has not authored yet — Horrify, Feign Death, Mark of the Beast (Ghost) and Mortal Coil, Rot and Decay, Demonic Gateway (Necromancer), all six sitting in the batch-07 rows above. The keys ARE authored: a `SUPPRESS_INCOMING` key is an interned string, so a key naming an unshipped enchant is inert and costs nothing, and each crystal completes itself the day its pool lands without being edited. Until then a Ghost crystal cancels Poltergeist alone and a Necromancer crystal Soul Siphon alone. `crystals/anti-m-kit-death-knight` has no such gap — all four of its pool members are authored | the six batch-07 enchant rows above; nothing of its own | with those enchants |
| `items/crystal.yml` (batch 08) | three clauses of the family-wide apply contract (`matrix/10` §A.7/§C.1). (a) The APPLY GAMBLE: the jar consumes a crystal BEFORE rolling a default 20 % apply chance; this engine applies with no roll at all (ADR-0034), and the likeness carries no success/failure pair to hold the gamble — this is a deliberate project-wide gesture rule, so the row is a record, not a request. (b) The REFUSAL RULES: refused on a SET piece outright, and on gear carrying a mastery enchant unless it is one of the three whitelisted (Discombobulate, Explosives Expert, Lava Strider) — an M-Kit piece with no blocked mastery enchant does accept one (§A.15). `applies-to` names the item KINDS a crystal fits, which is a different question, and no gear state expresses "refuses a crystal". Shipped as written: any ARMOR piece takes one. (c) The SECOND LIKENESS: Anti-M-Kit Crystals are their own item in the jar and here read "Armor Crystal (Anti-M-Kit (Ghost))" off the single template ADR-0034 allows per pack | a per-crystal apply-chance + refusal predicate on the crystal likeness (or the owner's confirmation that ADR-0034's no-roll apply stands); a second crystal likeness keyed by crystal | follow-up candidate |
| `items/heroic.yml` (batch 08) | four clauses of the heroic-armour contract (`matrix/10` §C.3–C.5). (a) PER-SLOT PERCENTS: the reduction is chest 10 %, legs 8 %, boots and helm 4.5 % each, and the knob is ONE uniform per-piece percent. `0.0675` is authored, which preserves the felt unit that decides fights — the full set, 27 %, exactly — and costs the asymmetry of a partial set (a lone chestplate reads 6.75 % where the jar reads 10 %, a lone helmet 6.75 % where it reads 4.5 %). (b) The M-KIT TIER: ability-set pieces are heroic at a second, steeper grade — 16/13/8/8, full set 45 %, with armour points HALVED to 2/4/3/2 against KOTH's 3/8/6/3 — and one `type: heroic` config exists per pack, so the pack carries the plain tier only. (c) The ABSOLUTE DURABILITIES 810/1000/935/686: the `durability` knob is a wear-CANCEL probability, not a maximum, and `diamond-stats` writes diamond's maxima where the platform supports a custom one. (d) The FOUR-LINE heroic lore block: a blank, `§7+{armor} Armor Value`, `§7{durability} Durability`, `§4This armor is stronger than diamond.` — `lore-line` is a single template recognising `{TYPE}`/`{AMOUNT}`, so the two stat lines have no token to interpolate and only the third ships. What DOES ship is the classification itself, which is why the file exists at all: a successful upgrade is what makes `%victim.heroicpieces%` read anything but 0, so every consumer of that count (`enchants/hero-killer`'s gate, Infinite Luck's accumulator) stops being inert-by-construction with this file. The 12.5 %-per-piece Infinite Luck negation (D-10-1) is NOT restated here — it is a property of the consumer and its row already stands on `enchants/infinite-luck` | per-slot heroic percents, a second heroic tier (or a per-piece heroic grade), an absolute max-durability knob, and armour-point/durability tokens on the heroic lore template | follow-up candidate |

## Armour-set batch (08) — structural gaps recorded in-file

The twelve `content/sets/` files and the eleven `content/crystals/` files ARE
authored and the gate is green. The fourteen `items/<set>-<piece>.yml` stopgaps
that once stood beside them are GONE — the per-piece surface landed on
`SetDef.Member` in wave 2e, so this pack's `items/` holds one universal likeness
per family (`crystal.yml`, `heroic.yml`, `pet.yml`) and nothing per instance.
What follows is every stop and structural gap the batch recorded as a comment
inside those files, plus three engine findings the verifiers turned up that no
file could record from the inside. The first row is the one that decided whether
any of the rest was visible in play; it is now closed, and every struck clause in
this table was re-verified against the tree when the pets batch closed.

| Entry | Recorded gap | Blocked on | Unblocks at |
| --- | --- | --- | --- |
| **`content/sets/*` (12) + `items/<set>-<piece>.yml` (14) — THE PER-PIECE SURFACE** | **THE CRITICAL ROW.** `SetDefReader` gives a set ONE shared `armor.lore` and ONE shared `armor.enchants` map, and a `pieces:` member carries `material` + `name` only (`MEMBER_KEYS`). Per-piece flavour lore, per-piece enchant rosters, the leather dye, the per-slot armour value, the per-slot durability and the per-slot heroic reduction rung therefore have no home on a set def, and ALL TWELVE set files preserve them as comments rather than lose them — nine carry the per-piece flavour verbatim (typos included: Yijki's `forebidden`, Ranger's `a ancient`, Dimensional Traveler's `barer`, Ghost's `Ghost don't have feet.`), and eleven carry the per-piece rosters. KOTH, Architect and Death Knight record that their pieces carry NO flavour lore at all, which is itself a measured fact with nowhere to live. The batch's answer was a second file per piece: fourteen `items/` files declaring `type: set-piece` (Dragon Slayer ×4, Ghost ×4, KOTH ×6 including the sword and the axe), each carrying `set`, `slot`, `color`, per-piece `lore`, per-piece `enchants`, `armor-value`, `durability`, `heroic`, `defense-reduction`, `heroic-lore`, `roll-formula`, `roster-size`, `rolls` and `mastery-pool`. **`ItemsLoader` DOES NOT CLAIM `set-piece`.** It dispatches `items/*.yml` on `type:` through a closed switch (soul-gem, crystal, heroic, slot-orb, the six scrolls, the four traks, dust, books, pet, pet-food, mask, reforge) and has no `set-piece` case, so all fourteen fall to `default -> diags.warning(DiagCode.W_ITEM_TYPE, "unknown item type …")`. A WARNING, not an error — which is exactly why the gate is green and the pack compiles — but the files load NOTHING, and every one of them says so in its own header (`ItemsLoader does not yet claim 'type: set-piece'`). The consequence chain is the part worth recording: no set piece mints, so the `heroic: true` those files carry never stamps a `HeroicStat`, so `%victim.heroicpieces%` STILL reads 0 — and `enchants/hero-killer`'s gate and Infinite Luck's accumulator are still inert-by-construction, which is the exact condition the hero-killer row above said would lift "the day the sets/items batch lands". It has not lifted. `items/heroic.yml` (`type: heroic`, claimed) is the only path to a non-zero count today, and it is the UPGRADE item, not a minted heroic piece | ~~a `set-piece` reader in `ItemsLoader` carrying the per-piece knobs those fourteen files already declare — or the same surface folded onto `SetDef.Member` so a set def can say them directly~~ **CLOSED in wave 2e** (ADR-0073) as the SECOND reading: `SetDef.Member` takes `lore`, `enchants`, `color` and `heroic`. `items/` holds one universal likeness per FAMILY, never one file per instance; a per-piece roster is compiler data (its `enchants/<id>` refs are validated library-wide and stamped into combat state) and `ItemsConfig` never reaches the compiler. Per-piece lore renders from state keyed on the item's gear KIND — which IS the slot name for anything a set can mint — so the codec is untouched. The consequence chain is live: a member's `heroic: true` mints through the upgrade gesture's own stats write, so `%victim.heroicpieces%` finally reads a real count. The fourteen stopgap files are GONE — their lore, dyes and the Ghost boots' guaranteed Gears now sit on the members (verbatim, `Ghost don't have feet.` and `Firey` included), the KOTH axe's likeness survives as a comment on `sets/koth.yml` (a set def has one `weapon:` and it is spent on the sword), `items/koth-sword.yml` was pure duplication of that `weapon:` block, and no file declares `type: set-piece` any more, so the `W_ITEM_TYPE` warning is gone with them. **One caveat found while landing it:** `heroic: true` routes `items/heroic.yml`'s per-piece reduction into the SAME additive channel a set's `DAMAGE_MOD(side: defense)` feeds (`DamageFold` sums `heroicReductionPercent + reductionPercent`), so the four M-Kit sets — whose 45 % completion row already IS their heroic wall folded — would bill 45 + 27 = 72 % against a measured 45 %. KOTH takes the stamp (it has no defense row at all, so the 27 % is a channel that was previously missing entirely); `sets/ghost`, `sets/necromancer`, `sets/architect` and `sets/death-knight` carry the byte-identical folded row and CANNOT take it until the M-Kit's steeper heroic tier lands. That is now their only blocker. **The lift finished in 2e.2**: all twelve sets use the members — nine carry their per-piece flavour verbatim (`forebidden`, `a ancient`, `barer`, `Ghost don't have feet.`, `Firey`, Necromancer's `magick` and the shroud's missing full stop all intact); KOTH, Architect and Death Knight carry NO member `lore:`, which is how this surface says "these pieces were built empty"; and the three M-Kit dyes that had nowhere to live (`#2239B9` twice, `#00FF00`) sit on `color:` beside Ghost's `#808080` and KOTH's `red`. The heroic ruling is now stated in all four M-Kit files, not just Ghost's. **What the members still cannot hold**, unchanged and recorded per file: the per-slot armour values (halved 2/4/3/2 on the M-Kits, 3/8/6/3 on KOTH), the per-slot durabilities (810/1000/935/686), and the heroic append's `§7+{armor} Armor Value` / `§7{durability} Durability` pair — a member's `lore:` prints ABOVE the shared block, and that pair belongs between its blank line and its last | **CLOSED — wave 2e** |
| `content/sets/*` + `items/*` — MINT-TIME LEVEL ROLLS | `armor.enchants` / `weapon.enchants` is `ref: level`, ONE fixed integer per entry, and this family's mint is a roll. Two shapes have no spelling. (a) The NEARLY-MAXED draw `min(M, max(1, M - 2) + rand(3))` over each enchant's own max `M` — recorded in seven set files and as a `roll-formula:` field in eight `items/` files. Authored as a fixed level, every minted piece is the TOP of the band and the `M-2` / `M-1` rungs never appear. `sets/ranger.yml` is where the cost is visible and the file states it plainly: twelve of its eighteen bow enchants are rolls, each authored at the top of its recorded band with the band beside it (`enchants/lethal-sniper: 5 # jar rolls uniform 2..5`, `enchants/silence: 4 # nearly-maxed band 2..4`, `enchants/blind: 3 # 1..3`, …), so every minted Ranger Bow is the best roll the jar could produce. (b) The per-piece PROBABILITY rolls — `60 % Valor` / `17.5 % Alien Implants` / `30 % Immortal` / `7.5 % Nature Wrath` / `45 % Phoenix re-roll` (which OVERWRITES the base roll it lands on, measured) — recorded as `rolls:` lists. The Yeti Maul's `25 % chance of Demonic Lifesteal at uniform 1..max` is DROPPED rather than minted at band-top, and for a second reason that stands on its own: `enchants/demonic-lifesteal` `requires` Lifesteal and `removes-required` strips it on apply, so the pair can never sit on one item however the roll is spelled. KOTH is the documented exception — every roster entry at MAX, no random rolls anywhere on the set (measured), so the family formula does not apply and its ONE probability is the leggings' `7.5 % Nature Wrath` | ~~a rolled-level spelling on a mint roster: a `min..max` band and a per-entry chance, resolved by the pack MINTER (which is what the era lines already say reproduces the distribution) rather than by item data~~ **CLOSED in wave 2e** — an `enchants:` entry takes `{ min, max }`, `{ nearly-maxed: M }` or a `chance` gate over either, drawn by the minter through the injected `Rolls` vocabulary. `nearly-maxed` is reproduced LITERALLY rather than as a uniform band over the same bounds: the outer `min()` collapses two of the three rungs once `M < 3`, and that skew IS the measured distribution. A bare level still means one certain level and consumes no draw, so a pack with no rolls authored mints exactly as before. Validation checks the whole BAND against the enchant's max, so a roll cannot mint an undefined level on some fraction of pieces and pass review on the rest. ~~Authoring the recorded rosters is content work the surface no longer blocks~~ **DONE in 2e.2, and it found the surface's own edge.** Every roster the codex ENUMERATES is authored — twenty-eight per-piece rosters across seven sets: Phantom's four, KOTH's four (at bare max levels — that set has no random rolls at all, so a certain entry is the right form and consumes no draw), and the four each on Yeti, Mother of Yijki, Ranger, Dimensional Traveler and Dragon Slayer, the last carrying its base rosters beside all four of its per-piece bonus rolls. Plus Ranger's twelve bow rolls off their band tops onto `{ nearly-maxed: M }` / `{ min: 2, max: 5 }`, the Yeti Maul's `{ chance: 25, min: 1, max: 3 }` Demonic Lifesteal, Supreme's four per-piece rolls, and the named probability rolls on all four M-Kits — whose SIXTEEN base rosters are enumerated too, at `10-armor-sets.md` §§ Ghost / Necromancer / Death Knight / Architect ("Base enchants via `addNearlyMaxedEnchants` (N): …"), and are now authored as certain `{ ability-set: M }` entries beside the probability rolls, `addNearlyMaxedEnchants` stamping every name unconditionally. Their §A.11 draw differs from §A.16's, and **R-QC64 BUILT the second shape rather than ledgering the difference** — `ability-set: M` is `M - rand(min(4, M))` plus the 25 % top-rung shave, so D-12-37 is VOID and the four M-Kits mint on their own helper. A HEROIC roster entry is TWO stamps in the jar — the heroic at uniform `1..M` and its non-heroic partner at that partner's max — and both halves author, the partner as a bare max level where the roster does not list it separately; Dragon Slayer's `addEnchants(…, 0.4)` gate rides its heroics as `{ chance: 40, min: 1, max: M }`, the only heroic gate in the family (`applyCustomEnchantments` lands every heroic unconditionally). **THE NEW RESIDUE IS `chance`'s TYPE:** `SetDefReader.rollInt` parses it with `ContentParse.intOr`, so a roster entry's chance is an INTEGER percent — and this family's probabilities are littered with half points (`7.5 % Nature Wrath` on KOTH's legs and Dragon Slayer's, `7.5 % Phoenix` + Guardians on Dragon Slayer's boots, `17.5 % Alien Implants` / `17.5 % Godly Overload` / `22.5 % Planetary Deathbringer` / `22.5 % Divine Enlighted` / `22.5 % Ethereal Dodge` / `12.5 % Guided Rocket Escape` / `12.5 % Infinite Luck` across the M-Kits). ~~`chance: 17.5` does not merely round: it warns and falls back to **100**, minting an entry that should be rare on every piece, so those rolls stay recorded in-file rather than approximated.~~ **CLOSED by R-QC51** — `chance` is a double, parsed with `ContentParse.doubleOr` and drawn in basis points, so every recorded half-point authors as written; the validation range is unchanged at (0,100]. Three more shapes have no spelling and are recorded per piece: the OVERWRITING re-roll, which replaces the base roll it lands on rather than adding an entry — the Phoenix one (Ghost 45 %, Necromancer 35 %), and the same shape on Dragon Slayer, whose heroics all sit at the END of their rosters, so a passing 40 % gate re-stamps Armored / Deathbringer / Implants / Overload / Enlighted at MAX over the draw the roster already made, and whose 5 % chest bonus does the same to Enchant Reflect (each entry carries the certain draw and the bump has no spelling; the other plain sets are unaffected, because their heroics come FIRST and the later plain entry is the one that survives); Supreme's three `10 % X, ELSE Y` EXCLUSIVE alternatives, where two independent gates would land both arms 9 % of the time and neither 1 %; and a duplicate ref (Supreme's "a second Marksman", Yeti's twice-listed `Armored`), a map being keyed by ref — one entry IS what "the second roll wins" means. Three refs are absent from this pack rather than unspellable, and a set's `enchants:` ref is validated library-wide, so naming one would be a compile ERROR, not an inert key: `Blood Lust` sits commented at its codex position on TEN rosters — Phantom's shroud, KOTH's chest, Yeti's, Mother of Yijki's, Ranger's cuirass, Dimensional Traveler's, and the 8th base entry of all four M-Kit chestplates — and un-comments the day wave 3b-3 lands `content/enchants/blood-lust.yml` (R-QC46's unblock list is wider than the two rosters it names); `Ghost` (Supreme's boots, Dragon Slayer's) and `Ghostly Ghost` (Dragon Slayer's) are commented permanently — R-QC38 drops both from the port, no consumer for either marker existing anywhere in the corpus | **CLOSED — wave 2e; the fractional mint `chance` closed in wave 3a (R-QC51)** |
| `sets/mother-of-yijki` — the World Ender's `125 % Revenge of Yijki` | THE WEAPON-HELD CHANCE. The jar recomputes ONE roll at `6.25 %` while the sword is held (the armour roll's 5 % modulated by ×1.25), and the surface has no way to modulate a chance on held identity: `on: weapon` gates a WHOLE bonus rather than a number, `%actor.helditem%` is a material name and never an item identity, and nothing lets a weapon bonus suppress its armour twin. The rider therefore ships as an INDEPENDENT `+1.25` pp roll, which lands `6.1875 %` a hit against the jar's `6.25 %` and gives a 1-in-1600 hit TWO fields where the jar can only ever land one. The direction and the felt rate are right; the shape is two rolls where the jar has one. Same knob, other files: `sets/supreme.yml`, `sets/ranger.yml`, `sets/phantom.yml`, `sets/yeti.yml` and `sets/koth.yml` all note that `on: weapon` IS the held-identity gate and closes the jar's forgeable display-name test — it is only the CHANCE half that has no expression | ~~a chance MODULATOR — either a weapon-held fact a `chance:` expression can read, or a way for an `on: weapon` bonus to REPLACE its `on: armor` twin rather than add to it~~ **CLOSED in wave 2e** as the FIRST of the two: `%actor.setweapon%` (BOOL), resolved at equip in `WornResolver` where slot provenance still exists and read by UUID off `WornState` — no live main-hand read on the hit path. It is deliberately the SAME condition that admits an `on: weapon` bonus (its weapon in the MAIN hand and its set complete), so reading the fact and writing `on: weapon` are one question. The replace-its-twin option was NOT taken and is not needed: with a fact a chance expression can read, one roll can carry both rates and the two-roll shape goes away. ~~Re-authoring Yijki on it is content work~~ **DONE in 2e.2**: `sets/mother-of-yijki.yml` carries ONE `DEFENSE` roll at `chance: 5` under `condition: "%actor.setweapon% : +1.25 %chance%"` — 5 % bare, 6.25 % with the World Ender held — and the independent `on: weapon` rung is gone. A BOOL cannot be read by a chance EXPRESSION (`ConditionCompiler.numVar` errors on it), so the modulator is the `±N %chance%` outcome clause rather than arithmetic; the clause budget is one per condition, which this ability had spare | **CLOSED — wave 2e** |
| `sets/ghost`, `sets/necromancer`, `sets/death-knight`, `sets/architect` — the M-KIT FOLD | The jar grants the M-Kit heroic reduction PER WORN PIECE (chest 16 %, legs 13 %, boots 8 %, helm 8 %) and the engine gates a set bonus on COMPLETION, so all four files fold the four component stats into ONE 4/4 bonus at the measured full-set 45 %. At the only wear state a complete set exists in, one 45 % row IS the four rows; the delta is PARTIAL WEAR — two Ghost pieces plus two other heroic pieces pay 16+13 in the jar and nothing here until the fourth lands. `items/heroic.yml` cannot supply the ladder from the other side either: it carries ONE flat `percent-reduction` (`0.0675`, the plain non-M-Kit tier) for every heroic piece, and exactly one `type: heroic` config exists per pack, so the M-Kit's second and steeper grade — with its armour points HALVED to 2/4/3/2 against KOTH's 3/8/6/3 — has no home at all. STANDING NOTE, carried verbatim in all four files: the day a per-slot heroic ladder lands, these four completion rows must GO, or the two channels double-count. **Wave 2e made that note load-bearing rather than theoretical.** A member's `heroic: true` now stamps the pack's heroic tier at mint, and `DamageFold` sums `heroicReductionPercent + reductionPercent` into ONE channel — the same one the 45 % row feeds — so setting it on any of these four bills 45 + 27 = 72 % against a measured 45 %. Dropping the 45 % row instead is not the fix either: `items/heroic.yml` carries the PLAIN tier, so the set would silently lose 18 points of its measured wall. All four therefore keep the fold and take NO stamp; KOTH, whose bonuses carry no defense row at all, takes it and gains the 27 % that was previously missing entirely | a per-slot heroic ladder (or a per-piece heroic grade) on `items/heroic.yml` — the same knob that file's own partial row above asks for from the plain tier's end. It is now the ONLY thing between these four and a minted-heroic set piece | follow-up candidate — with the second heroic tier |
| `sets/necromancer` (and `sets/ghost`, `sets/death-knight`, `sets/architect`) — the MASTERY POOL | An ability set has NO set proc: every "Necromancer ability" is a mastery enchant rolled onto the pieces at mint (2 drawn WITHOUT replacement, 50 % each). A set file has no root key for a pool and the draw belongs to the minter, so all four files record the pool and reference nothing — the same mint-surface gap the roll row above names, one level up. Necromancer is where it costs most: its pool is `Mortal Coil`, `Soul Siphon`, `Rot and Decay`, `Demonic Gateway` and THREE of the four are batch-07 stops in the table above, so the set's entire offence today is the one enchant it has no way to roll. Ghost is the same shape (`Horrify`, `Feign Death`, `Poltergeist`, `Mark of the Beast` — three deferred); Death Knight and Architect share a pool whose four members are all authored. The crystals already carry the mirror image of this row (`crystals/anti-m-kit-ghost`, `crystals/anti-m-kit-necromancer`, above), which is why those two crystals cancel one enchant each | a mint-side pool draw (the minter surface the roll rows want), plus the six batch-07 enchants | with those enchants + the set-piece surface |
| `sets/koth` + `items/koth-axe` — FOUR KOTH RIDERS | **(a) CLOSED (R-QC35a) — the axe is LIVE.** A set def takes `weapons:`, the keyed form of `weapon:` (the same shape `armor.pieces` uses), so KOTH ships both the sword and the axe with their recorded names; each weapon's lore and mint roster moved onto its own MEMBER, which is what lets several coexist. The strip authors as the two recorded rows, `[chance 50] DURABILITY(target=armor, mode=damage, amount=2)` plus `[chance 25] amount=1` on `%victim.type% == "PLAYER"`. `on: weapon` stays per-SET — it asks whether a KOTH set weapon is main-handed on a complete set — so WHICH weapon is decided by material, and under that gate `%actor.helditem% == "DIAMOND_AXE"` is EXACT where the bare regex this row rejected was not: nothing else can be both a DIAMOND_AXE and a stamped weapon of this completed set, so a Yeti Maul cannot reach it. The residue, recorded on the file: two weapons of the SAME material in one set would be indistinguishable, which no set in the family has. The original wording follows. ~~(a) THE AXE STRIP, not authored: `[chance 50] DURABILITY(target=armor, mode=damage, amount=2)` plus `[chance 25] DURABILITY(…, amount=1)` on a player hit while the axe is held — the engine's whole-armour expression of the jar's 50 %-per-slot roll for −2 or −3 (expected 5 durability a hit, a different formula from the Yeti Maul's 75 %/−1). Every effect exists; what does not is a SECOND set weapon — `weapon:` is one member per set def and `sets/koth.yml` spends it on the sword — so the axe is NOT LIVE. (Its file went with the wave-2e stopgap sweep; the likeness and this routing note now ride as a comment on `sets/koth.yml`, since a set def still has nowhere to put a second weapon.) The one surface alternative, `%actor.helditem% matchesregex ".*_AXE$"`, would hand the strip to ANY axe a KOTH wearer picks up, a Yeti Maul included: a play change, not a translation.~~ (b) AUTO BLESS, three clauses: the jar's 100 t INITIAL DELAY (`repeat:` schedules its first fire one period out, so the first cleanse lands at 20 t); the rest of the shared bless routine (the `bleedstacks` reset and the 0.2 walk-speed restore that `blessed.yml` runs on its own proc — matrix/10 decomposes this hook as the cleanse alone, and pinning a walk speed every second would clobber every other speed effect on the wearer once a second); and the `§e§l** BLESSED **` line, which matrix/04 records as throttled to one per 160 t FOR KOTH WEARERS while the cleanse stays unthrottled — a set-membership condition with no fact on the surface, already recorded on `blessed.yml`, and untuned it would print on the wearer's screen three times a minute forever. **(c) CLOSED (R-QC35c) — the footer is AUTHORED.** A set def takes `claim-footer:` with its two forms (the unclaimed line is a different sentence, not the claimed one with an empty token), the set-lore renderer binds `{CLAIMANT}` / `{DATE}`, and `sets/koth.yml` carries both strings verbatim. The state is a `ClaimCodec` pair kept OUT of `CombatState` on the trak/holy precedent — a claim lands on an external system's schedule and would thrash the content-hash cache from inside the blob. The DATE is stored ALREADY FORMATTED: the recorded `EEE MM/dd/yy` belongs to whoever runs the event, and re-formatting an instant would need a locale and zone contract the codex has no source for. Three states, not two — unstaked (no date, no footer at all), staked-but-unheld, and held. This plugin runs no KOTH event, so nothing in it writes the pair today; the surface exists so the system that does has somewhere to put it, which is what this row asked for. ~~(c) THE CLAIM FOOTER: `§b§l(!) §bClaimed by §d§l{player}§b on §d§n{date}`, unclaimed `§b§l(!) §bClaimed on §d§n{date}`, date format `EEE MM/dd/yy`. Both tokens are fed by the external KOTH event system, the set-lore renderer binds NEITHER, and a literal would print the brace text on every piece.~~ (d) THE WEAPON STRINGS: the jar mints both KOTH weapons in an external plugin and reads only their identity tags, so the codex records no display string, no lore block and no enchant loadout for either. The names extend the recorded piece-name pattern with `Sword` / `Axe` in the `{Piece}` slot (port-chosen and flagged), `enchants:` is deliberately absent rather than invented, and no `KOTH WEAPON BONUS` block is written — every OTHER set's weapon has one recorded and KOTH's has none | ~~(a) a multi-weapon set surface~~ **(a) CLOSED — wave 3c (R-QC35a)**; (b) an initial-delay knob on `repeat:`, a set-membership fact for the line throttle, and the shared bless routine's other two halves; ~~(c) a claimant/date token pair on the set-lore renderer, fed by an external event system~~ **(c) CLOSED — wave 3c (R-QC35c)**; (d) nothing engine-side — a codex pass, or the owner's leave to invent | follow-up candidate (a–c); owner ruling / codex (d) |
| `sets/dimensional-traveler` + `sets/dragon-slayer` — THE FREEZE-CLEAR HEAD | Nothing on the surface ENDS a live freeze, and both ends of that hole are in this batch. (a) Dimensional Traveler's field profile un-freezes a victim its falling blocks re-hit; `STATUS_CLEAR` lifts TELEBLOCK / POTION_LOCK / DISARM and no head clears FREEZE, so a re-hit victim stays rooted for the rest of the 80 t instead of being shaken loose. Recorded in-file, not faked. (b) Dragon Slayer's "Immune to Freezes" is the same hole from the other side, and the file records two independent reasons a `scope: KIND, key: FREEZE` window is NOT the answer: `trap.yml`'s freeze is a `MOVEMENT_SPEED` grant rather than a `FREEZE` effect, so the effect head keys nothing that enchant fires; and a KIND window would silently ALIGN Trap and Titan Trap, losing the measured asymmetry (Trap blocked outright with no leak chance, Titan Trap ignoring the immunity entirely) that `trap.yml`, `titan-trap.yml` and the `enchants/titan-trap` row above all name. It ships instead as three `SUPPRESS_INCOMING` rows keyed to `enchants/trap`, `enchants/ice-aspect` and `enchants/horrify`, the third inert until batch 07 lands. One in-file inaccuracy travels with it, worth fixing when that file is next opened: the footer says the immunity is keyed to `enchants/trap` + `enchants/ice-aspect` "alone" where the config keys three | ~~an UNFREEZE head (or a `FREEZE` rung on `STATUS_CLEAR`) for (a)~~ **CLOSED in wave 2e** as the `FREEZE` rung — the body already existed (`FrozenTargets.breakNow`, the window's one idempotent generation-guarded teardown, which is what `TRAP_BREAK` calls); the rung is the fourth wire code and the arm that reaches it, and that arm takes an entity hop where its three siblings run inline, because the teardown's freeze-tick unpin and attribute-modifier removals are writes only the target's own thread may make. Nothing for (b): keying by enchant is the intended shape for as long as the Trap / Titan Trap asymmetry stands. ~~Authoring the DT re-hit clear on the new rung is content work~~ **DONE in 2e.2**: the `IMPACT` payload on `sets/dimensional-traveler.yml` now runs `{ STATUS_CLEAR: { status: FREEZE, who: "@Victim" } }` between the health subtraction and the cue. (b) also had one in-file inaccuracy owed, the footer that said the immunity was keyed to two enchants where the config keys three — corrected in the same sitting | **(a) CLOSED — wave 2e; (b) by design** |
| ~~`sets/dimensional-traveler` — the RELATION-COLOUR TOKEN~~ | ~~The shift's freeze line is `§5§l** DIMENSIONAL SHIFT (§c{relation-color}{caster} [4s]§5§l) **`, and the jar's `§c` is ALWAYS overridden by the relation colour that follows it. `MESSAGE` binds `{ATTACKER}` — which on a DEFENSE activation is the wearer, the caster this line names — but has no relation-colour token, so the line ships as `§5§l** DIMENSIONAL SHIFT (§c{ATTACKER} [4s]§5§l) **`: the jar's line minus the one code point the jar itself overrode. `%victim.relation%` shipped in wave 1b, but a FACT is not a message token and the two are not interchangeable here — the colour is a property of the READER, not of the activation, so one broadcast line needs a different colour per recipient ~~ **CLOSED in wave 2e.3.** `{RELATION_COLOR}` shipped in wave 2e.2 (ADR-0074 §5) and the freeze line is authored on it here, so the string is byte-verbatim to the jar again — the `§c` included, which the colour that follows it always overrode. Every recipient of THIS line is an enemy of the wearer (the selector filters ENEMIES), so the token can only ever select `enemy-color`; it is left at its `&c` default and `ally-color` is unreachable here, so no value is invented for it. The mechanism is restored and the RENDER is unchanged, which is the whole delta | ~~a per-recipient relation-colour token on `MESSAGE` (the same per-viewer shape `PHANTOM_BLOCKS` needed for its client-only overlay)~~ shipped in 2e.2 | closed |
| ~~`sets/dimensional-traveler` — the `+4 Y` CUE ANCHORS~~ | ~~The shift plays `ENDERMAN_TELEPORT` and `ANVIL_LAND` at the target **+4 Y**. `SOUND` carries `sound` / `volume` / `pitch` and nothing else: it reads only the entity channel of `who`, so a location selector would silently move the cue OFF the victim to the activation point (the `reflective-block.yml` reading). Both cues drop the offset and play at the target. Cosmetic, and it is the knob `PARTICLE`'s `spread-y` half-answers for particles and nothing answers for sound ~~ **CLOSED in wave 2e.3.** `dy` shipped on `SOUND` and `PARTICLE` in wave 2e.2 (ADR-0074 §5) and both cues are authored at `dy: 4` here. It is the offset the row wanted rather than the location anchor it offered as an alternative: `dy` TRANSLATES the anchor the cue plays at and leaves `who` reading the entity channel, so the cue stays ON the victim instead of moving to the activation point | ~~a Y offset (or a location anchor) on `SOUND`~~ shipped in 2e.2 | closed |
| `sets/dimensional-traveler` — TWO LEGACY ALIAS ROWS | `ENDER_STONE → END_STONE` landed with the wave-2d.2 phantom-block work and `ANVIL_LAND` already carries a row, but `ENDERMAN_TELEPORT → ENTITY_ENDERMAN_TELEPORT` and **`ZOMBIE_WOODBREAK → ENTITY_ZOMBIE_BREAK_WOODEN_DOOR`** do NOT, so the shift's teleport cue and the field's impact cue both warn-skip on the legacy fork. Neither is NEW debt — `enchants/virus` (batch 05) already owes the first and `enchants/tombstone` (batch 07) the second, both parked on the legacy-sweep list below — so this entry only rides them, and the sweep closes all four files at once. Everything else on the entry is era-clean: falling-block entities and their block-state data differ per era behind the engine's own `FALLING_BLOCK` seam, and a walk-speed-0 freeze is era-safe | ~~the legacy sound sweep (both rows already listed below)~~ **CLOSED in wave 2e** — both rows landed in `Aliases.SOUND`, each javap-verified present in the 1.8.8 `Sound` enum AND in the modern constant lists (1.21.11, 26.1.2), with neither value colliding so `HandleResolver`'s reverse scan stays deterministic. This also closes the same two rows for `enchants/virus` and `enchants/tombstone` | **CLOSED — wave 2e** |
| `sets/supreme` — the Fanny Pack's MATERIAL (owner-visible invention) | matrix/10 § Supreme records the weapon's name (`§4§lSupreme Fanny Pack`), its whole nine-line lore block and its Sharpness V, and records NO MATERIAL. `DIAMOND_SWORD` is authored and flagged in-file: it is the family's weapon default (the Phantom Scythe and Yijki's World Ender are diamond swords, the Yeti Maul a diamond axe), the only choice that makes the recorded Sharpness V mean anything, and the only one consistent with the SWORD-only `applies-to` both of its custom enchants declare. It is nonetheless an invention on an owner-visible field. It is also the SECOND of two in this batch, and the other is invisible: KOTH's sword and axe are `GOLDEN_SWORD` / `GOLDEN_AXE` for two items the jar never built (on `sets/koth.yml`'s `weapon:` block and its axe comment since wave 2e; both were `items/koth-{sword,axe}.yml` when this row was written), and the record flags the invented NAME ("the only invented string in this set") while treating the material as an era-alias note — so the KOTH materials are just as invented and carry no flag. All three want the same ruling | an owner ruling, or a § Supreme / § KOTH codex pass that fixes the materials from the source | owner ruling |
| `sets/dimensional-traveler` — the `IMPACT` payload's ACTOR SCOPE (engine finding) | The shift's falling-block field hangs its landing payload on `trigger: IMPACT`, which is the only way to author one: a landing block fires the ACTOR's `IMPACT` abilities on what it hit and forwards the field's computed bite as `%damage%`. But `IMPACT` is a plain trigger with NO source scoping — `FallingBlockCasts` looks the owner up fresh at each landing and runs EVERY `IMPACT`-triggered ability that actor owns, not the one that armed the field. So a Dimensional Traveler wearer who also carries `enchants/tombstone` (whose anvil `FALLING_BLOCK` has its own `IMPACT` payload) fires Tombstone's 10 %-of-max whole-set armour damage and both of its zombie-door cues on each of the shift's ~142 landing blocks, and the same holds for every other author on that trigger — `TURRET_RING`'s skull strike and `SUMMON_STRIKE_PAYLOAD`'s melee rung both fire the actor's `IMPACT` too, so the collision surface grows with the pool. The per-victim re-hit ceiling bounds the DT field's own bite; it bounds none of the foreign payloads riding it. Recorded rather than worked around: the obvious dodge, a second trigger name per author, is precisely what the shared `IMPACT` contract exists to avoid | source scoping on `IMPACT`. **Wave 2e narrowed this to the SECOND option and disqualified the first.** "Fire only the abilities of the def that armed the cast" cannot be built: `defId` is assigned per ABILITY (`LibraryLoader` hands every reader one supplier and `SetDefReader.ability` draws from it per bonus), and a field's ARM and its `IMPACT` PAYLOAD are two separate `bonuses:` entries — on `sets/dimensional-traveler.yml` the arm sits in the `trigger: DEFENSE` bonus and the payload in a `trigger: IMPACT` one — so carrying `ctx.sourceDefId()` in the cast and matching `Ability.defId` at the landing would fire NOTHING AT ALL, which is worse than the current over-firing. The identity must be one the arm and its payload SHARE: the authored `group:`, which `AbilityDef` and every reader already carry. Shape: a group accessor on `EffectCtx`, the group id in `FallingBlockCasts.Cast` (one int through `Sink.fallingBlock`/`fallingBlockField` and `DispatchSinkBase.spawnCosmeticBlock`), and a group-filtered run through the EXISTING `TriggerRunner.runCandidates` — no new gate, no engine change. Unscoped stays the default so nothing authored today changes. The same filter scopes `TURRET_RING` and `SUMMON_STRIKE_PAYLOAD`, though a turret outlives its activation and must carry the group from `bindTurret` through `scheduleVolley` to `bindShot` rather than re-deriving it. **BUILT in wave 2e.2** (ADR-0074 §4: `EffectCtx.sourceGroup()` from `Ability.cdScopeGroup()`, one int through each carrier, `TriggerRunner.runGrouped`; the turret group rides the volley closure and NOT the emplacement), **and DECLARED by the content in wave 2e.3** — `sets/dimensional-traveler.yml` carries `group: "dimensional-shift"` on both the arm and the `IMPACT` payload, so the Tombstone collision this row records is closed. Three residues stay open and are recorded elsewhere on this page: the coarse-identity one (the filter matches the authored `group:`, which is a TIER word on every enchant — Tombstone's `mastery` — so scoping is per-GROUP, never per-feature), the crystal rider that must declare the same string the day it is authored, and every other unscoped author, which keeps today's fire-everything behaviour by design | **CLOSED — engine wave 2e.2, content wave 2e.3 (residues recorded)** |
| `crystals/dragon-slayer` + `crystals/ranger` vs THEIR OWN SETS (engine finding) | A defender-keyed window is keyed per `(holder, scope, key)`, and `SuppressionStore.defend` merges with `laterDefender` — `b.expiry() > a.expiry() ? b : a` — which keeps the WHOLE `Defender` record with the later expiry, including its `chance` AND its feedback line. There is no `max()` on chance anywhere on that path. Set and crystal arm the SAME triples, from two independent abilities, on the same `repeat: 20` / `duration: 60` cadence, so whichever fires later in a cycle governs the next 60 t — and the crystal's chance is always the LOWER one. `sets/dragon-slayer.yml` arms `(ENCHANT, enchants/ice-aspect)` at an absolute 100 and `(ENCHANT, enchants/silence)` at 75; `crystals/dragon-slayer.yml` re-arms both at 10. `sets/ranger.yml` arms `(KIND, TELEBLOCK)` at an absolute 100; `crystals/ranger.yml` re-arms it at 20. A wearer who completes the set AND carries its own crystal is therefore PERMANENTLY WORSE OFF than one who wears the set alone — 100 % freeze immunity degraded to 10 %, 100 % teleblock immunity to 20 % — and the set's verbatim block line is replaced by the crystal's (or, on Ranger, by no line at all). It is reachable in play: `items/crystal.yml` records that the jar's refusal of a crystal on a SET piece has no expression here, so any ARMOR piece takes one. `crystals/dragon-slayer.yml` asserts the opposite in its header — "nothing here suppresses the set half" — which is true of the jar's external flags and false of this port. NOT patched in-pack: re-keying either side would break the per-piece scaling the crystals row above already owes, and dropping the crystal row would delete a matrix-recorded behaviour | ~~a merge that never LOWERS a live defender window (max-chance, or additive) on `SuppressionStore.defend`~~ **CLOSED in wave 2e**: `strongerDefender` takes the stronger CHANCE over the later EXPIRY. Chance decides the identity (carrying its own attribution and block line, so the set's verbatim line is no longer replaced by the crystal's) because chance is the point of the window and expiry is a refresh detail; a chance tie falls back to the later expiry and a full tie keeps the incumbent, the same "ties keep what is already live" rule the activator-side merges use. `crystals/dragon-slayer.yml`'s header assertion — "nothing here suppresses the set half" — is now true of this port as well as of the jar. The per-piece SCALING this row's twin asks for from the other end is untouched | **CLOSED — wave 2e** |
| `sets/dragon-slayer` — `PROC_REBOUND` is DEAD ON ARRIVAL (engine finding) | The set contributes reflect level 10 to the MASTERY rung of the enchant-reflect ladder, and the file authors the band exactly right: `tier-min: 80, tier-max: 80`, the `mastery` tier WEIGHT off `tiers.yml`, precisely the grade `heroic-enchant-reflect.yml` reserves ("a future mastery takes `80..80` and outranks this one on tier 8 alone"). The band is correct and the ability still never fires. `ProcReboundEffect.run` arms with `ctx.level()` — the ABILITY's own level — and `AbilityDef.level` is documented "enchant level; `0` for non-enchant sources"; `SetDefReader.ability` passes a literal `0` for every set bonus, because sets are levelless. `ReboundPlan.claim` then rejects on `ability.level() > rule.level()`, i.e. on `level > 0`, and every enchant level starts at 1 — so the armed rule reaches a tier-8 proc, loses the level gate, and claims nothing, forever. The matrix's "level 10" has nowhere to go: `PROC_REBOUND` takes `chance`, `tier-min`, `tier-max` and `who` and DELIBERATELY not a level, on the reasoning that "the matrix's gate is a comparison against the worn piece, not an authored number" — which is right for an enchant and leaves a levelless source with no way to say one at all. Authored as recorded rather than faked, and inert until the level has a spelling | ~~a rebound level for a levelless source: either a `level` param on `PROC_REBOUND` (the doc's own reason for omitting it does not cover SET / CRYSTAL / HEROIC sources), or an authored level on a set bonus~~ **CLOSED in wave 2e** as NEITHER — the fix is a reading, not a knob. A rule armed at level `0` is LEVELLESS, not weakest: `ReboundPlan.claim` now treats `0` as "no level bound" and answers its whole band, while an enchant-armed rule keeps its gate unchanged, ties included. The matrix's "level 10" needs no home because the comparison it was for does not apply to a source with no grade to state. The authored `80..80` band was already correct and now fires | **CLOSED — wave 2e** |
| `sets/mother-of-yijki` — the World Ender's HEROIC-FORM sensitivity (owner decision) | The jar's DEFENSIVE weapon check accepted the World Ender's DIAMOND form only, so a Heroic Sword reforge kept the `+20 %` attack rider and silently LOST the `×1.25` revenge chance. Recorded, NOT reproduced: this port gates both branches on the set-weapon PDC stamp, which survives a reforge, so a heroic-converted World Ender keeps both halves. That is the same call `sets/yeti.yml` takes for the Maul (whose diamond-axe-only identity check the jar likewise voided on conversion, and which the port lets survive "as intended, with the reforge family"), so the two files are consistent and the pack has no split. Flagged here because it is a MEASURED behaviour the port drops by choice, and because reproducing it is not free: the jar's check reads the item's FORM, and no surface carries a heroic-form fact — the reforge family stamps the form but nothing reads it from a condition | owner ruling — reproduce the form-check (which needs a heroic-form fact first) or keep the port's survive-the-reforge reading, which is the one already shipped on two files | owner ruling |

## Armour-mask batch (11) — structural gaps recorded in-file

The twenty-six `content/masks/` files ARE authored and the gate is green, and so
is the family likeness: `items/mask.yml` SHIPS with this batch — authored, no row
owed here — which satisfies the k-group stop reports that filed it. What follows
is every stop those files recorded as a comment from the inside and cannot close
from there. The Multi-Mask is not among them and never will be: it is a likeness
(`name-multi` / `lore-while-on-item-multi`) plus `config.yml`'s
`masks.max-merge: 2`, the Multi Crystal shape ADR-0074 ruled, never a def.

| Entry | Recorded gap | Blocked on | Unblocks at |
| --- | --- | --- | --- |
| **MASK ACQUISITION — family-wide (all 26 files)** | The Mask Generator has NO pack surface, so every mask's acquisition odds are a comment. The roll is two-tier: `Math.random() <= 0.1` takes the RARE branch, everything else the COMMON pool of 11 at 90 %/11 = **8.1818 %** each (Pilgrim, Turkey, Scarecrow, Purge, Santa, Necromancer, Rift, Zeus, Glitch, Reindeer, Dungeon); the RARE pool is 9 at 10 %/9 = **1.1111 %** each (Monopoly, Party Hat, Death Knight, Lover, Joker, Bunny, Boss, Stronghold, Outpost); and seven are NEVER generated at all (Headless, Pumpkin Monster, Ghost, Dragon, Beanie, Spectral, Multi-Mask). Riding with the generator is its one-shot claim/blacklist anti-dupe framework, whose confiscation line is verbatim: `§c§l(!) §cIt seems you tried to claim a duplicate Random Mask, it has been removed from your inventory.` `MaskDef` carries no rarity, weight or pool field, and `items/` has no generator likeness, so all 27 percentages are recorded per file and expressed nowhere. Absence from a pool the pack does not model costs nothing; the pool itself is what has no home | a pool/weight field on `MaskDef` plus something that rolls it, and a per-player claim ledger for the anti-dupe half — or the ruling that masks are operator-minted and the pool is documentation. `matrix/` has no decomposition doc for the §C support-item codex the generator lives in, the same hole the `items/mastery-shard` row above names | owner ruling (mask roll surface vs operator-mint-only) |
| **HELP-MENU VISIBILITY — family-wide (3 masks)** | The source marks Pumpkin Monster, Beanie and Multi-Mask `active = false`, hiding them from the mask help menu while leaving them real, mintable and wearable. `MaskDef` has no hidden/active flag and no listing surface reads one, so every def ships LISTED and those three are visible here where the jar hid them. Each file records its own state in its ACQUISITION note | a `hidden`/`active` flag on `MaskDef` and a listing surface that honours it | the flag + the listing surface, or the ruling that every mask is always listed |
| ~~`masks/death-knight` — THE TWO BLOCK LINES~~ CLOSED (R-QC41) | Both verbatim strings ship on the mask's own `SUPPRESS`, as `consumed-message-victim` (`§c§l* PHOENIX BLOCKED [§7{ATTACKER}§c§l] *`) and `consumed-message-actor` (`§c§l* DEATH KNIGHT MASK [§7{VICTIM}'s Phoenix Blocked§c§l] *`). Both blockers are gone: a `mode: next-hit` charge now carries a `Feedback` record (the BURN is blind, the BLOCK is not — gate 5 asks about one ability's own scope, so the charge that stopped it names itself), and the consume path substitutes MESSAGE's `{ATTACKER}`/`{VICTIM}`, read as armer / silenced. The only edit to either string is the placeholder spelling. Emitted at the BLOCK, so nothing goes out on the ~50 % of hits that arm a charge nothing spends | nothing — ADR-0075 | closed |
| `masks/rift` — the SUPPRESSION SCOPE DELTA | Authored `SUPPRESS_INCOMING{scope: GROUP, key: mastery}` where the matrix asks `scope=TYPE, key=mastery`. TYPE scoping is REAL since R-QC3 (ADR-0075) and the matrix's condition is now expressible — but R-QC34's re-key was measured and **DECLINED**, because one interned slot names ONE type: the engine's TYPE vocabulary is the ability's combat DIRECTION, and declaring `suppress-type: mastery` on the ten mastery files would REPLACE that stamp, making every defender-side mastery ability silence-proof and breaking the END-SUPPRESS rule. It would buy nothing — all ten carry `group: "mastery"` 1:1 with `tier: mastery`, so GROUP already names the identical set, and the defender consult walks ENCHANT → GROUP → TYPE → KIND. The remaining delta is unchanged and unsmoothed: the window is BROADER than the source on every ordinary mastery proc (the source's only call site was the mastery-REFLECT path), and EMPTY on that reflect path — the gateless `PROC_REBOUND` re-run walks no gates and carries no random-backed roll supplier, so a partial window consulted there would block deterministically rather than roll (the wave-2d.2 ruling) | nothing engine-side; GROUP is the settled reading | a roll supplier on `PROC_REBOUND`, if the one path the source keyed is ever wanted |
| **DIVERGENT ATTACHED-STAMP ABILITY TEXTS — family-wide (3 files)** | Three masks carry a jar ATTACHED-stamp string that differs from their own lore line 1, and the matrix says transcribe both as-is. Joker: stamp `§cIncrease Combat Tag on players by 4s` against lore `§c+4s Combat Tag on enemy players`. Rift: stamp `§c50% Mastery Enchant Negation.` — the trailing full stop lore line 1 lacks. Boss: stamp `§c-25% incoming Boss DMG, +10% outgoing Boss DMG` against lore `§c-25% incoming DMG, +10% outgoing DMG to Bosses`. The port's on-helmet line is the likeness's own `Mask Equipped ({NAME})` wording (the wave 2e.2 ruling that struck the compound stamp for the same reason), and it renders the mask NAME rather than any ability text, so there is no second string slot for the variants to live in. Recorded in-file, rendered nowhere; the LORE — what a player actually reads on the item — is verbatim on all three | nothing. This is a record of what the 2e.2 ruling costs, not a request | never, by design: reproducing the stamp is the thing 2e.2 declined |

## Inventory-pet batch (12) — structural gaps recorded in-file

The seventeen `content/pets/` files, the universal `items/pet.yml` likeness and
the `pets:` block of `config.yml` ARE authored and the gate is green. Every entry
in the family is ACTIVE (the jar's 1 Hz hotbar ticker is a no-op for all
seventeen, so nothing passive is ported), every one earns XP ONLY from its own
ability, and every one ships its AS-INTENDED cooldown — the jar's remaining-time
arithmetic was inverted, so no pet cooldown ever blocked and the whole
on-cooldown branch was dead code (codex §A.6). What follows is every stop the
batch recorded as a comment inside those files.

| Entry | Recorded gap | Blocked on | Unblocks at |
| --- | --- | --- | --- |
| `pets/lava-elemental` — THE FAT BUCKET HALF | The pet's second payload half is not authored: `+{fatFill}` charges onto every Fat Bucket in the inventory (`25 x level`, 256 at L10), a hard cap of 256 a bucket enforced in the pet's own code, no bucket-budget limit on this branch, and its share of the XP credit — charges x STACK SIZE, so a stack of 64 at L10 credits 256 x 64 = 16 384 XP from one slot (measured, and kept as "the credit equals the charges actually added"). A Fat Bucket is an EXTERNAL item from an Arkkit patch: **codex-UNRESOLVED** (no material, name or lore recorded) and with no likeness anywhere in this port, so there is nothing to top up. The three verbatim lore lines still ship (`§7§f§l      +` / `§7§7Adds 25x Fat Bucket` / `§7§7charges to all Fat Buckets!`) and the ladders stay in the matrix entry; the XP grant is the converted bucket count alone | a Fat Bucket item existing in this port — which is an OWNER RULING, not engine work: its identity is codex-UNRESOLVED, so the choice is to invent one or to drop the clause permanently | owner ruling |
| `pets/lava-elemental` + `pets/water-elemental` — THE GATE-6 COOLDOWN RESERVATION | The one place the two conversion pets cannot express their intent. The jar UN-STAMPED the cooldown when nothing converted, so a click with no empty buckets cost nothing. Here the refusal is POST-PAYLOAD, not a condition: `INVENTORY_CONVERT` has to run before a count exists, and gate 6 reserves the cooldown window before the payload runs at all. So a zero-bucket use spends the full 5 minutes. Every OTHER refusal in the family is a condition (including the family's spectator refusal, which authors `cooldown: 0` and scopes to its own key), and gate 6 releases the reservation for those — this pair is the exception, and only because the refusing FACT is produced by the payload itself | either a refund path a post-payload branch can take (release the gate-6 reservation from inside an activation that already ran), or a pre-payload count fact `INVENTORY_CONVERT` could be gated on. Neither exists; the first is the smaller change and the one the shape actually wants | engine |
| `pets/world-destroyer` — THE PER-VICTIM CAGE | THE ONE UNPORTED CLAUSE; everything around it ships. The jar encases EACH victim where they stand — a 3x3 footprint, walls 3 high at y…y+2, an obsidian floor at y-1 and roof at y+3, up to 44 blocks, 100 t. This engine's `CAGE` is a different structure entirely: a TWO-PARTY trap centred on the MIDPOINT between caster and one victim, which TELEPORTS both parties into opposite interior cells (`CageGeometry.origin` + `sink.cage`) and whose pets pre-check (`PetService.cageWouldFail`) refuses the whole use when that midpoint volume is not clear — over `@Aoe{r=30}` it would teleport the caster into every victim's cage in turn and refuse the ability outright most of the time. `TEMP_BLOCK{shape: BOX}` is no substitute either: it FILLS its volume, burying and suffocating the victim rather than enclosing them. The verbatim `** DIMENSIONAL CAGE **` lines still ship (identity is verbatim even where a clause is stopped, the R9 shape), so the ability announces a cage whose blocks are owed. Recorded with it: the walls are a PORT PICK (D-12-31 — the jar wrote block id 417, `IRON_BARDING`, an ITEM with no block form on either era, so the measured walls rendered invalid/air-like; obsidian matches the floor and roof it really did place and exists on both trees), and the jar's two restore sweeps at 100 t and 101 t deleted the recorded obsidian to AIR (a grief bug, D-12-33 — a temp-block ledger restores originals exactly) | a per-victim, victim-anchored, caster-free HOLLOW SHELL primitive. Not a variant of `CAGE`: the two differ in anchor, party count and failure mode | engine |
| `pets/gaia`, `pets/smite`, `pets/world-destroyer`, `pets/raid-creeper`, `pets/vile-creeper` — THE EMPTY-SELECTION FACT | ~~An activation cannot see that its OWN selector matched nothing, and five entries pay for it differently.~~ **THREE CLOSE, TWO ARE RE-POINTED (R-QC66, ADR-0076).** `%selected%` is the count the most recently executed effect bound after every per-target filter, and −1 when the ability never activated (R-QC67), so an activation CAN now see its own emptiness. Gaia loses the `%nearbyenemies% >= 1` pre-gate entirely — a FIXED 8-block scan of LIVING ENTITIES where the payload reaches 13–28 blocks and the jar checked hostile PLAYERS only, so a lone cow opened it and a player at 20 blocks did not — and with the gate gone the jar's free mob-only wipe is back, which the gate had cost. World Destroyer's `§c§nPET: No valid enemy players nearby! (30x30)` and Smite's `§c§lPET: §cNo target found!` both ship, each on its own selection rather than on a coarser stand-in, and all three take `REFUND_COOLDOWN{unless: "%selected%"}` with their XP moved behind the hit arm — the as-intended "a failed use burns no cooldown and earns no XP" ordering. Smite pays its second debt too: `victim.type == PLAYER` is `each-if: %target.type% == "PLAYER"` on the FREEZE row, so a smitten MOB keeps its legs. AUTHORING SHAPE, recorded because it is the wave's one real trap: `%selected%` is rewritten by every targeting effect AND by every ability that fails to activate, so exactly one row and one sibling may read it — each pet captures the count into a var beside a 1-tick `fired` marker (the `lava-elemental.yml` idiom) and its two arms read the pair. THE TWO CREEPERS DO NOT CLOSE: their line answers a SPAWN failure, and `SPAWN_ENTITY`'s selector is `@Self`, which always resolves the activator whether or not the summon landed — so `%selected%` cannot see it and authoring the refusal on it would print never. Both files now record that honestly, and the branch is close to unreachable here anyway (the jar's `spawnCreeper` returned null; a spawn at the activator's own feet has nothing to refuse) | the creepers need a SPAWN that reports its count, the way `INVENTORY_CONVERT{count-var:}` does — not a subject fact | three closed; two re-pointed at a reporting spawn |
| `pets/raid-creeper` + `pets/vile-creeper` — THE SPAWN ANCHOR | The matrix spawns at `@Block{distance=5}`, the block you are looking at. `SPAWN_ENTITY` reads only ENTITY targets (`ctx.targets`) and never the location slot a block selector fills, so a `@Block` target resolves to nothing and falls through to the activation's own location — the user's feet. Both files author `@Self`, because that IS what the fallback does, said honestly rather than through a selector that looks like it aims | `SPAWN_ENTITY` reading `targetLocations`. A one-token change per bracket the day it does | engine |
| ~~`pets/raid-creeper` + `pets/vile-creeper` — THE CUSTOM-CREEPER SYSTEM~~ | ~~Both entries spawn ONE vanilla creeper, which is the whole on-surface skeleton. Everything that makes it a "Raid" creeper (the Lucky + Tactical hybrid stats, its AI, its explosion, the 100 % spawner drop rate) or a "Vile" one (corrupting the area it explodes in with `Rot and Decay V` for 5 s — both figures LORE LITERALS on the pet, not read from the enchant, and level-independent) lives in an EXTERNAL custom-creeper system the jar hard-references without declaring in its own plugin.yml. **codex-UNRESOLVED:** none of it is in the decompiled tree. Nothing about either creeper scales with pet level — only the cooldown does~~ **CLOSED BY OWNER RULING (R-QC54)** — the external system is never reconstructed. Both pets spawn the signature-pack creeper instead (`content/pets/creeper.yml`, mirrored bracket for bracket): a CHARGED creeper, `invincible: true` so it cannot die, `targeting: false` so it acquires nobody, `detonate: PLAYER_HIT` so its fuse lights only when something hits it, and `ttl: 600` so it despawns if nobody ever does. A planted sentry, which is what both entries read as, with no invented stats. The two pets are behaviourally identical; the descriptor is what separates them | ~~a port ruling on the external creeper system. Note `enchants/rot-and-decay` (batch 07) is separately stopped, and Vile Creeper only ADVERTISES that mastery — it never touches it, so the two do not unblock each other~~ | closed |
| ~~`pets/banner`, `pets/alchemist`, `pets/evolution`, `pets/stronghold-sell` — FOUR EXTERNAL PAYLOADS~~ | ~~Four entries take the `enchants/auto-sell.yml` treatment — obtainable, identity and XP economy real, cooldown deliberately kept (it is the only thing between an unauthored ability and a right-click that levels the head for free), the behaviour recorded and not implemented. (a) BANNER: give the player their faction's banner item, no soul cost, attributed "Banner Pet" — a factions plugin plus a custom-items build absent from the tree, the banner's own material/name/lore **codex-UNRESOLVED**; rides the same factions-bridge ruling `enchants/target-tracking` waits on. (b) ALCHEMIST and (c) EVOLUTION: the CosmicEvolutionKits flows, **OUT OF SCOPE under ruling R3** — Alchemist rolls `delivered(level)` items from the ALCHEMIST kit's loot table at kit level `max(3, petLevel)` (table contents codex-UNRESOLVED besides), Evolution raises the player's lowest eligible kit by one plus a `level x 1%` double proc. (d) STRONGHOLD SELL: the `sh-sell` window IS the payload, and **no reader exists anywhere in the decompiled tree** (codex-UNRESOLVED) — the window, its feedback and its XP ship, and nothing in this port consumes it. Every verbatim string that names a thing the pack cannot deliver is recorded rather than printed (a line announcing an absent item is worse than silence)~~ **CUT FROM THE PACK BY OWNER RULING (R-QC54)** — four hollow shells whose whole payload lives in plugins this port does not ship. All four files are DELETED rather than kept obtainable-but-inert; the pack's file count drops by four and their deviation rows (D-12-9/10/11, D-12-14, D-12-21/22, D-12-27) are struck as void | ~~(a) the factions-bridge ruling; (b)+(c) ruling R3 being revisited; (d) a `/sh sell` consumer, or a ruling that the window stays inert~~ | closed — cut |
| `pets/*` — PER-CONDITION FEEDBACK ON A `USE` ABILITY | A USE ability has no per-condition feedback hook: a failed condition simply does not activate, and the player sees the universal `pets.message-on-fail`. The family's SPECTATOR refusal escapes this by the folder ruling — it is authored as its OWN ability at `cooldown: 0`, which is what makes the verbatim `§c§lPET: §cYou cannot use that ability while spectating!` render — but the per-pet guards keep their own strings unrendered: Blackscroll's and Enchanter's "you already have an active … Pet applied" pair, Stronghold Sell's `§c§lPET: §cYou already have an active /sh sell effect!`, Gaia's `§c§lPET: §cNo valid enemy players nearby!` and Alchemist's `§c§lPET: §cNot enough space (3 slots) in inventory!` (which also has no inventory-space fact to read). The BEHAVIOUR is exact in every case — nothing fires, no cooldown burns, no XP lands; only the strings are lost | a per-condition message on a USE ability, or a second refusal ability per guard (the spectator shape, applied guard by guard — cheap, but it multiplies the bracket count of every file that takes it) | engine or a folder ruling |
| `pets/feign-death` — THE DECOY AND THE UNVANISH LINE | `VANISH` ships HIDE-ONLY: a packet-level hide from every player, ending on the timer or after `break-hits` of the actor's OWN landed hits (damage taken never spends one — measured, and structural in the effect). Two halves are not authored: the unequipped decoy corpse the jar spawned at the actor's position, which played the death animation and was destroyed after 30 t, and the `§4§l* Feign Death - UNVANISHED *` line it emitted when the window ended by EITHER route. `VANISH` takes no decoy and emits no end hook. The corpse-ticks value (30 t) and that one string stay in the matrix entry | the standing `VANISH` DECOY polish decision in the engine pool below (packet seam vs roster-marker) — the end-hook half rides it, being the same window's other missing edge | polish decision |
| `pets/feign-death` + `pets/tesla` — THE `USE`-CONTEXT COMBAT FACT | Both entries author a two-tier XP grant keyed on being in combat — Feign Death 5 in combat / 1 out of it, Tesla 10 / 4 — and on both the IN-COMBAT arm is INERT today. A USE activation is built with no combat context, so `%recentattackers%` reads 0 on every pet click and the out-of-combat arm answers every use. The store already feeds the sink; nothing sources it into the USE context. Both shapes are authored NOW so that nothing here needs rewriting the day it is. (The jar's THIRD tier on each — 50 for Feign Death, the warzone rung on Tesla — needs faction territory, which has no engine analogue, and collapses per D-12-7 / D-12-17 rather than waiting on this) | sourcing the recent-attacker store into the `USE` activation context | engine |

## Engine follow-up pool fed by these rows

- **Wave 2d (the sets/masks/pets family pool) — what SHIPPED.** Ten items, in the
  order a reader meets them on the surface:
  - `%actor.heroicpieces%` — the actor-side twin of wave 2c's victim fact, off the
    same worn-fact source. Infinite Luck's accumulator reads its OWN holder's gear.
    The row's other half is untouched: the accumulator is WEIGHTED (+12.5 per worn
    LEATHER heroic piece, iron/diamond 0) while the fact is an unweighted count, and
    whether the material weighting survives the port is still the owner ruling that
    row names.
  - `%actor.y%` (`POSITION_VARS`) — the first ABSOLUTE coordinate on the surface;
    every positional fact before it was relative, so a build-height gate had nothing
    to compare against. Feet Y, not eye Y (the eye offset moves with pose).
  - `FACING_SET` — turns a body in place. The `of=` reference the matrix writes is
    an `anchor` ENUM (activator|attacker|victim), matching `VELOCITY`: the DSL
    carries exactly ONE target slot per effect (`ContentParse` reads `who` and
    nothing else), so a second selector slot would have been a grammar change, and
    the three combat roles are the references an activation actually has. The
    recorded consumer authors `of=@Self`, which is `anchor: activator`.
  - `FALL_SHIELD` — one-shot fall cancel on an arbitrary player, spent AHEAD of the
    FALL walk so the landing is genuinely erased rather than reduced.
  - `VULNERABILITY` — the fragility mark, consulted on BOTH damage paths
    (`CombatDispatch` outside the defence-walk gate, `TriggerDispatch` on both
    environmental paths) so "every source" really is every source. Folded as a
    NEGATED reduction, not an outgoing bonus: the outgoing bucket is multiplied by
    `combat.attack-scale`, so a +100 % mark would land as +500 % on this pack.
    Non-stacking, tie-keeps-the-incumbent; the lapse notice is scheduled at the arm
    and matched on the expiry tick so a refresh silences the older arm.
  - `DEFENDER_KEYED_SUPPRESSION`, shipped as the head **`SUPPRESS_INCOMING`** (the
    primitive name describes the mechanism; the head names the authored act, beside
    `SUPPRESS`/`SUPPRESS_IMMUNE`). Same interned scope/key, parallel namespace on the
    HOLDER, consulted at gate 5 with the activation's victim id, `chance` rolled at
    the CONSULT. The erase stage lowers the new head through the SAME scope/key
    bridge, and the fuzz gate + spec-conformance exemption follow it there.
    ~~**Known partial:** the consult is per ACTIVATION, keyed on the activation's
    primary victim.~~ **CLOSED in 2d.2** (owner ruling R-v): a second consult runs
    inside `AbilityExecutor`'s target loop, so a chain hop onto a protected bystander
    is skipped the way the primary victim already was. The two do NOT merge — gate 5
    stops the whole activation (releasing the cooldown, firing the cue, recording
    SUPPRESSED), while a per-target veto can only remove a body, so the ability stays
    ACTIVATED with the cooldown it really did arm and `/se why` keeps saying so. The
    loop skips the ACTIVATOR (a window says what OTHERS may aim at its holder; filtering
    them would cut a mask wearer out of their own `who: @Self` heal) and the PRIMARY
    VICTIM (gate 5 already drew for them, and a second draw would square a partial
    mask's block rate). `chance` therefore rolls per TARGET APPLICATION now, not per
    activation — identical for every single-target consumer, and the doc line says so.
    The emptiness test is hoisted out of the loop, so a server with no window anywhere
    pays two field reads on an activated ability and never enters it. The GATELESS
    rebound path (`runForced`) is deliberately excluded: it walks no gates and its
    Activation carries no random-backed roll supplier, so a partial window there would
    block deterministically rather than roll.
  - `STATUS_CLEAR` (+ `%status.teleblock%`) — lifts a named engine window
    (TELEBLOCK / POTION_LOCK / DISARM). The wire codes live in
    `engine.sink.StatusKinds` beside `ArmorSelect` so the sink never depends on the
    effect package. The paired fact ships with it because a clear alone is half the
    item: the "you must be affected by X" refusal was inexpressible.
  - Asymmetric `@Bore` extents (`left`/`right`/`up`/`down`, `-1` = the symmetric
    half) — a `-half..+half` loop always spans an ODD number of blocks, so the 4x4
    and 6x6 rungs of the excavation ladders were unreachable at any half value.
    This closes the Atomic Detonate stop the ledger records, and **R-QC5 authors
    it**: L1 `{left=1, right=2, up=1, down=2, depth=4}` and L3 `{2, 3, 2, 3,
    depth=6}`, so no rung of that enchant is inert any more.
  - `BREAK_BLOCK.void-materials` — the per-block exception to `drops`, read on the
    region thread that owns each block (only it knows what the block is). This is
    the void-drops rider, NOT the volume-drop transform surface (below).
    **AUTHORED by R-QC7** on BOTH Detonate grades — the 7 bulk materials
    (COBBLESTONE, STONE, GRAVEL, DIRT, GRASS, SANDSTONE, NETHERRACK). The jar
    voided them on the heroic grade only and only when they did not sell; Auto
    Sell is out by R9, so the list is unconditional, and the base-grade extension
    is recorded as D-06-19. One authoring fact travels with it: the grass chain is
    written `GRASS_BLOCK|GRASS`, MODERN-first, against the pack's usual
    `1.8|modern` order — 1.8's `GRASS` is the grass block but the modern one is
    the plant, so a 1.8-first chain would void tall grass everywhere.
  - `EXP_MULTIPLY`'s doc line, which still said EXP_GAIN only across three surfaces
    after #291 made it live on MINE. Both triggers are named now, with WHY they
    round differently (EXP_GAIN rounds an already-granted amount; MINE truncates a
    whole-orb block yield).

- **Wave 2d ledger corrections.** `GHAST_FIREBALL→ENTITY_GHAST_SHOOT` and
  `WITHER_SHOOT→ENTITY_WITHER_SHOOT` both landed in wave 2c, but four places still
  read as owed: the struck line below records it correctly, the STILL-OWED list
  re-listed the ghast row (fixed here), and the `enchants/demonic-gateway` and
  `enchants/feign-death` prose still say the aliases are missing. Neither entry is
  blocked on a sound row; demonic-gateway's only stop is `TURRET_RING`.

- **Wave 2d STOPs — re-ruled at 2d.2 (owner ruling R-i) as a POLISH DECISION.**
  Both remaining rendering items are now one question, deferred together to a
  post-content polish decision rather than left as open STOPs: **packet seam vs
  roster-marker.** Seam = mint a `PlayerRender` era seam beside `PlayerVisibility`
  carrying real spawn/destroy/metadata packets on BOTH lanes (largest, correct, and
  the only shape that makes either item genuinely work). Marker = ship the affected
  content with its verbatim identity plus an authored note, the R9 shape. A
  soft-depend on a disguise plugin is the third option and was named at 2d.
  - `MOB_DISGUISE` (11, spectral) — **polish decision: packet seam vs
    roster-marker.** Rendering a player as a MOB to other clients is not reachable
    from any Bukkit surface: `PlayerVisibility` is one method,
    `setVisible(viewer, subject, boolean)`, with no entity-type or metadata axis.
    The modern lane has NO packet path and no ProtocolLib dependency; the legacy
    lane does (`LegacyDispatchSink.sendPacket`, `LegacyEquipmentRepaint`), so
    building it today would ship a disguise that works only on 1.8.9. The matrix
    flags it MAJOR era-risk. Not started, deliberately — a half-disguise is worse
    than none. Still current as written, and it now has its waiting content:
    `masks/spectral.yml` ships the marker shape with the parts that DO express
    authored live (the 20 t poll and the `%actor.y% > 200` gate), so the file takes
    one effect line the day the decision lands. Its helmet repaint is unaffected —
    that is a head texture, and it shares nothing with the whole-entity render.
  - `VANISH`'s DECOY half (07/12) — **polish decision: packet seam vs
    roster-marker.** The hide half **SHIPPED in 2d.2** (see below); what is deferred
    is the unequipped corpse that plays the death animation, plus — added when the
    pets batch closed — the window's **END FEEDBACK HOOK**. Nothing on the
    surface makes a player-shaped body — `SPAWN_ENTITY`/`SPAWN_SWARM`/`GUARD` all
    take an EntityType — so the corpse is the same greenfield packet work
    `MOB_DISGUISE` needs and rides the same decision. The `VANISH` effect ships
    WITHOUT `decoy`/`decoy-ttl` params rather than accepting and ignoring them, so
    nothing authored today has to change when the decision lands. The end hook is
    the smaller, INDEPENDENT half and does not need the packet answer: `VANISH`
    ends by four paths (timer, hit cap, quit, an explicit clear) through ONE shared
    restore closure, and there is no way to say "print this when it does". Two
    consumers today — `pets/feign-death`'s `§4§l* Feign Death - UNVANISHED *`
    (recorded, unauthored) and the GHOST-set Feign Death mastery (matrix/07) that
    shares the same window. The universal `pets.message-on-end` is NOT the answer:
    it is a bracket-`duration:` line, this pet opens no bracket window, and the
    cosmic pack blanks it (it would fire on refusal branches too).
  - ~~`PHANTOM_BLOCKS` (07)~~ — **SHIPPED in 2d.2** (owner ruling R-ii), as the
    `BlockVisibility` seam minted on the `PlayerVisibility` chain.

- **Wave 2d.2 — what SHIPPED.** Fourteen items.
  - **Field family.** `TempBlockLedger` gains an OWNER on each placement (owner
    ruling R-vi): per LAYER, not per tile, so one player's floor buried under
    another's keeps its claim and re-surfaces with it, and a same-material refresh
    re-owns (two identical floors are indistinguishable in the world). The claimant
    is the ACTIVATOR, never the effect's `who` — `TEMP_BLOCK` stamps its shape
    AROUND a target, and stamping that target would answer "whoever is standing in
    it". `%actor.ownedground%` reads the same block one below the feet that
    `%actor.groundblock%` does. `STACKING_DOT` re-asks whose ground is underfoot
    every pulse (a pulse spent off it costs nothing and adds nothing) and its ladder
    is PER VICTIM, shared across attackers, so overlapping fields ramp one ladder.
  - `BLOCK_FIELD_PROFILE` — fourteen inert-by-default params on `FALLING_BLOCK`
    rather than a second kind; `height` widened `0..12 → 0..64`, which was the
    actual stop (the +10 origin plus per-layer steps had no spelling at any value of
    the old range). The whole field is ONE intent, because the layer count, step,
    density and palette are all DRAWS and draws belong in the sink.
    `ThreadLocalRandom`, not `ScatterFill`: a coordinate-stable hash would make a
    re-cast field fall through the same holes every time. The re-hit ceiling lives
    on `FallingBlockCasts` (its javadoc already anticipated it), keyed by the VICTIM
    alone so a crowd of wearers shares one allowance, as a FIXED bucket, claimed
    LAST so a block that lands on nobody never burns a slot.
  - `DELAYED_STRIKE_FIELD` — the contract writes phase 1/2 as effect CHAINS and the
    DSL has no nested-effect param type, so the cues and payload are flat params on
    one kind (the `PERIODIC_DAMAGE` `tick-*` shape). Nothing measured was lost.
    Damage is `max(floor, health - amount)` rather than the recorded
    threshold-branched pair: one expression, and it cannot kill by construction.
  - `TURRET_RING` — per-SITE PvP through `ProtectionProvider.allows` (owner ruling
    R-iv), uncached by the service's own design, so N sites cost N walks. A volley
    CHAIN on each turret's own entity scheduler rather than a repeating region task:
    the period is jittered per volley, and a task anchored at the caster cannot
    reach a turret across a Folia boundary. Crystal invulnerability needed NO NMS
    work — both eras treat a zeroed non-living damage event as cancelled, so the
    matrix's flagged legacy hazard did not materialise. A new aimed-spawn path was
    needed (the existing launch is welded to the shooter's eye/look). One divergence
    recorded: the 1.21 line rescaled `setDirection`, so a fireball shot reads faster
    there than the authored speed. `ENDER_CRYSTAL → END_CRYSTAL` alias added.
  - `SUMMON_PURGE` — removes rather than converts, through the despawn path (no
    drops, XP or death event). The filter is a monotone exemption ladder; note the
    top rung SPARES an offline owner's summon. Only ownership-attributable summons
    are touched — a wild mob is nobody's summon.
  - `SUMMON_STRIKE_PAYLOAD` — the 5th `PHASE_*` rung. `HIGHEST` + `ignoreCancelled`
    (MONITOR cannot cancel, and a courier must not be spent on a blow a protection
    plugin already refused). Fires the OWNER's `IMPACT`. Melee-only falls out of the
    shape. Visuals stay out: a burst at the struck player is better said as a
    `PARTICLE` on the IMPACT abilities this rung already fires there.
  - **Pets.** `SOUL_COST_EXEMPT` at gate 10, ahead of the gem branch ("deducts none"
    cannot mean "still requires soul mode") and sparing the escalation ladder;
    `REMOVE_SOULS` is waived too (the other way an activation spends the actor's own
    souls, and it never passes gate 10), while theft from a victim's gems is NOT.
    `BOOK_RATE_MODIFIER` hooks both CONSUME paths, never the mint (a black scroll's
    rate is fixed when the scroll is made), and deliberately not inside the shared
    success helper, which also renders lore. `INVENTORY_CONVERT` ships the INTENDED
    cap-straddling direction (D-12-4/D-12-6): convert up to the budget, return the
    overflow. `ITEM_XP_TRACK` per owner ruling R-iii — the new wrapper carries the
    cosmic semantics (one level per grant, remainder banked, bank unbounded at cap)
    and the shipped `rollExp` is UNTOUCHED; the two coexist keyed by which path
    grants, and a test asserts they disagree on identical input. **Recorded unit
    divergence:** the `window` param is MINUTES, not the contract's milliseconds —
    `ItemStateStore` has no 64-bit surface, a seconds epoch overflows an int in
    2038, and a hi/lo pair adds a torn-write case to a counter that travels on
    traded items.
  - **Masks.** `PHANTOM_BLOCKS` via a new `BlockVisibility` seam on the
    `PlayerVisibility` chain, material-token shaped because 1.8 has no `BlockData`.
    The seam has TWO methods: reverting by MATERIAL is wrong on the legacy lane
    (granite is stone plus a data byte), so the real appearance is captured behind
    the seam — which also lets the resend run on the viewer's own thread. The revert
    re-reads the ground at close time rather than replaying an arm-time snapshot, or
    a block mined mid-window would be stranded as the new ghost. `ENDER_STONE →
    END_STONE` alias added. `VANISH` hide-only (R-i): store-backed window with ONE
    shared restore closure, because four paths end a vanish; a quit RESTORES rather
    than forgets (the hidden set lives on the watchers' connections). Only the
    attack side counts a hit, so taking damage cannot spend one. `SPAWNER_YIELD` as
    a worn CHANNEL, not an activation — nothing procs a spawner firing, and the
    intended behaviour is a per-spawn wearer test, which an armed window would turn
    back into the cache D-11-10 rejects. Grants do not stack.
  - `POTION_AMP_REDUCE` and `SUPPRESS_INCOMING`'s per-target refinement — both
    recorded in their own entries above.

- **~~Wave 2d.2 STOP — `WORN_COMPOSITE` (11, Multi-Mask).~~ RULED AND SHIPPED in wave 2e.2 (ADR-0074) — the PER-ITEM reading, built as the Multi Crystal shape; see the 2e.2 block below for what landed and why the costing above was wrong.** The only item of the pool
  not built, and it stops on a CONTRADICTION between two lines of its own matrix
  entry rather than on size. `11-masks.md:597` gives the primitive `params: children
  (list of mask keys)` — a DEF-level declaration. `11-masks.md:606` says it is
  "minted via the custom-item command with explicit child names", and the verbatim
  strings interpolate per-ITEM `{child1}, {child2}` into both the compound name and
  the ATTACHED stamp. If children are per-item then a `children` param on a def is
  meaningless (the def cannot know them) and the only surviving def-level param is
  the child cap. The two readings build different things. **Options for the owner:**
  (a) DEF-DECLARED — `MaskDef.children` + a `children:` root key in `MaskDefReader` +
  the N-way fold in `WornResolver` + the compound render; NO codec change at all,
  the helmet keeps one `maskKey`. Cost: one fixed Multi-Mask forever, the compound
  name and ATTACHED stamp become constants, and it contradicts `:606`. (b) PER-ITEM
  — everything in (a) plus a `CombatState` component and `CombatCodec` list label,
  `MaskCodec` (one PDC string today, with nowhere for a child list to enter),
  `ItemEnchanter.applyMask/removeMask` carry-through and detach-clean, a new mint
  entry point taking N child names with compile-time validation (this repo has NO
  custom-item command; `EraServices.customItem` is the third-party bridge),
  `MaskDefReader` taught that a container mask is legitimately ability-less (it
  errors `E_LOAD_MASK` on an empty ability list today), three positional
  `new CombatState(...)` sites in `ScrollService`, `MaskIllusionService.shownHeadFor`,
  `MaskBreakSalvage`, and pack content plus fingerprint goldens — realistically
  larger than the rest of the masks pool combined. (c) HYBRID — def declares the cap
  and base likeness, the item carries the children; (b) minus the reader work.
  **Three further rulings (b)/(c) need regardless:** how a composite is CREATED in
  play (`MaskListener` explicitly refuses mask-onto-mask today — "no merge, unlike
  crystals" — so it is a new command, a new gesture, or admin-mint-only); which head
  a composite's illusion shows, since `shownHeadFor` picks one `def.head()` and a
  composite has N; and whether breaking or salvaging a composite returns the
  composite or its children as separate masks.

- **Wave 2e — what SHIPPED (ADR-0073).** Seven items; the first three are one
  surface and close the batch's critical row.
  - **The per-piece surface, on `SetDef.Member`** — `lore`, `enchants`, `color`,
    `heroic`. Not a fifteenth `items/` type: `items/` holds one universal likeness
    per FAMILY (the crystal, the mask, the pet), never one file per instance, and a
    per-piece roster is compiler data on the wrong side of the "items config never
    reaches the compiler" line. Lore and enchants REFINE the shared ones rather than
    replace them, and all four keys are absent on every set authored before this, so
    those sets mint and render byte-identically. Per-piece lore renders from state
    (ADR-0040) keyed on the item's gear KIND — `LEATHER_BOOTS` → `BOOTS`, which IS
    the slot name for anything a set can mint — so nothing is stamped on the item and
    the codec is untouched; `SetLore.armor(setKey, slot)` is a default method, so a
    lookup written before this keeps answering.
  - **Mint rolls** — `{ min, max }`, `{ nearly-maxed: M }`, and a `chance` gate over
    either. `EnchantRoll` is pure data in `se-compile` (RNG-free); the draw is
    `feature.apply.SetMint` over the injected `Rolls`. The image-fixture importer
    takes the top of the band instead, because generated art must not depend on a draw.
  - **`heroic: true` at mint**, through `HeroicStamp` — the upgrade gesture's own
    stats write with none of its economy (no roll, no consumed item, and deliberately
    no material swap, since an authored piece already names its final likeness). Both
    callers share one instance wired at the composition root. The per-slot heroic
    LADDER stays deferred with the second heroic tier; a minted piece takes the pack's
    single configured tier, which is what `items/heroic.yml` can express.
  - **`PROC_REBOUND`'s levelless reading**, **the never-weaken defender merge**,
    **`%actor.setweapon%`** and **`STATUS_CLEAR`'s `FREEZE` rung** — each recorded in
    its own row above.
  - Plus the two legacy sound rows, which also close `enchants/virus` and
    `enchants/tombstone`.

- **Wave 2e.2 — what SHIPPED (ADR-0074). The engine remainder is DRAINED.**
  - **`WORN_COMPOSITE`, ruled PER-ITEM and built as the Multi Crystal shape.** The 2d.2
    costing above priced option (b) as "larger than the rest of the masks pool combined"
    on the premise that per-item children need a `CombatState` component, a `CombatCodec`
    list label and three positional-ctor sites. They do not. Crystals already answered
    "N identities on one item": pack them into ONE entry string. A helmet's existing
    `maskKey` holds `"masks/a+masks/b"`, a plain key has no delimiter, and so the codec,
    the record's arity and its ~30 construction sites are all untouched — a pre-composite
    mask decodes as the single mask it always was. `MaskItemData` is `CrystalItemData`'s
    twin over a shared `KeyEntries` packing, and `MaskDefReader` needs no change at all,
    because there IS no container def: "Multi-Mask" is a likeness (`name-multi` on
    `items/mask.yml`), exactly as "Multi Crystal" is. The three attendant rulings landed
    as the owner set them — creation is the mask-onto-mask gesture (the crystal merge,
    capped by a new `masks.max-merge`), the illusion wears the FIRST child's head, and
    extraction is whole-entry with the Item Extractor splitting a composite's topmost
    child through a `MaskSplitter` seam (the `ReforgeExtractor` arrangement). The jar's
    generated compound name / ATTACHED stamp are NOT reproduced: those are its wording,
    and our own universal likeness owns that string.
  - **`IMPACT` source scoping**, on the group-shared identity as narrowed. All three
    feeders carry it — falling blocks in the `Cast`, turrets in the volley chain's own
    re-arming closure (**corrected in the 2e.3 verification:** the group is NOT on the
    emplacement — `TurretCasts.bindTurret` stores none and says so; `scheduleVolley`
    re-passes the captured int to every `fireVolley`/`bindShot`, so a turret that outlives
    its activation still scopes every shot, and a per-shot registry read would fail OPEN),
    summon couriers on `SummonFlags` (the strike rung forgets the registries
    before it dispatches). `-1` stays unscoped, so nothing authored today changes.
  - **`%status.freeze%`**, actor-scoped, reading `FrozenTargets`' TICK-budget liveness.
    **The row's premise was wrong and is corrected here:** only TELEBLOCK ever had a guard
    fact — POTION_LOCK and DISARM have none either, and stay unbuilt because nothing asks.
    No victim reading ships: `STATUS_CLEAR` on a target is already idempotent, so a re-hit
    unfreeze needs no guard to ask permission of.
  - **`{RELATION_COLOR}` on `MESSAGE`**, with its two colours in `ally-color`/`enemy-color`
    params — the wave-1e rule (bindings in the parameter, never in the text), so the DT
    freeze line stays byte-verbatim. It could not ride `tokens`, which is numeric end to
    end, nor live in `Colors.translate`, which cannot see who is reading. Post-merge fix:
    the presence scan looked only for the underscore spelling while `Tokens.sub` fills the
    hyphen alias for free, so an authored `{RELATION-COLOR}` reached chat as a literal —
    the scan covers BOTH now, and either spelling is safe to author.
  - **`dy` on `SOUND` and `PARTICLE`** — the "+4 Y" cue anchor, resolved in the sink for
    the entity branch (ADR-0043 forbids the effect reading a target's position) and inline
    for the location one. It TRANSLATES the anchor; `spread-y` only widens a scatter.
  - Plus **the legacy alias sweep, CLOSED** — see that section below.

- **What a 2e.3 carries.**
  - ~~The CONTENT lift these surfaces unblock: the recorded rosters and probability rolls
    onto wave 2e's roll forms, Yijki's `×1.25` revenge onto `%actor.setweapon%` so one
    roll carries both rates, Dimensional Traveler's re-hit unfreeze onto the `FREEZE`
    rung, its `+4 Y` cues onto `dy`, and its freeze line onto `{RELATION_COLOR}`~~
    **ALL DONE** — the cue/relation halves landed with the 2e.3 lift (the two rows above
    are closed, each with why the render is unchanged), and the roster/Yijki/unfreeze
    halves land with the pets+setlift PR: all twelve sets are on the members, every
    enumerated roster is authored, and the re-hit unfreeze runs as `STATUS_CLEAR` inside
    the shift payload. One residue that lift recorded in-file: a mint roster's `chance`
    is an INTEGER percent and this family's probabilities are full of half points, so
    those entries stay comments until a fractional chance is worth a knob.
  - ~~The `group:` lines that USE `IMPACT` scoping — `sets/dimensional-traveler.yml`'s arm
    and payload, and `enchants/tombstone.yml`'s.~~ **HALF DONE, and the other half is a
    STOP.** `sets/dimensional-traveler.yml` declares `group: "dimensional-shift"` on the
    arm and on its `IMPACT` payload, which closes the recorded bug outright: a wearer
    carrying both this set and Tombstone no longer fires Tombstone's whole-set armour bite
    on each of the field's ~142 landing blocks. Tombstone's own anvil was already scoped —
    its `group: "mastery"` is non-null, so `runGrouped` filters on it — which leaves ONE
    residue, the coarse-identity collision ADR-0074 §4 records: an anvil landing still
    fires every other `mastery` enchant's `IMPACT` abilities. No other mastery declares
    one today, so it is latent rather than live.
    **Narrowing it to `group: "tombstone"` is refused, and the reason is a conflict, not
    an oversight.** `group:` is one ROOT key per enchant (`EnchantDefReader`'s
    `ROOT_KEYS`; neither `LEVEL_KEYS` nor `ABILITY_KEYS` carries it), so the payload
    cannot take a narrower group than the enchant, and the same interned string is a
    SUPPRESSION match key — `DefaultEraseStage` interns `cdScopeGroup` and every
    `SUPPRESS*` `key` into ONE `cooldownScopes` interner, which is what `SuppressionStore`
    matches at gate 5. Two live authored windows name it: `sets/dragon-slayer`'s
    `SUPPRESS_INCOMING{scope: GROUP, key: mastery, chance: 25}` (its "25% Mastery Enchant
    Negation") and `masks/rift`'s at `chance: 50`. Renaming would silently remove Tombstone
    from both. ~~**Owner ruling needed**~~ **RULED (R-QC40) and BUILT in wave 3b-1, as BOTH
    shapes at once** — they turned out to be one build, not two. `EnchantDefReader`'s
    `ABILITY_KEYS` now takes a per-ABILITY `group:` (shape a), and it feeds a SECOND
    identity, `Ability.sourceGroup` (shape b), which the `IMPACT` filter and
    `EffectCtx.sourceGroup()` read while gate 5 keeps matching on `cdScopeGroup`. Shape (a)
    alone would have been the trap this row names — the narrowed word would have moved the
    match key too. Both intern into the one `cooldownScopes` interner and an unauthored
    override erases to exactly the family id, so nothing else in the pack moved. What made
    the residue LIVE rather than latent was batch 2B: `rot-and-decay` and `demonic-gateway`
    each authored `IMPACT` payloads under the same family word, so a turret skull fired
    Tombstone's whole-set armour bite and both of Rot and Decay's branches. All three files
    now declare their own `group:` on arms and payloads and keep `group: "mastery"` at the
    root. ADR-0074 §4 carries the amendment.
  - **INTEGRATION ROW — the crystal route re-opens the same hole, and must land with the
    same group.** The set is not the only ARM this field can have. **SATISFIED at the pets+setlift rebase.** The 2e.3
    verification read this as pending against main; the pets+setlift branch had in fact
    authored the rider in the same sitting as its set (the 0.2 %-per-piece roll, the whole
    payload copied number-for-number), and the rebase applied the pre-condition before it
    landed: the rider's DEFENSE rung declares `group: "dimensional-shift"` — the same
    interned string as the set's arm and payload, so a crystal-armed landing scopes to the
    shift group instead of firing the wearer's whole `IMPACT` roster — and its two cues
    carry `dy: 4` with the freeze line on `{RELATION_COLOR}`, per that file's own rule that
    if either copy is retuned both move together. A wearer carrying the crystal WITHOUT the
    set has no `IMPACT` ability in the group and keeps the recorded gap the crystal file
    names, unchanged.

- **Found by the 2e.3 claims sweep — the `SOUND has no who` class, TWELVE files.
  EXECUTED under R-QC55 (QC batch 2a).** Twelve
  cosmic-pack entries drop a recorded PER-TARGET cue on the premise that `SOUND` cannot
  address one, and coarsen it to a single copy at the activation location:
  `enchants/teleblock`, `enchants/unfocus`, `enchants/healing`, `enchants/virus`,
  `enchants/cowification`, `enchants/eagle-eye`, `enchants/arrow-break`,
  `enchants/paradox`, `enchants/lifebloom`, `enchants/smoke-bomb`, and — **added by the
  2e.3 verification, which re-grepped the corpus rather than the list** —
  `enchants/longbow` and `enchants/piercing`. Those last two hide behind a different
  wording ("the effect has no recipient param", "SOUND has no per-target form") and were
  both touched by this same sweep's alias pass, so grep the PREMISE, not one phrasing.
  The premise has been
  false since wave 1e: `SOUND` takes a `who` target slot (default `@Here`) and plays at
  each resolved entity's own position — `enchants/teleportation`'s era note says so and
  authors it, which is what makes the other twelve a DRIFT rather than a ruling. Wave 2e.2's
  `dy` completes the pair, so an overhead per-target cue is now one line. NOT swept with
  the alias claims, deliberately: correcting these is a CONTENT change per file (an
  authored `who:`, and for several a second recorded volume), each with its own fidelity
  call about whether the jar's copy was per-listener or merely per-position — which is a
  lift to rule on, not a comment to fix. Two of the twelve also carry a per-listener VOLUME
  the anchor cannot express (`lifebloom` 1.5, `smoke-bomb` 1.6): `who` moves where a cue
  plays, never who hears it, so those two stay coarsened whatever is ruled.
  **The per-file rulings, all twelve:** three cues MOVED — `paradox`'s eat cue onto the
  heal's own ally set, `healing`'s chime and `piercing`'s arrow cue back onto the shooter
  (both jar-recorded at the shooter's own location, where a bow impact activates at the
  victim). Two stay coarsened as predicted, `lifebloom` and `smoke-bomb`, and for a reason
  stronger than the volume: each jar cue is a SECOND copy of a sound id the entry already
  plays, and ADR-0066 emits an id at most once per event sink, so an authored second copy
  would never play at all. The remaining seven — `teleblock`, `unfocus`, `virus`,
  `cowification`, `eagle-eye`, `arrow-break`, `longbow` — needed no `who`: the jar's
  position was already the activation point in every case (`victimEntity.getLocation()` on
  both combat sides, the shooter on a launch ability), so only the false premise and the
  residue were wrong in writing. The residue on all twelve is the same and is now stated
  per file: every one of these was a private `player.playSound`, and `who` moves where a
  cue plays, never who hears it, so each ships world-audible.

- **Found by the 2e.3 VERIFICATION — the same shape again, on `PARTICLE`: the "cannot be
  raised" class, SEVENTEEN files. EXECUTED under R-QC55 (QC batch 2a).** The sweep above caught the premise on `SOUND` and
  stopped there. It sits on `PARTICLE` too, in seventeen entries, each dropping a recorded
  anchor — victim/attacker EYE height, `+1 y`, `+0.5 y`, or Devour's victim FEET — on the
  reading that a per-target burst "centres on the body" and nothing can move it:
  `enchants/bidirectional-teleportation`, `enchants/corrupt`, `enchants/devour`,
  `enchants/epicness`, `enchants/farcast`, `enchants/hex`, `enchants/lethal-sniper`,
  `enchants/nature-wrath`, `enchants/rage`, `enchants/reflective-block`, `enchants/sniper`,
  `enchants/soul-trap`, `enchants/spirit-link`, `enchants/teleblock`,
  `enchants/teleportation`, `enchants/virus`, `enchants/voodoo`. Wave 2e.2 put `dy` on
  `PARTICLE` as well as `SOUND` (`range(-16, 16)`, and `ParticleEffect` hands it to the
  ENTITY branch, not only the location one), so every one of those anchors is now one
  authored number — the negative ones included. Deferred on the same ground as the `SOUND`
  class, plus one extra fidelity caveat: `dy` TRANSLATES by a flat distance while "eye
  height" is per-entity, so an entry whose burst can land on mobs as well as players is
  picking a player-sized approximation — a per-entry call to rule on, not a comment to fix.
  **The arithmetic the rulings turn on, and the reason six of the seventeen took no number.**
  The entity branch does NOT anchor at the feet: `ModernDispatchSink.midBody` is
  `getLocation() + height/2` — 0.9 on a standing player — and the legacy sink uses a flat
  `+1.0` (1.8 has no `getHeight`). So a recorded anchor becomes `dy = anchor − 0.9`, and the
  eleven that moved are: eye height → `dy: 0.7` (`corrupt`, `epicness`, `hex`,
  `lethal-sniper`, `rage`, `sniper`, and `bidirectional-teleportation`'s grapple arm),
  feet `+0.5` → `dy: -0.4` (`teleportation`, `bidirectional-teleportation`'s ally arm,
  `spirit-link`'s wearer heart), eye `+0.35` → `dy: 1.07` (`spirit-link`'s ally heart), eye
  `+0.5` → `dy: 1.2` (`voodoo`), and victim FEET → `dy: -0.9` (`devour`). The six that took
  NO number are the whole `+1 y` sub-class — `farcast`, `nature-wrath`, `reflective-block`,
  `soul-trap`, `teleblock`, `virus` — where the body centre already meets the record within
  ~0.1 blocks (0.9 modern, exactly 1.0 legacy), so a flat correction would only trade the
  modern lane's 0.1 for the legacy lane's; `nature-wrath` additionally fires into a MOB set
  whose per-entity heights no flat offset can follow. Each file states its own residue.

- **What a 2d.3 carries.**
  - ~~`WORN_COMPOSITE`, once the reading above is ruled.~~ **SHIPPED in wave 2e.2** (ADR-0074).
  - ~~The volume-drop TRANSFORM surface.~~ **SHIPPED in wave 3b-3 as option (a) (R-QC10 + R-QC22a).**
    `BREAK_BLOCK` gained `smelt` + `smelt-materials`, threaded into
    `sink.breakBlock(loc, drops, voided, smeltCount, smeltable)` so the transform is decided on
    the region thread that owns the block — the same reason `void-materials` is read there. Two
    departures from the recommendation as written, both smaller rather than larger: `smelt` is an
    INT (products per smeltable block, 0 = off) and not a boolean, because the jar's own rule is a
    COUNT (`setAmount(autoSmeltLevel)`) and because a numeric param takes a fact expression — which
    is what lets the Auto-Smelt-AND-Fuse co-enchant rule be one authored product instead of a second
    conditioned ability per rung; and `teleport-drops` was NOT added, because its only consumer would
    be Telepathy's in-volume routing (still deferred on its own row) and a volume's inventory write
    crosses regions in a way the MINE path never does. The recipe table moved to
    `platform.item.SmeltTable`, shared with `feature.combat.MineDrops`, so the two drop-transform
    paths cannot disagree about what an ore becomes. `smelt-materials` exists because the shared
    table knows sand and cobble, which the jar never smelted in a blast.
  - **FREEZE's no-jump knob — BUILT in QC wave 3c (R-QC57).** Exactly the shape designed below and for the
    reasons given: a `no-jump` FLAG on `FREEZE` threaded onto `FrozenTargets.Window` (an unconditional change
    would silently re-tune ice-aspect and the Dimensional Traveler field), read back by
    `FrozenTargets.blocksJump` — a SECOND read, never a widening of `isFrozen`, because frozen and
    ground-pinned are different states. The flag LATCHES on refresh, so a second consumer's plain FREEZE
    landing mid-window cannot release a pin the window already had. The listener is
    `FreezeJumpGuardListener` on the modern binding seam (`EraServices.freezeJumpGuard`, real on modern,
    `NoopListener` on legacy), one cancel on Paper's own event. `tombstone.yml` and `titan-trap.yml` author
    it and both files' "not shipped on either lane" notes are rewritten to the era split. The design record
    follows, unchanged: the mechanism question is
    settled and the answer is era-split, so recording it is worth more than half-shipping it. The
    amplifier-128 JUMP trick is ruled out pack-wide. On the MODERN lane there is a clean, exact,
    zero-cost mechanism: Paper's `PlayerJumpEvent` is cancellable and has existed since well before
    the 1.17.1 floor, so a listener that cancels it while `FrozenTargets.isFrozen(id)` is the whole
    feature. On the 1.8.8 LEGACY lane no such event exists (the overlay builds against CraftBukkit,
    not Paper), and every substitute is felt-wrong: a `PlayerMoveEvent` velocity test rubber-bands
    the victim and pays for itself on the hot movement path, and a per-tick velocity write jitters.
    So the shape is modern-only with a documented legacy degradation, which is exactly what FREEZE
    already does for its powder-snow visual. What it needed to land: a `no-jump` flag on `FREEZE`
    threaded onto `FrozenTargets.Window` (an unconditional change would silently re-tune every
    existing FREEZE consumer — ice-aspect, the Dimensional Traveler field — not just the two that
    want it), plus an overlay-only listener wired through the modern binding seam. Both landed.
  - `PROC_REBOUND`'s gateless `runForced` path still bypasses defender-keyed
    suppression: its `Activation` carries no random-backed roll supplier, so a
    partial window there would block deterministically rather than roll. Recorded in
    2d.2 as an untouched pre-existing hole, not a regression.

- **Wave 2 critical path:** SUMMON_PAYLOAD (5 consumers above + sets/masks later),
  ESCALATING_SOUL_COST, PROC_REBOUND — **all three SHIPPED and drained.**
  PROC_REBOUND closed the last pair (`enchants/enchant-reflect` +
  `enchants/heroic-enchant-reflect`); wave 2b's `SOUL_MODE_DISABLE` closed
  `enchants/soul-trap`. One authoring note the effect's own docs do not carry:
  its `tier-min`/`tier-max` are compared against the incoming enchant's rarity-tier
  WEIGHT (`TierRegistry.Tier.weight()`), so the bands are 0..50 / 10..70 / 80..80
  on this pack's ladder — the surface's `tier-min: 6, tier-max: 7` example reads
  as rung numbers and would answer for nothing on any real ladder.
- ~~**Wave 2c queue.** `ProcReboundEffect`'s own documentation contradicts the
  contract it ships~~ **FIXED in wave 2c.** The class javadoc told an author to
  "author mastery as `8..8`, heroic as `6..7`, normal as `0..5`" and the
  `.example(...)` spelled `tier-min: 6, tier-max: 7`, but the bands are compared
  against the incoming enchant's rarity-tier WEIGHT (`TierRegistry.Tier.weight()`),
  so a literal `6..7` band answered for nothing at all — the trap both reflect
  files had to be authored around (rows above). Both now name the weight
  explicitly and spell this pack's ladder (`0..50` / `10..70` / `80..80`), and
  the javadoc states WHY the grades overlap: the tier-min precedence rule is
  what makes an overlapping chain exclusive. Doc-only, no behaviour;
  `dsl-reference.md` and `authoring-surface.txt` regenerated.
- **Wave 1f (small):** ~~victim worn-enchant-level fact and its actor-side twin~~
  **SHIPPED** as `%victim.enchlevel.<stem>%` / `%actor.enchlevel.<stem>%`, keyed
  on the file stem and reading a level of 0 for an enchant nobody carries. It
  closed the Metaphysical/Sticky consumers and Silence's co-held Solitude (which
  also retired the `SET_VAR` teardown that row wanted — an unheld sword just
  leaves the worn state), and left Corrupt's held-Inversion carve-out and
  Explosive's priority check as drop-ins. It did NOT close Hero Killer:
  WORN_GEAR_FACT is a worn-armour CLASSIFICATION and piece COUNT, not one
  enchant's level, and no fact family counts pieces — ~~still open~~ **closed a
  wave later** by `%victim.heroicpieces%`, and the file is authored (row above);
  a state-tied (unbounded) `MOVEMENT_SPEED` grant that lives until the counter
  feeding it clears, plus a player-side var death clear — the two halves of
  Bleed's player slow, and what the matrix's STACK_COUNTER "derived-modifier
  hook" actually asked for beyond the counter itself (`SET_VAR op=increment`
  shipped that half in wave 1);
  ~~`%item.durabilitypercent%`~~ **SHIPPED**, on the `ITEM_DAMAGE` context alone
  (the read is taken there off the exact damaged stack; every other trigger,
  `EQUIP_CHANGE` included, leaves it NaN and the slot cleared to 0) — which is
  why `enchants/repair-guard` is authored on that trigger rather than the
  matrix's unequip transition (D-02-15);
  ~~MODIFY_FOOD `mode=absolute`~~ **SHIPPED** (Nutrition still waits on the EAT
  half alone); EAT worn-scan ruling; ~~per-block cooldown-scope opt-out~~
  **SHIPPED** as the per-ability `cooldown-scope: none`, which drops that block's
  enchant scope so it neither blocks on nor arms the shared bucket — authored on
  batch 06's `guided-rocket-escape.yml`, and in batch 7 on the two files this
  bullet named: `enchants/spirits`' payload pulse (its whole heal half) and
  `enchants/rocket-escape`'s FALL companion (which was cancelling nothing, so the
  escape ended in the fall damage it had just saved you from). Still outstanding
  for the same reason: Plague Carrier / Self Destruct express their re-arm
  without `cooldown:` and could take the knob whenever their own payload row
  moves;
  a payload whose abilities are pinned to the SPAWNING activation rather than
  re-read from the owner's live WornState, so a summon can outlive its owner's
  gear (Plague Carrier and Self Destruct both kill their wearer by design);
  GUARD/SPAWN_ENTITY per-level potion amplifiers; a TNT `fuse` param on
  SPAWN_ENTITY (Self Destruct's 5/4/3 s ladder — `ttl` despawns the charge
  instead of firing it);
  ~~PERIODIC_DAMAGE tick-cue params~~ **SHIPPED** as `tick-sound` /
  `tick-volume` / `tick-pitch` / `tick-particle` / `tick-particle-count`, and
  authored on Divine Immolation in batch 7 — ONE slot, so a pulse with two
  recorded bursts still drops the second (its LAVA×15);
  ~~PERIODIC_DAMAGE `replace` semantics fix~~ **SHIPPED** — `armDotSuppression`
  masks the named DoT's damage CAUSE for the window and strips nothing, so the
  converted DoT's icon and particles survive as they do in the jar. Divine
  Immolation's `POTION(WITHER)` row is live and its comment is rewritten;
  ~~per-victim cooldown scope~~ **SHIPPED** as the ability/enchant-level
  `cooldown-per-victim: true`, and authored on `enchants/thundering-blow`, whose
  recorded 50 t IS keyed to the entity its activation strikes. It does NOT close
  Cleave or Mighty Cleave: their stamp is per SPLASH MEMBER, and the knob keys on
  the activation's own victim, not on the selector targets — those two still want
  a cooldown addressing a selector target, the subject gap `enchants/pummel`
  carries.
- **Block-context facts (batch 06, the mining family) — ALL THREE CLOSED in
  wave 2c.** `INTERACT_LEFT`/`INTERACT_RIGHT` now carry
  `PlayerInteractEvent#getClickedBlock` and anchor the activation LOCATION on it,
  so `%block.type%`/`%isblock%` populate and `@Here` is the clicked face rather
  than the clicker's feet (`enchants/obsidian-destroyer` unblocked). `EXP_GAIN`
  turned out to be the wrong door — `PlayerExpChangeEvent` carries no source of
  any kind, so no hook there can distinguish block XP — and the honest fix was
  the alternative this entry already named: `fireMine` now reads the sink's
  `expMultiplier()` and scales the block's own `ExpToDrop`, so `EXP_MULTIPLY` is
  live on MINE and `enchants/experience` authors as a MINE ability with no
  `%isblock%` condition at all (MINE *is* the block-sourced path). And
  `BLOCK_MATERIAL_FILTER`'s deny half shipped as `exclude-materials` on all four
  block-volume selectors, which is what both excavation entries were stopped on —
  `enchants/detonate` is now fully authorable; ~~`heroic/atomic-detonate` still
  wants asymmetric `@Bore` extents, and the tool-class sublists / void-drops
  riders are still owed (neither blocks the base entry)~~ — the extents and the
  void-drops rider both landed in wave 2d and are authored (R-QC5 / R-QC7); only
  the tool-class sublists are still owed, and they block nothing.
  **All four files are AUTHORED** in the drain that followed (`experience.yml`,
  `obsidian-destroyer.yml`, `detonate.yml` whole, `atomic-detonate.yml` at all
  four rungs since R-QC5), together with `hero-killer.yml` off the same wave's
  `%victim.heroicpieces%`. Two authoring facts the rows above carry and every
  future block-volume entry will want: a deny list of jar-era names is authorable
  against the floor-strict gate ONLY as per-entry `1.8|modern` FALLBACK CHAINS,
  since the compiler takes the first name the running version has; and a depth
  lottery needs no var — two ORDERED abilities (deep first at the roll, floor
  unconditionally) compose into "deep on a hit, shallow otherwise" because the
  floor volume is already air by the time the second runs.
- **Legacy-sweep alias rows.** THIRTEEN ARE DONE — ~~`DIG_STONE→BLOCK_STONE_BREAK`,
  `FIREWORK_LAUNCH→ENTITY_FIREWORK_ROCKET_LAUNCH`,
  `FIREWORK_TWINKLE2→ENTITY_FIREWORK_ROCKET_TWINKLE_FAR`,
  `FIREWORK_BLAST→ENTITY_FIREWORK_ROCKET_BLAST`,
  `CREEPER_HISS→ENTITY_CREEPER_PRIMED`, `PISTON_EXTEND→BLOCK_PISTON_EXTEND`,
  `DRINK→ENTITY_GENERIC_DRINK`, `ANVIL_BREAK→BLOCK_ANVIL_BREAK`,
  `ZOMBIE_PIG_ANGRY→ENTITY_ZOMBIFIED_PIGLIN_ANGRY`,
  `CHICKEN_HURT→ENTITY_CHICKEN_HURT`,
  `MAGMACUBE_WALK2→ENTITY_MAGMA_CUBE_SQUISH`,
  `GHAST_SCREAM→ENTITY_GHAST_SCREAM`, `WITHER_HURT→ENTITY_WITHER_HURT`~~ all
  landed in `Aliases.SOUND` (the "cosmic-port legacy sweep" block), each verified
  against both the 1.8 enum and the modern constant list before landing. Those
  cover Inversion, Vampire, Disarmor, Disintegrate, Divine Immolation, Epicness
  and Silence (batch 03), Plague Carrier and Eagle Eye (batch 05) — and Plague
  Carrier's `CREEPER_HISS` pairing is now verified, retiring the "unverified"
  caveat its entry row above carries.
  **THE STILL-OWED LIST IS CLOSED — every row below LANDED in wave 2e.2**, each key
  javap'd against the real Spigot 1.8.8 `Sound`/`Material` enums and each value grepped
  verbatim out of BOTH committed modern fixtures, all values mutually distinct so the
  reverse scan stays deterministic. `ZOMBIE_WOOD → ENTITY_ZOMBIE_ATTACK_WOODEN_DOOR`
  landed with them (Tombstone's second door cue, listed on that entry rather than here),
  and `ENDERMAN_TELEPORT` was already shipped in wave 2e — this list was stale for it.
  **RE-OPENED in wave 2e.3, with FOUR rows: the list was closed against ITSELF, not
  against the content.** A re-audit of every sound token `cosmic-pack` authors (42
  distinct, each checked against the real Spigot 1.8.8 `Sound` enum and both committed
  modern fixtures) found four that reach the 1.8 lane through neither a direct constant
  nor an alias row, because no row here ever listed them:
  ~~`FIZZ→BLOCK_FIRE_EXTINGUISH`~~ (Clarity, batch 01 — that entry's ONLY sound, so the
  scrub fires silently there, the same failure Permafrost and Soul Trap had),
  ~~`DIG_WOOL→BLOCK_WOOL_BREAK`~~ (Cactus, batch 01 — the cue `mighty-cactus.yml` already
  points at as the family's one piece of era debt),
  ~~`IRONGOLEM_DEATH→ENTITY_IRON_GOLEM_DEATH`~~ (Guardians and Spirits — Spirits' era note
  named the pair while listing only its ORB_PICKUP sibling as owed) and
  ~~`WITHER_DEATH→ENTITY_WITHER_DEATH`~~ (`sets/mother-of-yijki` — whose era note claimed
  the resolver carried both wither cues when only `WITHER_SPAWN` is there). Two of the
  four silence the SIGNATURE pack as well, which this audit scoped to cosmic-pack and so
  did not name: `BLOCK_FIRE_EXTINGUISH` is also `enchants/auto-smelt`'s cue and
  `ENTITY_WITHER_DEATH` also `sets/spooky`'s and `sets/reaper`'s, so the four rows buy
  back eight files (five cosmic, three signature), not five.
  **ALL FOUR LANDED in the legacy sweep**, era-verified rather than list-verified: each
  1.8 key re-javap'd out of the real Spigot 1.8.8 `Sound` enum
  (`FIZZ`=`random.fizz`, `DIG_WOOL`=`dig.cloth`, `IRONGOLEM_DEATH`=`mob.irongolem.death`,
  `WITHER_DEATH`=`mob.wither.death`, read off `CraftSound`'s own table), each modern value
  grepped verbatim out of BOTH committed fixtures, and neither key nor value colliding with
  a row already in the map, so the reverse scan stays deterministic. Recorded in each file.
  The sweep re-ran the audit the way this entry's lesson demands — walking the distinct
  sound tokens BOTH packs author against the 1.8.8 enum and the alias table — and found no
  fifth cosmic-pack gap: every other unbridged token is signature-pack and names a cue with
  no 1.8 ancestor at all. Re-walked after the pets batch landed (104 distinct tokens, 46 of
  them cosmic-pack `SOUND:` effect args), which added the six rows recorded below and
  nothing else. The lesson stands for whoever closes the next one: walk the
  CONTENT's authored tokens, not this list.
  The rows it covered, all now landed (kept for provenance — each names the content it
  was silencing):
  `ENDERMAN_SCREAM→ENTITY_ENDERMAN_SCREAM` (Soul Trap, batch 04 — the trap's ONLY
  cue, so the proc is silent on the legacy lane until the row lands; the entry's
  other era debt, the `WITCH_MAGIC`/`SPELL` particle pair, is already carried by
  `Aliases.PARTICLE` as `SPELL_WITCH→WITCH` and `SPELL→EFFECT`),
  `SPLASH→ENTITY_GENERIC_SPLASH` (Blessed, batch 04 — the 1.8-era sound name;
  note `Aliases.PARTICLE` already carries an unrelated `WATER_SPLASH→SPLASH`),
  `MYCEL→MYCELIUM` (Devour, batch 04 — a `Aliases.MATERIAL` row, not a sound:
  the block-crack burst carries the material through the same resolver and
  `MYCEL` is the 1.8.9 spelling),
  `ARROW_HIT→ENTITY_ARROW_HIT` (Piercing and Longbow, batch 05) and
  `HURT_FLESH→ENTITY_PLAYER_HURT` (Sniper, batch 05; Lethal Sniper rides the
  same row, batch 06) — matrix 05 marks both entries era-clean, but
  `Aliases.SOUND` carries neither pair today;
  `ORB_PICKUP→ENTITY_EXPERIENCE_ORB_PICKUP` (Healing, batch 05) and
  `FIRE→BLOCK_FIRE_AMBIENT` (Hellfire, batch 05) — both flagged on their matrix
  entries, neither in `Aliases.SOUND`;
  `ENDERMAN_HIT→ENTITY_ENDERMAN_HURT` and `EAT→ENTITY_GENERIC_EAT` (Teleblock,
  batch 05 — the EAT row is also owed by Paradox, batch 02, which flagged it and
  never got one),
  `PORTAL_TRIGGER→BLOCK_PORTAL_TRIGGER` (Unfocus, batch 05, matrix-flagged) and
  `ENDERMAN_TELEPORT→ENTITY_ENDERMAN_TELEPORT` (Virus, batch 05 — matrix 05 calls
  the entry era-clean, but the 1.8 constant drops the `ENTITY_` prefix like every
  other pair here). Unfocus also rides the `ARROW_HIT` row above and Teleportation
  the `ORB_PICKUP` one.
  `COW_IDLE→ENTITY_COW_AMBIENT` and `COW_HURT→ENTITY_COW_HURT` (Cowification,
  batch 05 — matrix 05 says the cow sounds "all exist in 1.8", which is true of
  the SOUND but not of the modern spelling the file authors);
  `GLASS→BLOCK_GLASS_BREAK` (Permafrost, batch 07 — the 1.8 spelling of the
  freeze cue, which is that file's ONLY sound, so the whole proc is silent on the
  legacy lane until the row lands; `BLOCK_GLASS_BREAK` IS in both modern
  fixtures, and the 1.8 side wants the usual confirmation against a real jar);
  `BAT_TAKEOFF→ENTITY_BAT_TAKEOFF` (Dodge, batch 01, and its heroic Ethereal
  Dodge, batch 06 — `dodge.yml`'s era line already calls this "a resolver alias",
  but `Aliases.SOUND` carries no such row, so BOTH files silently drop the cue on
  the 1.8 lane; the pair is the ordinary `ENTITY_`-prefix drop);
  `WATER→BLOCK_WATER_AMBIENT` (Titan Trap, batch 06 — the matrix flags the
  rename on the entry; `Aliases.SOUND` carries no row, and `BLOCK_WATER_AMBIENT`
  IS in both modern fixtures) and, on `Aliases.MATERIAL` rather than the sound
  table, `STATIONARY_WATER→WATER` (the same entry's head-height water cue: 1.8
  splits still from flowing water and the file authors the modern `WATER`, so the
  legacy lane places the flowing block or nothing until the sweep rules);
  `ZOMBIE_METAL→ENTITY_ZOMBIE_ATTACK_IRON_DOOR` (Bidirectional Teleportation,
  batch 06 — flagged on the matrix entry, still absent from `Aliases.SOUND`;
  ~~`WITHER_SHOOT→ENTITY_WITHER_SHOOT`~~ **LANDED in wave 2c**, verified against
  the real Spigot 1.8.8 `Sound` enum and both modern constant lists, alongside
  `GHAST_FIREBALL→ENTITY_GHAST_SHOOT` — so half the grapple's two-cue burst now
  plays on the 1.8 lane and the other half waits on the ZOMBIE_METAL row. The same entry also rides the existing
  `ORB_PICKUP` row for its ally blink; `reflective-block.yml`, batch 06, rides
  the `ZOMBIE_METAL` row too — it is that entry's ONLY cue, so the reflect lands
  silently on the legacy lane until the row does);
  ~~`GHAST_FIREBALL→ENTITY_GHAST_SHOOT` (`enchants/demonic-gateway`, batch 07)~~
  **LANDED in wave 2c** (`Aliases.SOUND`, era-verified against the real Spigot
  1.8.8 `Sound` enum and both committed modern constant lists), alongside
  `WITHER_SHOOT→ENTITY_WITHER_SHOOT`. Demonic Gateway's two firing cues are
  therefore NOT owed — the entry's only remaining stop is `TURRET_RING`. Wave 2d
  re-read this list and found the pair struck once (line 242) and re-listed as
  owed here: both are shipped, and the `enchants/demonic-gateway` /
  `enchants/feign-death` rows above still carry the stale "carries neither"
  claim in their prose;
  **`ANVIL_BREAK` collision — RESOLVED on `BLOCK_ANVIL_DESTROY`.** The row was
  settled by convention on `BLOCK_ANVIL_BREAK` (one spelling pack-wide, the
  batch-03 one) because both modern constants exist in
  `test-fixtures/handles/sounds-1.21.11.txt` and `sounds-26.1.2.txt`, so
  `ModernHandleEraTest` passes either and cannot arbitrate. The character
  question it left for the sweep is answered, and it answers AGAINST the
  convention: the two are different cues, so fidelity decides.
  Evidence, all four legs read off real jars rather than a rename table —
  1.8.8 `CraftSound` gives `ANVIL_BREAK` = `random.anvil_break`; 1.8.8's anvil
  step sound (`Block$3`) answers `dig.stone` for `getBreakSound()`, so
  `random.anvil_break` is NOT that era's block-break cue but its shatter;
  paper-api gives `BLOCK_ANVIL_BREAK` = `block.anvil.break` and
  `BLOCK_ANVIL_DESTROY` = `block.anvil.destroy`; and modern NMS puts
  `SoundEvents.ANVIL_BREAK` in `SoundType.ANVIL`'s BREAK slot (the mining break
  that inherited `dig.stone`) while `AnvilBlock.onBrokenAfterFall` fires the
  level event that plays `block.anvil.destroy`. So `BLOCK_ANVIL_DESTROY` is the
  descendant of the cue the jar named and `BLOCK_ANVIL_BREAK` a lighter,
  unrelated one — the settled row had the two lanes playing different sounds.
  The sweep flipped the row and all three authors together, keeping one spelling
  pack-wide: `eagle-eye.yml` (20 SOUND lines), `disarmor.yml` (8) and
  `disintegrate.yml` (4), each era comment rewritten with the reason so the
  spelling is not "corrected" back. `ANVIL_LAND` is untouched — its own row
  already maps to `BLOCK_ANVIL_LAND`, and both are `random.anvil_land`'s line.
  **The pets batch (12) adds six sound rows and rides two more.** Every sound in
  that codex is a 1.7/1.8 name and every pet file authors the modern spelling per
  pack convention, so each missing pair is a cue that plays on ONE era tree:
  ~~`DIG_SNOW→BLOCK_SNOW_BREAK`~~ (Stronghold Sell, Feign Death, Raid Creeper, Vile
  Creeper, Gaia — the widest of the six, and Gaia also rides it),
  ~~`LAVA_POP→BLOCK_LAVA_POP`~~ (Lava Elemental, Anti Teleblock, Evolution,
  Alchemist, Blackscroll, Enchanter, Banner),
  ~~`VILLAGER_NO→ENTITY_VILLAGER_NO`~~ (Anti Teleblock, Lava Elemental, Water
  Elemental), ~~`ITEM_PICKUP→ENTITY_ITEM_PICKUP`~~ (Alchemist, Banner, Blackscroll,
  Enchanter), ~~`NOTE_PLING→BLOCK_NOTE_BLOCK_PLING`~~ and
  ~~`VILLAGER_YES→ENTITY_VILLAGER_YES`~~ (Tesla, XP Booster).
  **ALL SIX LANDED in the legacy sweep**, held to the same bar as its own four: each
  key re-javap'd out of the real Spigot 1.8.8 `Sound` enum with its id read off
  `CraftSound`'s table (`dig.snow`, `liquid.lavapop`, `mob.villager.no`,
  `mob.villager.yes`, `note.pling`, and `random.pop` for `ITEM_PICKUP` — the one that
  changed noun rather than shape), each modern value grepped out of both committed
  fixtures, no key or value colliding with a row already in the map. Two of the six
  buy back the SIGNATURE pack as well (`BLOCK_LAVA_POP` and `ENTITY_ITEM_PICKUP`), and
  with them cosmic-pack's `SOUND:` effect args resolve on 1.8.9 in full: 46 distinct
  tokens, 0 unresolvable. Water Elemental rides
  the existing `SPLASH→ENTITY_GENERIC_SPLASH` row (Blessed, batch 04) and
  Tesla/XP Booster the existing `ORB_PICKUP→ENTITY_EXPERIENCE_ORB_PICKUP` one
  (Healing, batch 05). `ANVIL_LAND`, `ENDERDRAGON_GROWL` and `WITHER_SHOOT` are
  already in `Aliases.SOUND`, and both particle tokens the family uses
  (`EXPLOSION_LARGE`, `SPELL`) are already in `Aliases.PARTICLE`.
- **Pet head textures — the 1.8.9 fallback is PAPER (12).** The per-type skull
  texture is codex-side and was NOT captured by the matrix (the codex carries the
  profile, not the base64), so no pet file authors `head:` and all seventeen mint
  untextured. `PetService.mint` calls `heads.head("")`, gets `null`, and falls to
  `ItemFactory.buildItem(def.material(), Material.PAPER, …)` — on the modern lane
  the `PLAYER_HEAD` token resolves and the pet is a blank head, but `PLAYER_HEAD`
  does not exist on 1.8 (there it is `SKULL_ITEM:3`) and `Aliases.MATERIAL`
  carries no row for it, so **every cosmic pet mints as PAPER on 1.8.9**. Two ways
  out and they are not equivalent: a `PLAYER_HEAD→SKULL_ITEM` material alias fixes
  the SHAPE on both trees for every untextured pet (and mask) at once, which is the
  sweep's own kind of work; recovering the seventeen base64 profiles fixes the
  SKIN and is a codex question, not an engine one. The alias is worth landing
  regardless — an untextured head is the intended fallback, a sheet of paper is
  not.
  **SHAPE HALF LANDED in the legacy sweep, with one correction to this row and one
  residue.** The correction: `Aliases.MATERIAL` is the wrong map — `ItemFactory`'s
  mint path never consults it (it goes `Material.getMaterial` → `matchMaterial` →
  its own `LEGACY_FALLBACK`), so an `Aliases` row would not have fired at all. The
  row landed in `LEGACY_FALLBACK`, which is where the other newer→older mint
  degradations live and is dormant on the modern lane by construction. The residue:
  that path carries a Material and no DATA VALUE, and 1.8's `SKULL_ITEM` at data 0
  is the SKELETON skull — the player variant is `SKULL_ITEM:3`, which only
  `LegacyTexturedHeads` mints and which it refuses to build without a blob. So a
  1.8 pet is now a head-shaped trophy of the wrong species rather than a sheet of
  paper. Making it the right species is a seam question, not a table one: either
  `TexturedHeads.head` grows a blank-blob branch returning a bare `SKULL_ITEM:3`
  (it currently answers `null`, and `MaskService` shares that contract), or the
  profiles are recovered and the whole question dissolves. Recorded, not chosen.
  `MaskService` is unaffected either way — every cosmic mask authors a blob, so it
  only reaches this path if the reflection itself fails.

- **`PROJECTILE_LAND` on 1.8.9 — CLOSED by the legacy sweep, arrows only.** The
  trigger shipped modern-only (wave 2c, ADR-0044 seam `feature.compat.Projectiles`)
  because 1.8.8's `ProjectileHitEvent` carries only the projectile, and
  `EntityArrow` fires it BEFORE branching on the hit entity and before moving the
  arrow to the impact — so during the event neither the landing point nor the
  entity-vs-block discrimination is readable, and the leaf answered "no landing".
  Five cosmic enchants author the trigger (Cowification, Hellfire, Explosive, Venom,
  Infernal) and were silently half-dead on the legacy lane. The bytecode says the
  answer is one tick away rather than absent: the block branch writes the hit tile,
  pulls the arrow back along its flight, sets `inGround` and ends the tick at the
  impact, while an entity hit has already `die()`d the arrow. So the seam changed
  from "answer now or decline" to a callback, and the legacy leaf probes the arrow's
  in-ground flag on its own entity scheduler one tick on — no new abstraction, no
  tracking store, no hot-path work, and modern stays byte-identical (its four unit
  tests are unchanged). Residual, recorded rather than papered over: on 1.8 the
  trigger fires for ARROWS only. A thrown projectile or fireball is already `dead`
  when its hit event fires, so nothing survives to probe; covering those would need
  a per-projectile damage flag fed from a combat listener, which is a real tracking
  store for zero authored content. All five authors are bow enchants and
  `PROJECTILE_DRESSING` attaches a rider rather than replacing the arrow, so every
  authored case is an arrow. The five files' era comments are corrected.
- **The mask/crystal amethyst gesture cues — CLOSED as verified, unchanged.**
  `items/crystal.yml` and `items/mask.yml` author `block.amethyst_block.chime` /
  `block.amethyst_cluster.break`, 1.17+ handles, and both files claimed those
  "warn-skip on the legacy fork". They do not warn. Item-likeness cues are raw
  strings that no stage handle-resolves, so there is no load-time gate; at use time
  `feature.compat.Sounds` finds no constant of that name, the legacy key fallback is
  `KeySoundFallback.NONE`, and the cue is dropped with no log at all. The gesture is
  unaffected — the cue plays after the commit — so a 1.8.9 apply or remove is simply
  mute. The cues are KEPT modern rather than degraded to a 1.8 stand-in, which would
  change the sound everywhere the pack actually plays it; both era comments now state
  the verified outcome instead of the warn-skip claim.

- **~~`barbarian.yml`'s D-04-4 note contradicts `tank.yml`~~ — CLOSED against the
  codex: there is no deviation.** The note said the jar's `contains("_AXE")` gate
  "counts every PICKAXE holder as an axe holder". It cannot: `"DIAMOND_PICKAXE"` has
  one underscore and it sits before `PICKAXE`, so `_AXE` is not a substring — which
  is what `tank.yml`'s era line already asserted while shipping `contains` for the
  same suffix. The owner read the codex (`04-enchants-axes.md § Barbarian`, line
  386): the gate really is `getItemInHand().getType().name().contains("_AXE")`, and
  no Material name on either era carries `_AXE` other than terminally. So the
  authored `matchesregex ".*_AXE$"` selects exactly the measured set and D-04-4's
  premise was false. The row stays in `deviations.md`, struck and restated as a
  verified NON-deviation so nobody re-derives the claim; `barbarian.yml`'s note now
  says the regex is legibility, not a correction. `tank.yml` was right and is
  untouched.

- **STOP — the 1.8.9 lane has no era gate, and the signature pack cannot publish
  there.** Found while re-walking the content for the four alias rows above, and
  recorded rather than fixed because the fix is an owner decision.
  `ModernHandleEraTest` exists precisely because an unresolved handle is a
  **blocking** `E_UNKNOWN_HANDLE` — `Severity.ERROR.blocking()`, and
  `ContentReloader` publishes only when `!library.hasErrors()`, so ONE dead token
  costs the whole library, not one effect. That test has no legacy twin. Resolving
  every `SOUND:` effect token the shipped content authors through the production
  `HandleResolver` + `Aliases` against the real Spigot 1.8.8 `Sound` enum:
  `cosmic-pack` is CLEAN (46 distinct tokens, 0 unresolvable — this sweep's four
  rows, the pets batch's six and the anvil flip closed the last of it), the default
  catalogue had exactly
  one (`BLOCK_ANVIL_PLACE`, fixed here as a `FALLBACKS_1_8` lossy degradation to
  1.8's `ANVIL_LAND`, which is the constant that owns `random.anvil_land` and so
  cannot be an `Aliases` row), and `signature-pack` has **53 of 72** — beacons,
  bells, shields, vexes, witches, illusioners, the 1.9 attack cues. Every one of
  those refuses the signature pack's publish on a 1.8.9 server. It has never been
  caught because the legacy smoke compiles its own inline library rather than the
  shipped packs. The fork, for the owner: (a) downgrade an unresolved HANDLE to a
  non-blocking warn so the op skips and the library still publishes — a policy
  change that would also soften the modern gate this exact mechanism was built for;
  (b) author ~55 `A|B` chains or `FALLBACKS_1_8` degradations, most onto cues 1.8
  simply does not have; or (c) declare the signature pack modern-only and say so.
  Whichever it is, the missing piece is the same: a legacy-era twin of
  `ModernHandleEraTest` driven by a committed 1.8.8 constant fixture, so this class
  fails `./gradlew build` instead of a live `/se pack apply`. Out of scope for the
  port; owed by whoever owns the legacy lane.

- **Found by the masks batch (11) — three engine findings no content file could
  close.**
  - **ADR-0074 §4's mask/crystal scope identity does not exist.** The ADR
    describes the `IMPACT` filter group as "the conflict-matrix group for enchants
    and sets, **the bare def stem for masks and crystals**". No stage derives that
    stem. `MaskDefReader` and `CrystalDefReader` both take `cdScopeGroup` from an
    authored `group:` key alone (`ContentParse.blankToNull(node.string("group"))`,
    `null` when absent), and `DefaultEraseStage` interns `null` to `-1` — so every
    mask and crystal ability in this pack is UNSCOPED and runs the whole `IMPACT`
    roster, which is the exact behaviour §4 says it narrowed. No cosmic-pack mask
    or crystal authors a `group:` today, so nothing is misfiring: it is the
    doc that is wrong, not the runtime. LATENT until a mask or crystal arms a
    falling-block field, a turret or a summon courier — at which point the
    unscoped cast fires every other `IMPACT` author the wearer owns, the same bug
    the `sets/dimensional-traveler` row above closed from the set side. Fix is one
    of two: amend §4 to say masks and crystals scope only when they declare a
    `group:` (and let the crystal rider declare one, as its own pre-condition row
    already requires), or build the stem derivation the ADR claims. Note the
    interner is shared across scope kinds, so a derived stem would collide a mask
    and a crystal of the same name by construction.
  - **Interned group ids are captured as bare ints across a reload.** The three
    `IMPACT` carriers hold the scope as a plain `int` with no generation stamp —
    `FallingBlockCasts.Cast`, `SummonFlags`, and the turret volley's re-arming
    closure (which captures the id precisely because a per-shot registry read
    would fail OPEN). Interning is per-SNAPSHOT, and a transactional reload swaps
    the snapshot by reference while those casts are still in flight, so an id
    minted against the old interner is read against the new one: a mid-field
    reload can mis-scope a landing, a courier or a volley to whatever now holds
    that id, and `-1` is the only value that survives by luck. Nothing in play
    reaches it today (no reload runs mid-combat in the gate), and the blast radius
    is one activation's payload, but it is a correctness hole rather than a
    tuning one. Engine decision owed: stamp the snapshot generation beside the id
    and drop a cast whose generation is stale, or re-resolve the group by string
    at the consume site.
  - **`SpawnerYieldEffect`'s "never count against the spawner's own budget" claim
    looks wrong.** The effect's own `doc(...)` states the added copies "spawn as
    CUSTOM, so they never re-trigger this and **never count against the spawner's
    own budget**". The first half holds — a CUSTOM spawn reason cannot re-enter a
    `SpawnReason.SPAWNER` listener — but the second does not follow from it: the
    copies are placed with a real world spawn and become ordinary nearby entities,
    and a spawner's own `maxNearbyEntities` check is a RADIUS SCAN of live
    entities that knows nothing about how each arrived. So a wearer standing at a
    spawner should throttle it sooner, not never — the yield partly self-limits.
    `masks/bunny.yml` honestly repeats the shipped doc, so the file needs no edit
    if the doc is corrected. Owed: an engine/doc check — verify the scan against a
    real server on both eras, then either strike the clause or state the real
    interaction, and re-check the "grants do not stack" neighbour claim in the
    same pass.

## Matrix maintenance queue

**EXECUTED 2026-08-05 under QC ruling G5.** The matrix docs are on main at
`docs/dev/cosmic-port/matrix/`; the branch-isolation this section used to cite is
gone, so every correction below was applied to the named doc. Bullets are kept, not
deleted — they are the record of WHY each matrix line reads the way it does. Each
carries its outcome as a lead-in: `**APPLIED**`, `**PARTLY APPLIED**`, or
`**NO-OP**`.

- **APPLIED** — **`matrix/03` § Trap — two false engine claims.** The decomposition says the
  engine's timed `MOVEMENT_SPEED` modifier "restores the PRIOR speed, fixing
  the jar's hard-coded 0.2F restore clobber": it does not. Engine policy hands
  back the vanilla 0.2 default precisely so a re-fire can never ratchet speed
  upward, which reproduces the jar's clobber rather than fixing it. The numbers
  line then says overlapping traps refresh — "the timed modifier refreshes
  instead": there is no refresh path. Every grant registers its own timed
  revert, so the first revert ends a freeze a later grant re-armed. Both claims
  need striking on the matrix branch; `trap.yml` already carries the corrected
  reading.
- **APPLIED** — **`matrix/03` § Trap — the attacker-side entry guard.** Recorded as
  `!%trap.frozen%` read on the attacker ("the attacker reads their own copy").
  Superseded by owner ruling for the reason `deviations.md D-06-17` already
  gives on Heroic Titan Trap: it is the same wrong-subject idiom, and the
  intent is anti-re-trap. Authored victim-side; the matrix line should point at
  D-06-17.
- **APPLIED** — **`matrix/04` § Boss Slayer — the shipped boss list cannot be
  `%victim.mobtype%`.** D-04-7 rules the jar's inert boss flag out and hands the
  designation to "a pack-configured boss mob-type list expressed as
  `%victim.mobtype%` conditions". That fact is the MythicMobs soft hook
  (ADR-0027): with no integration installed it resolves to the empty string for
  every entity, so a `%victim.mobtype%` list would ship the enchant exactly as
  inert as the jar it is fixing. `boss-slayer.yml` expresses the list as
  `%victim.type%` over the vanilla bosses (ENDER_DRAGON, WITHER — both resolve
  on 1.8.9) and documents the `%victim.mobtype%` widening for MythicMobs
  servers. The matrix line should name the fact it actually needs.
- **APPLIED** — **`matrix/11` § Boss Mask (line 643) — the same unshippable boss
  condition.**
  The entry decomposes the designation as `%victim.mobtype% == {boss-id}`, which
  is the MythicMobs soft hook (ADR-0027) and resolves to the empty string for
  every entity on a stack without the integration — so the mask would ship as
  inert as the absent-plugin metadata flag it replaces, which is exactly the
  correction already recorded for `matrix/04` § Boss Slayer above. The shipped
  `masks/boss.yml` uses the same list its twin does,
  `%victim.type% == "ENDER_DRAGON" || %victim.type% == "WITHER"`, deliberately
  identical so the pack carries ONE boss designation rather than two. Both matrix
  lines should be corrected together, and both files already document the
  `%victim.mobtype%` widening for servers that do run MythicMobs.
- **APPLIED** — **`matrix/04` § Blessed — the `deepwounds` writer's subject
  contradicts itself.**
  The activation line says Deep Wounds "writes on its attacker … read here from
  the actor's own store", but `matrix/03` § Deep Wounds decomposes the write as
  `SET_VAR(name=deepwounds, …) @Victim`. Both halves cannot hold: a write on the
  Deep Wounds attacker would never reach the Blessed wielder's store. The shipped
  pair is internally consistent on the victim-side reading —
  `deep-wounds.yml` writes `who: "@Victim"`, `blessed.yml` reads the bare
  actor-side `%deepwounds%`, and the wounded player IS the later Blessed actor.
  The matrix line should say "on its victim".
- **PARTLY APPLIED** (the rungs below are now recorded in doc 05's corpus-wide
  notes as a marked editorial note; the full 22-entry appendix backfill still wants
  the appendix itself, which is not in-repo) — **`matrix/05` — the whole doc is
  missing its acquisition rows.** Docs 01–04
  end every `numbers` block with `Acquisition: max N, base X, interval Y,
  weight W, tier T`, and shipped files take their `tier:` (and their max level)
  from it. Doc 05 carries none — the backfill commit covered 01 only — so the
  bow batch has no recorded rung for any of its 22 entries. Authored files take
  max level from the per-level ladders (unambiguous) and carry forward the tier
  the pack already ships for that enchant name, noting it in-file
  (`pacify.yml` rare, `piercing.yml` epic, `snare.yml` uncommon,
  `sniper.yml` legendary, `hellfire.yml` uncommon — so `pacify`, `snare` and
  `hellfire` ship BELOW their appendix rung, tier 4 / 3 / 4). Backfill 05 and
  re-check those five against it.
  Where the pack ships no prior rung, the codex's own registration appendix
  (`# | display | class | pkg | tier | max | base | interval`) IS the missing
  data and is what the batch-05 drop-ins used, noted in-file: `virus.yml`
  uncommon (tier 2, max 4), `teleportation.yml` and `venom.yml` rare (tier 3,
  max 5 / max 3), `unfocus.yml` epic (tier 4, max 5), `teleblock.yml` soul
  (tier 6, max 5 — the one entry whose tier the matrix DOES state, and the
  appendix agrees). The backfill should copy that appendix wholesale rather
  than re-deriving 22 rungs by hand. The bow-family batch took the same
  appendix, in-file on each: `explosive.yml` and `cowification.yml` uncommon
  (tier 2, max 5 / max 3), `farcast.yml` rare (tier 3, max 5),
  `arrow-lifesteal.yml`, `eagle-eye.yml` and `dimension-rift.yml` epic (tier 4,
  max 5 / 5 / 4), `healing.yml` and `lightning.yml` common (tier 1, max 4 /
  max 3), `infernal.yml` rare (tier 3, max 3) and `longbow.yml` epic (tier 4,
  max 4). Every one of those maxes is corroborated by the entry's own
  numbers ladder, so only the rungs are appendix-only.
- **APPLIED** (renamed to `raged` on § Sniper and on § Pacify's do-not-conflate
  note) — **`matrix/05` § Sniper — the Rage-immunity key has no writer under that
  name.** The entry gates on `effectedByRage` "written by the sword enchant
  Rage", but `matrix/03` § Rage decomposes exactly one victim-side stamp,
  `raged` (4 t / 200 ms), and that is what `rage.yml` publishes and
  `execute.yml` already reads. Same window, same subject, so `sniper.yml` reads
  `%victim.var.raged%`; the matrix line should either name `raged` or say which
  writer is owed for a second key.
- **APPLIED** (the doc-wide sweep this bullet commissions ran over docs 02, 03, 05
  and 06: every gap block whose primitive has shipped now carries a **SHIPPED as
  `X`** clause, and the two stale gap-index tables — 03 and 05 — are annotated
  row-by-row) — **Batch-1 staleness pattern — closed gaps still marked open.** Gap blocks in
  `matrix/02` and `matrix/03` still read as open against a surface that has
  since shipped them: `VELOCITY_ANCHOR` (02 § Ragdoll) is the `anchor` param
  `VELOCITY` now carries; in 03, `VAR_SCALED_DAMAGE` was absorbed into
  EXPR_PARAMS, `TARGET_VAR_FACT` ships as `%victim.var.<name>%`, `COUNTER_VAR`
  as `SET_VAR op=increment` plus `%counter.<name>%`, and `PERIODIC_DAMAGE`,
  `HURT_TRIGGER` and `SPAWN_ORIGIN_FACT` (`%victim.fromspawner%`) all landed in
  wave 1. Authors must check each gap block against
  `docs/reference/authoring-surface.txt` before deferring on it — a gap block
  is a batch-1 snapshot, not a live statement. The matrix sweep should re-run
  that check across every doc, not just the two named here.
- **APPLIED** — **`matrix/05` § Hijack — two decomposition rows the surface cannot
  honour.**
  The gate is written `%victim.mobtype% == "IRON_GOLEM"`, which is the
  MythicMobs soft hook (ADR-0027) and resolves to the empty string with no
  integration installed — the same fact the Boss Slayer row above corrects;
  `%victim.type%` is what an iron-golem gate needs. And step 1,
  `CONVERT_SUMMON(radius=1, who=@Victim)`, cannot express "flip THIS summon":
  the effect is the Grand Bell (ADR-0071), player-targeted and ringed around
  that player, so a golem target resolves to nothing. That second one is why
  the entry is deferred rather than shipped (row above), not a wording fix.
- **APPLIED** (folded into the sweep above) — **`matrix/05` gap index — four gaps
  already closed.** `TARGET_RELATION_FACT`
  ships as `%victim.relation%` (ALLY/ENEMY/NEUTRAL), `RANDOM_RANGE_PARAM` as
  the `rand(lo, hi)` expression function (`floor(rand(L, 3L))` is Healing's
  integer roll), `DURABILITY_PIECE_SELECT` as `DURABILITY select:
  most-damaged`, and `SUMMON_REBIND_UPGRADE` as the `SUMMON_REBIND` effect —
  the batch-1 staleness pattern above, confirmed on doc 05.
- **APPLIED** — **`matrix/02` §§ Spirits and Undead Ruse — "no attacker-type fact
  exists" is wrong, and the widening it justifies is unnecessary.** Both entries drop the
  jar's player-damager requirement and note the widening to mob melee. On a
  DEFENSE pass the "victim" IS the attacker, so `%victim.type% == "PLAYER"` is
  exactly that fact — `guardians.yml` (batch 01, the same jar condition) has
  shipped it since the first content batch. Both files are authored with the
  gate; the matrix lines should drop the widening note.
- **APPLIED** (all nine rungs copied onto the entries) — **`matrix/06` § tool
  enchantments — the acquisition rows carry no tier.** The
  nine tool entries end on `max N; table weight W; item set …; base X, interval
  Y` and never state a rung, unlike docs 01–04 (and unlike the doc's own heroic
  entries, which all say tier 7). The rungs exist and the doc even prints them
  in its own preamble table, one line above the entries: `detonate` 4, `fuse` 4,
  `obsidiandestroyer` 2, `telepathy` 2, `skilling` 2, `haste` 1, `oxygenate` 1,
  `autosmelt` 1, `experience` 1. Batch 06 authored off that table
  (`auto-smelt.yml` and `haste.yml` common, `fuse.yml` epic), noting it in-file.
  The backfill should copy those nine onto the entries themselves.
- **APPLIED** — **`matrix/06` § Detonate — the `FACE_ORIENTED_BOX_SELECTOR` gap
  block is stale for the symmetric half.** It reads "TRENCH is a single perpendicular
  layer and TUNNEL a 1×1 line — no combination yields a 3×3×depth slab", which
  was true when the block was written and is not now: `@Bore{half-width,
  half-height, depth, materials}` shipped, is oriented on the mined face, and
  `@Bore{half-width=1, half-height=1, depth=D}` IS the entry's 3×3×D slab. Only
  the ASYMMETRIC extents (Atomic Detonate's `(1,2,1,2)`/`(2,3,2,3)`) and the
  per-block DENY filter are still open — the batch-1 staleness pattern above,
  now confirmed on doc 06. Both entries stay deferred on
  `BLOCK_MATERIAL_FILTER`, not on the selector.
- **APPLIED** (spades added; the ITEM-SET RULING now sits on the entry's
  interactions line) — **`matrix/04` § Reforged — the weapons-and-tools item set is
  missing the spades.** The entry spells the set as "5 swords + 5 axes + bow + 5 pickaxes +
  5 hoes", and `reforged.yml` faithfully ships `[SWORD, AXE, BOW, PICKAXE,
  HOE]`. The codex's own set table (doc 15) records `weapons_and_tools` as
  5 swords, 5 axes, bow, 5 pickaxes, 5 hoes **and 5 spades** (then the 5 axes
  a second time — 31 entries, 26 distinct), so the shipped file is one whole
  tool family short and Reforged never lands on a shovel. If the table is
  right, the matrix line gains the spades and `reforged.yml` gains `SHOVEL`.
  Same table is what batch 06 read `tools` (21) off: 5 pickaxes + 5 hoes +
  5 spades + 5 axes + FISHING_ROD, which is why `haste.yml` is
  `[TOOL, FISHING_ROD]` and not `[TOOL]`.
  **ITEM-SET RULING (owner, refining the batch-3 spell-it-out convention
  `obliterate.yml` records).** Take a COMPOSITE whenever it is an EXACT match for
  the jar's set — `ItemGroups` defines `TOOL` as pickaxe + axe + shovel + hoe and
  nothing else, so `[TOOL, FISHING_ROD]` is the canonical spelling of the 21-tool
  set (`haste.yml`, `oxygenate.yml`, `telepathy.yml`, and `atomic-detonate.yml`
  when it lands). ENUMERATE only where no composite fits — the weapons cases,
  where `WEAPON` would drag in the crossbow, trident and mace the jar never
  listed (`obliterate.yml` `[SWORD, AXE, BOW]`, `reforged.yml` above).
- **APPLIED** — **`matrix/06` § Master Inquisitive — the payout ability has no
  victim to read.**
  The decomposition gates the `EXP_GAIN` ability on
  `%victim.inquisitive-mark% >= 1`, but an XP-gain activation carries no victim
  (the marked mob is already dead) and no such fact family exists — the victim
  var spelling is `%victim.var.<name>%`, and it is unreadable there. The shipped
  file takes the base's three-rule chain instead: ATTACK writes the mark on the
  mob, KILL reads it back through `%victim.var.inquisitive.master%` and opens a
  40 t pickup window on the holder, EXP_GAIN spends that window. Same delta
  `inquisitive.yml` already carries (the jar paid out on ANY killer's blow; the
  window is the holder's own kill), so the two grades stay uniform. The matrix
  line should decompose the payout as a KILL → window → EXP_GAIN chain.
- **APPLIED** (folded into the sweep) — **`matrix/06` gap index — three more gaps
  already closed.**
  `ANY_DAMAGE_TRIGGER` (defined at Divine Enlighted, consumed by Guided Rocket
  Escape) ships as the `HURT` trigger — DEFENSE-direction, no target, every
  damage cause — which `enlighted.yml` has taken for the same jar behaviour
  since batch 01; `FLY_SPEED_PARAM` (Guided Rocket Escape) ships as `FLY`'s own
  `speed` param, restoring the PRIOR fly speed rather than the jar's hard-coded
  0.1; and `IMPACT_HEIGHT_VAR` (Lethal Sniper) ships as `%impactheight%`,
  already load-bearing on `sniper.yml`. `TARGET_SCOPED_VAR` was closed in
  wave 1 (`SET_VAR who=@Victim` + `%victim.var.<name>%`). The batch-1 staleness
  pattern, confirmed a second time on doc 06.
- **APPLIED** (folded into the sweep) — **`matrix/06` gap index — three MORE
  closed, on the tools/heroic batch.**
  `AIR_TICKS_RESTORE` (Oxygenate) ships as `FILL_OXYGEN`'s own `amount` param —
  "air ticks to add, clamped to the target's maximum air" is the gap block
  verbatim; its optional `skip-if-overflow` is not wanted, since D-06-6 rules
  the clamped top-up the as-intended reading. `FOOD_DRAIN_CANCEL` (Alien
  Implants) ships as `MODIFY_FOOD mode=cancel-drain`, which cancels hunger LOSS
  for a window and leaves gains alone — the jar's "only cancels decreases, so
  eating still works" exactly, re-armed on REPEATING per the effect's own
  authoring note. `PULL_IMPULSE` (Bidirectional Teleportation) ships as
  `VELOCITY mode=toward, anchor=activator` with an EXPRESSION `strength`: the
  recorded `clamp(distance²/50, 1.0, cap)` is authored literally, so only the
  jar's separate Y softening (`y × -mag/1.75`) is approximated, not the
  magnitude curve. `RELATION_VAR`, defined on the same entry, was already closed
  in wave 1b as `%victim.relation%`. Four more instances of the batch-1
  staleness pattern.
- **APPLIED** — **`matrix/06` § Bidirectional Teleportation — the ally branch
  cancels BEFORE it range-checks.** The decomposition orders the ally arm as "1. condition
  `%distance% <= 30` … else too-far string; 2. `CANCEL()`", which would let a
  too-far allied arrow deal its damage. The source cancels the event and removes
  the arrow first and only then compares `distanceSquared`, so a too-far allied
  hit still deals ZERO — it just prints instead of blinking. That is the same
  shape `teleportation.yml` already ships for the non-heroic (both arms open
  with CANCEL), and `bidirectional-teleportation.yml` follows it. The matrix
  line should move the cancel above the range test.
- **APPLIED** — **`matrix/06` § Vengeful Diminish — the armed-cap state is NOT
  per-enchant.**
  The interactions line records the jar's shared armed-cap key as a structural
  change ("engine state is per-enchant"). It is not: `DamageCapStore` holds ONE
  armed cap keyed by player UUID, and a re-arm replaces it wholesale (factor and
  reflect together), so the jar's shared-key behaviour is reproduced exactly and
  the co-occurrence caveat the entry raises is the jar's own. The matrix line
  should drop the structural-change note and record the last-arm-wins wrinkle
  instead; `vengeful-diminish.yml` carries the corrected reading.
- **NO-OP for the matrix docs** (already replaced by the `matrix/13` pointer this
  bullet carries) — **The support-item codex (§C) — CLOSED by
  `matrix/13-support-items.md`.** The
  §C decomposition pass commissioned by R-QC12 + R-QC47 has landed: 27 entries
  over the crystals, the heroic tiers, the extractor, the shard and the whole
  economy-item roster, decomposed onto the 22 item-likeness `type:` keys. It
  overturns the claim this row used to carry — the crystal item, the targeted
  and random upgrades, the extractor and the shard are EACH recorded with
  material, name and full lore, so batch 8's marked port-choices in
  `packs-src/cosmic-pack/items/crystal.yml` and `items/heroic.yml` are
  settleable from source and should be replaced with the strings that doc
  carries. Its own § Corrections this pass owes other documents lists the rest.
- **APPLIED** — **`matrix/10` — the Infinite Luck exemption is awarded twice, to
  three sets.**
  § Dimensional Traveler calls its `+30 %` hook "NO Infinite Luck check (unique
  among damage sets, measured)" while § Dragon Slayer calls the same exemption
  "shared with KOTH — the only two damage-modifying sets without one", and
  § KOTH repeats the pair. Three sets cannot fill two slots, and one of the two
  lines has to go. The pack inherited the contradiction across files, which is
  what a batch-08 verifier flagged: `crystals/koth.yml` and `sets/koth.yml` and
  `sets/dragon-slayer.yml` all spell it "the only two", and the Dimensional
  Traveler pair spelled it "unique" / "uniquely in the family".
  `crystals/dimensional-traveler.yml` is normalised to the majority spelling —
  it now states the uncontested fact (this hook has no check) and claims no
  exclusivity — and cites this row; `sets/dimensional-traveler.yml` still
  carries "uniquely in the family" and should follow whichever way the matrix
  lands. Nothing behavioural: no Infinite Luck condition is authored on any of
  the three, so the shipped folds are already correct either way.
  RESOLVED in favour of Dragon Slayer + KOTH — the majority reading and what
  `sets/koth.yml`, `sets/dragon-slayer.yml` and `crystals/koth.yml` all ship.
  § Dimensional Traveler now states only the uncontested fact and claims no
  exclusivity; § KOTH names the pair explicitly, so all three sections agree.
  § Phantom's `>= 3` lost its "highest of any set" superlative in the same pass
  (Dimensional Traveler records `>= 5`, Mother of Yijki `>= 4`).
- **NO-OP** (the bullet says so itself: nothing is owed to the doc) — **`matrix/10`
  § Ghost — the Anti-M-Kit section number.** The entry already
  flags it (`§C.6` in the codex's prose, `### C.2` as the heading), so nothing
  is owed to the doc; repeated here only because the crystal files cite the
  drift and a reader meeting `§C.2` in the codex should not think they have the
  wrong section.
