version: 2
updates:
  # Keep GitHub Actions in our workflows current.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    labels:
      - dependencies
      - ci

  # Gradle dependency updates are enabled once the build is scaffolded.
  # - package-ecosystem: gradle
  #   directory: /
  #   schedule:
  #     interval: weekly
  #   commit-message:
  #     prefix: build
  #   labels:
  #     - dependencies
