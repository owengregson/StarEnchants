/**
 * The StarEnchants type system: one {@link schema.spec.ParamSpec} per DSL kind. A single
 * declaration drives validation, tab-completion, docs, and migration, so the four can't drift
 * (docs/architecture.md §7, §10).
 */
package schema.spec;
