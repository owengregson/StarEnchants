/**
 * Selector sub-grammar: parses a {@code @Head{k=v,k=v}} token into an untyped
 * {@link schema.grammar.sel.SelectorAst}; typing is se-compile's job (docs/architecture.md §2).
 */
package schema.grammar.sel;
