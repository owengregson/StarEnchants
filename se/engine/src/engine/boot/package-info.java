/**
 * Boot-time wiring of the engine's built-in registries into the production content
 * {@link compile.Compiler} (docs/architecture.md §2.1, §7). See {@link engine.boot.ContentCompiler}.
 */
package engine.boot;
