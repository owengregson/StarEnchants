/**
 * The concrete effect kinds — the merged Cosmic Enchants-style catalog (§7).
 * {@link engine.effect.kind.BuiltinEffects} is the one greppable file that registers them all.
 */
package engine.effect.kind;
