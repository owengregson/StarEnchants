/**
 * The built-in selector kinds ({@code Self/Victim/Attacker/Aoe/Nearest}) and the
 * explicit {@link engine.selector.kind.BuiltinSelectors} registry that wires them
 * (docs/architecture.md §7).
 */
package engine.selector.kind;
