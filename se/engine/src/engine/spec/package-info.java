/**
 * The effect SPI's declarative surface (§7): {@link engine.spec.EffectSpec} wraps the schema {@code ParamSpec}
 * and adds the engine-specific facts (declared {@link compile.model.Affinity}, {@link engine.spec.TargetSpec}
 * slots). {@link engine.spec.T} names the built-in selector kinds.
 */
package engine.spec;
