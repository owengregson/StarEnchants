# First-boot extraction manifest for bundled config packs (ADR-0023). One pack archive per line,
# relative to packs/. Each listed .zip is copied to <data>/packs/ on first enable (never overwriting
# an operator's copy) so /se pack apply <name> can swap it in. Each shipped .zip is produced at build
# time from its reviewable config tree — se/bootstrap/packs-src/signature-pack/ (the boot default) and
# se/bootstrap/packs-src/cosmic-pack/ (an applyable preset).
signature-pack.zip
cosmic-pack.zip
