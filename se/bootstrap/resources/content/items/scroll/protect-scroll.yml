# Protect Scroll (white scroll) — stamps a one-shot guard marker on gear so the NEXT failed enchant
# apply spares the item from destruction instead of shattering it. Carries a `role` rather than content.
display: "&fProtect Scroll"
description: "Protects an item — a failed enchant will spare it once."
tier: rare
kind: scroll
material: PAPER
grants: { role: PROTECT }
