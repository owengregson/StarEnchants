# Manifest of default files extracted to <plugin>/menus/ on first boot (one path per line, relative to
# menus/). Each menus/<name>.yml overrides the layout of the GUI registered under <name>: apply, enchants,
# sets, crystals, reference, transmog, enchanter, alchemist, tinkerer, admin. Any field you omit keeps that
# menu's built-in default; a menu with no file here uses its default entirely. Operator edits are never
# overwritten. Reload with /se reload.
apply.yml
enchanter.yml
