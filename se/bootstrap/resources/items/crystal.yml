# The physical crystal item — drag it onto gear to apply it (a success roll), or drag one crystal onto
# another to MERGE them into a multi-crystal (two effects in one crystal slot). Everything below is
# configurable; reload with /se reload. {CRYSTAL} renders the component crystal name(s).
#
# The material/name/lore here are the SHARED FALLBACK: each crystal (content/crystals/*.yml) declares its
# own material/name/lore so it self-explains its effect, and only falls back to these when it omits a
# field. They are also the likeness of a MERGED multi-crystal — whose two effects can't share one
# material — whose lore is each component crystal's own lore, concatenated.
#
# Per-item crystal SLOT capacity is not here — it is a cross-cutting knob (master config.yml crystals:
# section, a later increment); until then it defaults to 1.
type: crystal
material: AMETHYST_SHARD
name: "&d{CRYSTAL} Crystal"
lore:
  - "&7Drag onto gear to apply."
  - "&7Merge two crystals into a multi-crystal."
success-chance: 75
consume-on-fail: true
# Apply/merge and extract gesture sounds (namespaced keys, valid across the version range).
sounds:
  enabled: true
  apply: "block.amethyst_block.chime"
  remove: "block.amethyst_cluster.break"
# The crystal EXTRACTOR — drag it onto crystal-bearing gear to pop its last crystal back into a whole item.
extractor:
  material: AMETHYST_CLUSTER
  name: "&dCrystal Extractor"
  lore:
    - "&7Drag onto crystal-bearing gear"
    - "&7to extract its last crystal."
