# Matrix: 12-pets.md § Gaia Pet
# An ACTIVE inventory pet whose WHOLE payload is the Nature's Wrath armour-enchant package at 50 % duration,
# free of its soul cost: wipe every mob in range, then root, drain and weaken the hostile players around you.
# `content/enchants/nature-wrath.yml` is the same package one door over — this file mirrors its authoring
# choices deliberately, because the two ARE one behaviour and two readings of the same matrix rows would drift.
#
# FAMILY FACTS (matrix § Family-level facts, shared by all 17 entries; recorded once per file):
#  - The jar fires on ANY interaction (no action filter, codex §A.2); as intended it is right-click only,
#    which IS the engine's USE trigger (ledger D-12-1). The 1 Hz hotbar ticker is a no-op for every pet, so
#    nothing passive is ported and this pet is `type: ACTIVE` with no worn ability.
#  - COOLDOWN: the jar computes `cooldown − now − last-used`, always a large negative, so no pet cooldown ever
#    blocked and its on-cooldown branch is dead code (codex §A.6). The AS-INTENDED value ships: 600 s =
#    12 000 t (ledger D-12-25), on the engine's own gate-6 cooldown in place of the matrix's
#    SET_VAR/`pet-cd-gaia` template.
#  - XP: pets earn ONLY from using their own ability. `ITEM_XP_TRACK` carries the grant, the family's two
#    verbatim lines and the one-level-per-grant/remainder-banked/unbounded-at-cap semantics; `exp-curve` +
#    `max-level` carry this pet's ladder. The recorded 50-XP warzone tier needs faction territory, which has
#    no engine analogue, so it collapses to the flat 5 (ledger D-12-24). The jar's always-empty/overflowing
#    50-segment bar (codex §A.5) is replaced by the engine's proportional clamped bar (ledger D-12-2).
#  - LORE SPLIT: `descriptor` is the flavour block and `description` the `§7§f§lAbility` body, both verbatim —
#    and the body carries its own `§7§f§lAbility` / `§7§f§lCooldown` headers and cooldown value line
#    (`§7§7 10 Minutes`). The pack's ONE pet likeness, `items/pet.yml`, holds the cosmic name template and the
#    family's Level/EXP tail, and nothing else.
#  - HEAD: the source's own `skinBlob`, authored verbatim below (R-QC56); the family reading is in
#    `items/pet.yml`.
#  - Pet heads are invalid helmet items (the masks family refuses to wear one) — an interaction-layer rule.
# FAMILY-WIDE, AUTHORED ONCE FOR THE FOLDER: items/pet.yml + config.yml — full reading in anti-teleblock.yml.
# THE NO-TARGET ANSWER IS THE SELECTION ITSELF (ADR-0076). The jar wipes the mobs FIRST and only then throws
# when no enemy player is in range, so a mob-only click still cleared the mobs — for free, since the throw
# also skipped the XP and the cooldown stamp. All three halves now express, and the pre-selection gate that
# stood in for them is GONE: the payload always runs (so the free mob wipe is back), `%selected%` reports what
# the player package actually caught, and `REFUND_COOLDOWN{unless:}` hands the window back on a whiff. What
# the gate cost is worth naming, because it is what closes: `%nearbyenemies%` is a FIXED 8-block scan of
# LIVING ENTITIES where the payload reaches 13–28 blocks and the jar's check was hostile PLAYERS only, so a
# lone cow inside 8 blocks opened the ability and a player at 20 blocks did not.
# The verbatim `§c§lPET: §cNo valid enemy players nearby!` is now the exact complement of the real selection.
# It is a sibling ability rather than a row, because a message cannot be gated inside an ability, and it reads
# a captured VAR rather than `%selected%` directly: only the IMMEDIATELY NEXT ability can read that fact (an
# ability that does not activate publishes −1 over it, R-QC67), and this pet needs TWO branches. So the
# payload copies the count into `gaia.hit` beside a 1-tick `gaia.fired` marker — the `lava-elemental.yml`
# idiom one door over — and the cue/XP arm and the refusal arm read the pair. `gaia.fired` is what keeps the
# refusal off a click the payload never ran at all (spectating, or on cooldown).
# WHAT THE PACKAGE DROPS, each for a reason another file already settled:
#  - `POTION(JUMP, level=129)`: NOT shipped on either lane, by the pack-wide amplifier-128 ruling
#    (`titan-trap.yml`, restated on `tombstone.yml` / `enchants/horrify`) — on 1.8 it is an integer overflow
#    read as a no-jump lock, and on the modern lane it is a real and enormous jump that would fling the very
#    targets the proc exists to pin. FREEZE's own walk lock IS the hold.
#  - `POTION(SLOW, level=129)`: the amp-128 SLOW rung carries no overflow hazard, but `FREEZE(slow: 100)` is
#    already a full movement stop and `nature-wrath.yml` ships the package without it. Kept out so the two
#    files read as one behaviour.
#  - the per-DoT-tick victim feedback — `§2§l** NATURE'S WRATH **`, `GHAST_SCREAM2` 2.0/2.0 and the SPELL
#    burst at the victim: FREEZE has no per-pulse hook (only PERIODIC_DAMAGE does, and re-homing the DoT there
#    would move its attribution). Recorded on the shared enchant's entry, dropped here as cosmetics.
#  - the `wrath-active` per-victim re-proc guard: `each-cooldown:` (ADR-0076) is now the shape for it, but the
#    pet's own 10-minute cooldown already paces re-procs far harder than the jar's guard did, so the row would
#    only ever bite across two owners. Dropped, recorded.
#  - the break-free roll: the matrix records `30 − 7.5 × L` % on the ENCHANT's entry and none on this one, so
#    `breakout-chance` stays at its 0 default here — the pet's root is unbreakable for its (halved) window.
# The mob wipe uses `DESPAWN`, not the matrix's `KILL`: the jar deletes via `remove()` — no drops, no XP, no
# death event — which is exactly `DESPAWN`, so ledger D-12-23 (KILL would fire vanilla deaths and hand out the
# loot the jar never dropped) is NOT taken. `nature-wrath.yml` ships the same verb under D-02-3.
# interactions: this is a free, soul-cost-free proc of the Nature's Wrath armour enchant (matrix/02) at 50 %
# duration. The Poltergeist mastery immunity `nature-wrath.yml` carries is EXPRESSIBLE here now that it is an
# `each-if:` on the payload row rather than a division of the ability's `chance:` (which a deterministic pet
# click had nothing to divide). It is not authored in this pass — the per-target wave's content stage names
# the enchant and the two Dimensional Traveler copies, not this pet — so it stays a ledger row, and until it
# lands a Poltergeist wearer takes the pet's root where the enchant would spare them at `12.5 % × level`.
# The jar's own exclusion list (WorldGuard PvP, spectator, gamemode, god/staff mode,
# faction truce/ally, protection-flagged mobs, ender crystals) maps onto the ENEMIES filter plus the engine's
# global PvP gates, and the koth-world short release depends on a named external world — recorded, not ported.
# era: legacy particle names `LARGE_EXPLODE`/`SPELL` author as `EXPLOSION_LARGE`/`SPELL`, and `DIG_SNOW`,
# `WITHER_SHOOT` and `ENDERDRAGON_GROWL` as their modern spellings. `WITHER_SHOOT` and `ENDERDRAGON_GROWL`
# already have `Aliases.SOUND` rows, and so does `DIG_SNOW → BLOCK_SNOW_BREAK`, so every cue plays on the
# legacy fork. FREEZE has no 1.8 visual at all: the legacy sink no-ops the pin and the vignette and the
# authored slow lands as an NMS movement-speed modifier, so the root is DoT + slow (see `nature-wrath.yml`).
# FOLDER HARMONISATION (owner ruling): '&' colour form, own Cooldown block, spectator refusal as its own ability.
display: "Gaia Pet"
color: "&e"
type: ACTIVE
# HEAD TEXTURE (R-QC56): the source's own `skinBlob` constant, verbatim — the family reading (why the
# blob IS the identity, and the two source data bugs) is in `items/pet.yml`.
head: ""
descriptor:
  - "§7§7A segment of the goddess"
  - "§7§7of the earth and nature itself."
description:
  - "§7"
  - "§7§f§lAbility"
  # The source renders a ROMAN numeral here (`RomanNumber.toRoman(level)` — `Nature Wrath I` … `IV`). The pet
  # lore surface has no roman token (the renderer fills COLOR/NAME/TIME_FORMATTED/LEVEL/MAX_LEVEL/EXP/EXP_NEXT/
  # EXP_BAR and nothing else) and one `description` serves all four brackets, so the live level renders in
  # arabic through {LEVEL}. Inventing a token for one line is not the trade; the numeral form is the residue.
  - "§7§7Call upon the wrath of Nature."
  - "§7§7[§cNature Wrath {LEVEL}§7] (50% duration)"
  - "§7"
  - "§7§f§lCooldown"
  - "§7§7 10 Minutes"
# The recorded ladder `1000 + level × 500` (the cost of REACHING level N): 2000 / 2500 / 3000, 7500 to cap —
# 1500 uses at 5 XP each.
exp-curve: { base: 1000, per-level: 500 }
max-level: 4
levels:
  # Per level: radius 8 + 5×L = 13/18/23/28; potion window (7+L)×20 t = 160/180/200/220; the walk-lock/DoT
  # window is HALF that ((7+L)×10 = 80/90/100/110 t) — the jar's ×0.5 release, which is the "(50% duration)"
  # the lore advertises — so the DoT lands 4/4/5/5 ticks at L HP each: 4/8/15/20 total. Cooldown flat 600 s.
  1:
    cooldown: 12000
    abilities:
      - condition: '%actor.gamemode% != "SPECTATOR"'
        effects:
          - { SET_VAR: { name: "gaia.fired", value: "1", ttl: 1, who: "@Self" } }
          # The mob sweep runs first and its two bursts carry the sweep's own selector, so they resolve the mob
          # set while it still exists (the per-mob closing cue). It is unconditional again: the jar's free
          # mob-only wipe is back, because nothing gates the ability ahead of its own selection now.
          - { LIGHTNING: { damage: 0, who: "@Aoe{r=13, filter=MOBS}" } }
          - { PARTICLE: { particle: "EXPLOSION_LARGE", count: 10, spread: 0.6, who: "@Aoe{r=13, filter=MOBS}" } }
          - { PARTICLE: { particle: "SPELL", count: 35, spread: 0.4, who: "@Aoe{r=13, filter=MOBS}" } }
          - { DESPAWN: { who: "@Aoe{r=13, filter=MOBS}" } }
          # The player package: hostile PLAYERS only (the wave-1 filter conjunction, `nature-wrath.yml`'s reading
          # of the entry's player-only payload). %selected% below is THIS package's post-filter count.
          - { LIGHTNING: { damage: 0, who: "@Aoe{r=13, filter=ENEMIES+PLAYERS}" } }
          - { POTION: { effect: "WEAKNESS", level: 3, duration: 160, who: "@Aoe{r=13, filter=ENEMIES+PLAYERS}" } }
          - { FREEZE: { duration: 80, dot: 1, dot-period: 20, slow: 100, neutralize-frost-slow: false, who: "@Aoe{r=13, filter=ENEMIES+PLAYERS}" } }
          # Both read the FREEZE row's count: the refund inline, the capture into a var the two arms below can
          # still see. Order matters — SET_VAR declares a target slot of its own, so it overwrites %selected%
          # the moment it runs, and the refund has to ask first.
          - { REFUND_COOLDOWN: { unless: "%selected%" } }
          - { SET_VAR: { name: "gaia.hit", op: increment, step: "%selected%", ttl: 1, who: "@Self" } }
      # Caught somebody: the proc's cues and its XP. Recorded per frozen victim; ADR-0066 plays one sound id at
      # most once per event, so the copies collapse.
      - condition: '%gaia.fired% == 1 && %gaia.hit% >= 1'
        cooldown: 0
        effects:
          - { SOUND: { sound: "ENTITY_ENDER_DRAGON_GROWL", volume: 2.0, pitch: 2.0 } }
          - { MESSAGE: { text: "§a§lPET: ** NATURE'S WRATH **" } }
          - { SOUND: { sound: "BLOCK_SNOW_BREAK", volume: 1.1, pitch: 3.0 } }
          - { SOUND: { sound: "ENTITY_WITHER_SHOOT", volume: 1.1, pitch: 3.0 } }
          - { ITEM_XP_TRACK: { amount: 5, message: "§a§l+ §a{xp} Pet EXP §a§l[§7{exp}/{needed}§a§l]", level-up-message: "§a§l*** {item}§a has increased to level §a§n{level}§a§l ***" } }
      # Caught nobody: the verbatim refusal. The window was already handed back above, and no XP banks.
      - condition: '%gaia.fired% == 1 && %gaia.hit% < 1'
        cooldown: 0
        effects:
          - { MESSAGE: { text: "§c§lPET: §cNo valid enemy players nearby!" } }
      - condition: '%actor.gamemode% == "SPECTATOR"'
        cooldown: 0
        effects:
          - { MESSAGE: { text: "§c§lPET: §cYou cannot use that ability while spectating!" } }
  2:
    cooldown: 12000
    abilities:
      - condition: '%actor.gamemode% != "SPECTATOR"'
        effects:
          - { SET_VAR: { name: "gaia.fired", value: "1", ttl: 1, who: "@Self" } }
          # The mob sweep runs first and its two bursts carry the sweep's own selector, so they resolve the mob
          # set while it still exists (the per-mob closing cue). It is unconditional again: the jar's free
          # mob-only wipe is back, because nothing gates the ability ahead of its own selection now.
          - { LIGHTNING: { damage: 0, who: "@Aoe{r=18, filter=MOBS}" } }
          - { PARTICLE: { particle: "EXPLOSION_LARGE", count: 10, spread: 0.6, who: "@Aoe{r=18, filter=MOBS}" } }
          - { PARTICLE: { particle: "SPELL", count: 35, spread: 0.4, who: "@Aoe{r=18, filter=MOBS}" } }
          - { DESPAWN: { who: "@Aoe{r=18, filter=MOBS}" } }
          # The player package: hostile PLAYERS only (the wave-1 filter conjunction, `nature-wrath.yml`'s reading
          # of the entry's player-only payload). %selected% below is THIS package's post-filter count.
          - { LIGHTNING: { damage: 0, who: "@Aoe{r=18, filter=ENEMIES+PLAYERS}" } }
          - { POTION: { effect: "WEAKNESS", level: 3, duration: 180, who: "@Aoe{r=18, filter=ENEMIES+PLAYERS}" } }
          - { FREEZE: { duration: 90, dot: 2, dot-period: 20, slow: 100, neutralize-frost-slow: false, who: "@Aoe{r=18, filter=ENEMIES+PLAYERS}" } }
          # Both read the FREEZE row's count: the refund inline, the capture into a var the two arms below can
          # still see. Order matters — SET_VAR declares a target slot of its own, so it overwrites %selected%
          # the moment it runs, and the refund has to ask first.
          - { REFUND_COOLDOWN: { unless: "%selected%" } }
          - { SET_VAR: { name: "gaia.hit", op: increment, step: "%selected%", ttl: 1, who: "@Self" } }
      # Caught somebody: the proc's cues and its XP. Recorded per frozen victim; ADR-0066 plays one sound id at
      # most once per event, so the copies collapse.
      - condition: '%gaia.fired% == 1 && %gaia.hit% >= 1'
        cooldown: 0
        effects:
          - { SOUND: { sound: "ENTITY_ENDER_DRAGON_GROWL", volume: 2.0, pitch: 2.0 } }
          - { MESSAGE: { text: "§a§lPET: ** NATURE'S WRATH **" } }
          - { SOUND: { sound: "BLOCK_SNOW_BREAK", volume: 1.1, pitch: 3.0 } }
          - { SOUND: { sound: "ENTITY_WITHER_SHOOT", volume: 1.1, pitch: 3.0 } }
          - { ITEM_XP_TRACK: { amount: 5, message: "§a§l+ §a{xp} Pet EXP §a§l[§7{exp}/{needed}§a§l]", level-up-message: "§a§l*** {item}§a has increased to level §a§n{level}§a§l ***" } }
      # Caught nobody: the verbatim refusal. The window was already handed back above, and no XP banks.
      - condition: '%gaia.fired% == 1 && %gaia.hit% < 1'
        cooldown: 0
        effects:
          - { MESSAGE: { text: "§c§lPET: §cNo valid enemy players nearby!" } }
      - condition: '%actor.gamemode% == "SPECTATOR"'
        cooldown: 0
        effects:
          - { MESSAGE: { text: "§c§lPET: §cYou cannot use that ability while spectating!" } }
  3:
    cooldown: 12000
    abilities:
      - condition: '%actor.gamemode% != "SPECTATOR"'
        effects:
          - { SET_VAR: { name: "gaia.fired", value: "1", ttl: 1, who: "@Self" } }
          # The mob sweep runs first and its two bursts carry the sweep's own selector, so they resolve the mob
          # set while it still exists (the per-mob closing cue). It is unconditional again: the jar's free
          # mob-only wipe is back, because nothing gates the ability ahead of its own selection now.
          - { LIGHTNING: { damage: 0, who: "@Aoe{r=23, filter=MOBS}" } }
          - { PARTICLE: { particle: "EXPLOSION_LARGE", count: 10, spread: 0.6, who: "@Aoe{r=23, filter=MOBS}" } }
          - { PARTICLE: { particle: "SPELL", count: 35, spread: 0.4, who: "@Aoe{r=23, filter=MOBS}" } }
          - { DESPAWN: { who: "@Aoe{r=23, filter=MOBS}" } }
          # The player package: hostile PLAYERS only (the wave-1 filter conjunction, `nature-wrath.yml`'s reading
          # of the entry's player-only payload). %selected% below is THIS package's post-filter count.
          - { LIGHTNING: { damage: 0, who: "@Aoe{r=23, filter=ENEMIES+PLAYERS}" } }
          - { POTION: { effect: "WEAKNESS", level: 3, duration: 200, who: "@Aoe{r=23, filter=ENEMIES+PLAYERS}" } }
          - { FREEZE: { duration: 100, dot: 3, dot-period: 20, slow: 100, neutralize-frost-slow: false, who: "@Aoe{r=23, filter=ENEMIES+PLAYERS}" } }
          # Both read the FREEZE row's count: the refund inline, the capture into a var the two arms below can
          # still see. Order matters — SET_VAR declares a target slot of its own, so it overwrites %selected%
          # the moment it runs, and the refund has to ask first.
          - { REFUND_COOLDOWN: { unless: "%selected%" } }
          - { SET_VAR: { name: "gaia.hit", op: increment, step: "%selected%", ttl: 1, who: "@Self" } }
      # Caught somebody: the proc's cues and its XP. Recorded per frozen victim; ADR-0066 plays one sound id at
      # most once per event, so the copies collapse.
      - condition: '%gaia.fired% == 1 && %gaia.hit% >= 1'
        cooldown: 0
        effects:
          - { SOUND: { sound: "ENTITY_ENDER_DRAGON_GROWL", volume: 2.0, pitch: 2.0 } }
          - { MESSAGE: { text: "§a§lPET: ** NATURE'S WRATH **" } }
          - { SOUND: { sound: "BLOCK_SNOW_BREAK", volume: 1.1, pitch: 3.0 } }
          - { SOUND: { sound: "ENTITY_WITHER_SHOOT", volume: 1.1, pitch: 3.0 } }
          - { ITEM_XP_TRACK: { amount: 5, message: "§a§l+ §a{xp} Pet EXP §a§l[§7{exp}/{needed}§a§l]", level-up-message: "§a§l*** {item}§a has increased to level §a§n{level}§a§l ***" } }
      # Caught nobody: the verbatim refusal. The window was already handed back above, and no XP banks.
      - condition: '%gaia.fired% == 1 && %gaia.hit% < 1'
        cooldown: 0
        effects:
          - { MESSAGE: { text: "§c§lPET: §cNo valid enemy players nearby!" } }
      - condition: '%actor.gamemode% == "SPECTATOR"'
        cooldown: 0
        effects:
          - { MESSAGE: { text: "§c§lPET: §cYou cannot use that ability while spectating!" } }
  4:
    cooldown: 12000
    abilities:
      - condition: '%actor.gamemode% != "SPECTATOR"'
        effects:
          - { SET_VAR: { name: "gaia.fired", value: "1", ttl: 1, who: "@Self" } }
          # The mob sweep runs first and its two bursts carry the sweep's own selector, so they resolve the mob
          # set while it still exists (the per-mob closing cue). It is unconditional again: the jar's free
          # mob-only wipe is back, because nothing gates the ability ahead of its own selection now.
          - { LIGHTNING: { damage: 0, who: "@Aoe{r=28, filter=MOBS}" } }
          - { PARTICLE: { particle: "EXPLOSION_LARGE", count: 10, spread: 0.6, who: "@Aoe{r=28, filter=MOBS}" } }
          - { PARTICLE: { particle: "SPELL", count: 35, spread: 0.4, who: "@Aoe{r=28, filter=MOBS}" } }
          - { DESPAWN: { who: "@Aoe{r=28, filter=MOBS}" } }
          # The player package: hostile PLAYERS only (the wave-1 filter conjunction, `nature-wrath.yml`'s reading
          # of the entry's player-only payload). %selected% below is THIS package's post-filter count.
          - { LIGHTNING: { damage: 0, who: "@Aoe{r=28, filter=ENEMIES+PLAYERS}" } }
          - { POTION: { effect: "WEAKNESS", level: 3, duration: 220, who: "@Aoe{r=28, filter=ENEMIES+PLAYERS}" } }
          - { FREEZE: { duration: 110, dot: 4, dot-period: 20, slow: 100, neutralize-frost-slow: false, who: "@Aoe{r=28, filter=ENEMIES+PLAYERS}" } }
          # Both read the FREEZE row's count: the refund inline, the capture into a var the two arms below can
          # still see. Order matters — SET_VAR declares a target slot of its own, so it overwrites %selected%
          # the moment it runs, and the refund has to ask first.
          - { REFUND_COOLDOWN: { unless: "%selected%" } }
          - { SET_VAR: { name: "gaia.hit", op: increment, step: "%selected%", ttl: 1, who: "@Self" } }
      # Caught somebody: the proc's cues and its XP. Recorded per frozen victim; ADR-0066 plays one sound id at
      # most once per event, so the copies collapse.
      - condition: '%gaia.fired% == 1 && %gaia.hit% >= 1'
        cooldown: 0
        effects:
          - { SOUND: { sound: "ENTITY_ENDER_DRAGON_GROWL", volume: 2.0, pitch: 2.0 } }
          - { MESSAGE: { text: "§a§lPET: ** NATURE'S WRATH **" } }
          - { SOUND: { sound: "BLOCK_SNOW_BREAK", volume: 1.1, pitch: 3.0 } }
          - { SOUND: { sound: "ENTITY_WITHER_SHOOT", volume: 1.1, pitch: 3.0 } }
          - { ITEM_XP_TRACK: { amount: 5, message: "§a§l+ §a{xp} Pet EXP §a§l[§7{exp}/{needed}§a§l]", level-up-message: "§a§l*** {item}§a has increased to level §a§n{level}§a§l ***" } }
      # Caught nobody: the verbatim refusal. The window was already handed back above, and no XP banks.
      - condition: '%gaia.fired% == 1 && %gaia.hit% < 1'
        cooldown: 0
        effects:
          - { MESSAGE: { text: "§c§lPET: §cNo valid enemy players nearby!" } }
      - condition: '%actor.gamemode% == "SPECTATOR"'
        cooldown: 0
        effects:
          - { MESSAGE: { text: "§c§lPET: §cYou cannot use that ability while spectating!" } }
