# Cosmic codex source: 04-enchants-axes.md — axes/BossSlayer.java
# Source-code values intentionally win over the advertised 5%: it multiplies by 1 + 0.075 * level.
display: Boss Slayer
description: Deal an extra (level * 5%) base damage to bosses.
trigger:
- ATTACK
applies-to:
- AXE
group: legendary
tier: legendary
stacking: HIGHEST
levels:
  1: { condition: &gate '%victim.isplayer% == 0 && %victim.boss% == true && %actor.heldsettled5% == true', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.075 } }] }
  2: { condition: *gate, effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.15 } }] }
  3: { condition: *gate, effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.225 } }] }
  4: { condition: *gate, effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.30 } }] }
  5: { condition: *gate, effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.375 } }] }
