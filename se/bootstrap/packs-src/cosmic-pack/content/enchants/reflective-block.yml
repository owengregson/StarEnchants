# Imported from EliteEnchantments (id: reflective-block). Review before shipping.
display: "Reflective Block"
description:
  - "&eMythic upgrade for Block."
  - "&eWhile blocking, greatly reduces"
  - "&eor fully negates incoming damage"
  - "&eand strikes the attacker back."
  - "&c&l(!)&r &7Requires &eBlock"
trigger: DEFENSE
applies-to: [SWORD]
group: "mythic"
tier: mythic
requires: ["enchants/block"]
removes-required: true  # the mythic upgrade supersedes its base counterpart (net-zero enchant slots)
levels:
  1:
    chance: 45
    cooldown: 100
    condition: "%blocking% == true"
    effects:
      - { SOUND: { sound: "ITEM_SHIELD_BLOCK", volume: 1.0, pitch: 1.2 } }
      - { PARTICLE: { particle: "CRIT_MAGIC", count: 8 } }
      - { DAMAGE_MOD: { side: "defense", mode: "add", amount: 80 } }  # from REDUCTION:80
      - { DAMAGE: { amount: 8, who: "@Attacker" } }  # from DAMAGE:3:8:TARGET — legacy random range 3-8 collapsed to its max
  2:
    chance: 65
    cooldown: 100
    condition: "%blocking% == true"
    effects:
      - { SOUND: { sound: "ITEM_SHIELD_BLOCK", volume: 1.0, pitch: 1.2 } }
      - { PARTICLE: { particle: "CRIT_MAGIC", count: 8 } }
      - { DAMAGE_MOD: { side: "defense", mode: "add", amount: 90 } }  # from REDUCTION:90
      - { DAMAGE: { amount: 8, who: "@Attacker" } }  # from DAMAGE:3:8:TARGET — legacy random range 3-8 collapsed to its max
  3:
    chance: 80
    cooldown: 100
    condition: "%blocking% == true"
    effects:
      - { SOUND: { sound: "ITEM_SHIELD_BLOCK", volume: 1.0, pitch: 1.2 } }
      - { PARTICLE: { particle: "CRIT_MAGIC", count: 8 } }
      - { DAMAGE_MOD: { side: "defense", mode: "add", amount: 100 } }  # from REDUCTION:100
      - { DAMAGE: { amount: 8, who: "@Attacker" } }  # from DAMAGE:3:8:TARGET — legacy random range 3-8 collapsed to its max
