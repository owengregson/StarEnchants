# Cosmic codex source: 06-enchants-tools-heroic.md — heroic/HeroicAssassin.java
display: Shadow Assassin
description: Heroic Enchantment. The closer you are to your enemy, the more damage you deal. Requires Assassin V enchant on item to apply. Cannot be black-scrolled.
trigger: ATTACK
applies-to: [SWORD]
group: heroic
tier: heroic
disabled-environments: [THE_END]
stacking: HIGHEST
levels:
  1:
    abilities:
      - { trigger: ATTACK, condition: '%distance% < 0.625', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.075 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 0.625 && %distance% < 1.25', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.06 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 1.25 && %distance% < 1.875', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.045 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 1.875 && %distance% < 2.5', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.03 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 2.5 && %distance% < 3.75', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.015 } }] }
  2:
    abilities:
      - { trigger: ATTACK, condition: '%distance% < 0.625', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.15 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 0.625 && %distance% < 1.25', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.12 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 1.25 && %distance% < 1.875', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.09 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 1.875 && %distance% < 2.5', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.06 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 2.5 && %distance% < 3.75', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.03 } }] }
  3:
    abilities:
      - { trigger: ATTACK, condition: '%distance% < 0.625', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.225 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 0.625 && %distance% < 1.25', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.18 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 1.25 && %distance% < 1.875', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.135 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 1.875 && %distance% < 2.5', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.09 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 2.5 && %distance% < 3.75', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.045 } }] }
  4:
    abilities:
      - { trigger: ATTACK, condition: '%distance% < 0.625', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.3 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 0.625 && %distance% < 1.25', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.24 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 1.25 && %distance% < 1.875', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.18 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 1.875 && %distance% < 2.5', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.12 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 2.5 && %distance% < 3.75', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.06 } }] }
  5:
    abilities:
      - { trigger: ATTACK, condition: '%distance% < 0.625', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.375 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 0.625 && %distance% < 1.25', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.3 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 1.25 && %distance% < 1.875', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.225 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 1.875 && %distance% < 2.5', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.15 } }] }
      - { trigger: ATTACK, condition: '%distance% >= 2.5 && %distance% < 3.75', effects: [{ DAMAGE_MOD: { side: attack, mode: multiply, amount: 1.075 } }] }
requires: [enchants/assassin]
removes-required: true
