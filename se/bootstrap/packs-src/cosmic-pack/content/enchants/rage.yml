# Imported from Cosmic Enchants (id: rage). Review before shipping.
display: "Rage"
description:
  - "&eEach consecutive hit in a combo"
  - "&eadds stacking bonus damage."
trigger: ATTACK
applies-to: [WEAPON]
group: "legendary"
tier: legendary
levels:
  1:
    effects:
      - { PARTICLE: { particle: "FLAME", count: 10, who: "@Victim" } }
      - { DAMAGE_MOD: { side: "attack", mode: "add", amount: "%ragestacks% * 110" } }  # stacking combo percent scaling the fold; %ragestacks% = min(streak, level); the rage-stacks service owns the audio/feedback
  2:
    effects:
      - { PARTICLE: { particle: "FLAME", count: 10, who: "@Victim" } }
      - { DAMAGE_MOD: { side: "attack", mode: "add", amount: "%ragestacks% * 140" } }  # stacking combo percent scaling the fold; %ragestacks% = min(streak, level); the rage-stacks service owns the audio/feedback
  3:
    effects:
      - { PARTICLE: { particle: "FLAME", count: 10, who: "@Victim" } }
      - { DAMAGE_MOD: { side: "attack", mode: "add", amount: "%ragestacks% * 165" } }  # stacking combo percent scaling the fold; %ragestacks% = min(streak, level); the rage-stacks service owns the audio/feedback
  4:
    effects:
      - { PARTICLE: { particle: "FLAME", count: 10, who: "@Victim" } }
      - { DAMAGE_MOD: { side: "attack", mode: "add", amount: "%ragestacks% * 195" } }  # stacking combo percent scaling the fold; %ragestacks% = min(streak, level); the rage-stacks service owns the audio/feedback
  5:
    effects:
      - { PARTICLE: { particle: "FLAME", count: 10, who: "@Victim" } }
      - { DAMAGE_MOD: { side: "attack", mode: "add", amount: "%ragestacks% * 220" } }  # stacking combo percent scaling the fold; %ragestacks% = min(streak, level); the rage-stacks service owns the audio/feedback
  6:
    effects:
      - { PARTICLE: { particle: "FLAME", count: 10, who: "@Victim" } }
      - { DAMAGE_MOD: { side: "attack", mode: "add", amount: "%ragestacks% * 250" } }  # stacking combo percent scaling the fold; %ragestacks% = min(streak, level); the rage-stacks service owns the audio/feedback
